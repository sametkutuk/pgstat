{
  "schema_version": "1.0",
  "updated_at": "2026-08-31",
  "project": {
    "name": "pgstat product family",
    "customer_role": "product customer / acceptance tester",
    "codex_role": "project manager + product manager + lead developer",
    "max_in_progress": 8,
    "operating_model": "one product program, two independent runtime layers: pgstat Core and pgdbaagent"
  },
  "status_lifecycle": [
    "planned",
    "in_progress",
    "codex_verified",
    "customer_validation",
    "done",
    "blocked"
  ],
  "closure_policy": {
    "done_requires_all_acceptance_criteria_done": true,
    "done_requires_codex_verification_evidence": true,
    "done_requires_customer_acceptance_or_not_required": true,
    "automatic_task_close": false,
    "reason": "A task can be suggested for closure by Codex after verification, but final done status needs explicit acceptance evidence or a documented not_required customer gate."
  },
  "tasks": [
    {
      "id": "PGSTAT-P0-009",
      "order": 0,
      "priority": "P0",
      "workstream": "governance",
      "status": "done",
      "title": "Harden docs impact checker rules",
      "problem": "Local hooks enforce generated docs and broad manual docs, but project status and task lifecycle need stricter checks.",
      "requirements": [
        "Validate project-board schema.",
        "Generate project status from project-board.",
        "Fail if done tasks lack verification or customer acceptance evidence.",
        "Fail if too many tasks are in_progress."
      ],
      "acceptance_criteria": [
        {
          "id": "AC1",
          "text": "Project board validator runs from local hooks.",
          "status": "done"
        },
        {
          "id": "AC2",
          "text": "Generated project status is staged by pre-commit and checked by pre-push.",
          "status": "done"
        },
        {
          "id": "AC3",
          "text": "Project execution plan documents the workflow.",
          "status": "done"
        }
      ],
      "implementation_plan": [
        "Create docs/project-board.json.",
        "Create generator and validator scripts.",
        "Wire scripts into local hooks."
      ],
      "verification_plan": [
        "node scripts/check-project-board.mjs",
        "node scripts/generate-project-status.mjs",
        ".githooks/pre-commit",
        ".githooks/pre-push"
      ],
      "required_docs": [
        "docs/project-board.json",
        "docs/generated/project-status.md",
        "docs/project-execution-plan.md"
      ],
      "dependencies": [],
      "customer_acceptance": {
        "required": false,
        "status": "not_required",
        "evidence": "Governance automation task for local project control."
      },
      "verification_evidence": [
        "Project board validator added.",
        "Generated project status added.",
        "Local hooks updated to validate board and reject project status drift.",
        "Acceptance criteria are governance automation checks, customer gate not required."
      ],
      "completed_at": "2026-07-14",
      "next_action": "Closed; continue with PGSTAT-P0-001."
    },
    {
      "id": "PGSTAT-P0-001",
      "order": 1,
      "priority": "P0",
      "workstream": "contracts",
      "status": "done",
      "title": "Promote next critical data-family contracts",
      "problem": "Many generated table/data-family contracts are still semantic scaffolds, so pgdbaagent could consume weak lifecycle evidence.",
      "requirements": [
        "Review the highest-value remaining data families from docs/generated/contract-review-queue.md.",
        "Confirm lifecycle semantics, schedule, retention, purge owner, partitioning, rollup, consumers, and security classification.",
        "Promote stable decisions into docs/data-contract-registry.md or generator hints where appropriate."
      ],
      "acceptance_criteria": [
        {
          "id": "AC1",
          "text": "At least the next 10 high-value data families have reviewed semantic contracts.",
          "status": "done"
        },
        {
          "id": "AC2",
          "text": "Each promoted family has retention and purge behavior documented.",
          "status": "done"
        },
        {
          "id": "AC3",
          "text": "Generated contract-review queue reflects the reduced review gap after regeneration.",
          "status": "done"
        }
      ],
      "implementation_plan": [
        "Use contract-review-queue priority and consumer count to pick the next families.",
        "Read migrations, collector repositories, PurgeEvaluator, PartitionManager, API routes, and UI references.",
        "Update contract hints/manual registry and regenerate generated docs."
      ],
      "verification_plan": [
        "node scripts/generate-doc-inventory.mjs",
        "node scripts/check-doc-impact.mjs --staged",
        "Review generated queue counts and promoted rows."
      ],
      "required_docs": [
        "docs/work-orders/PGSTAT-P0-001-critical-data-family-contracts.md",
        "docs/data-contract-registry.md",
        "docs/generated/data-family-contracts.md",
        "docs/generated/contract-review-queue.md",
        "docs/project-execution-plan.md",
        "docs/generated/project-status.md"
      ],
      "dependencies": [
        "PGSTAT-P0-009"
      ],
      "customer_acceptance": {
        "required": true,
        "status": "accepted",
        "evidence": "Customer reported live partition remediation was deployed and Opus verified it on 2026-07-14; aggregate contracts accepted with partition remediation tracked separately."
      },
      "verification_evidence": [
        "Promoted 10 aggregate data-family contracts: agg.pgss_hourly, agg.pgss_daily, agg.pg_table_stat_hourly, agg.pg_wal_hourly, agg.pg_wal_daily, agg.pg_activity_hourly, agg.pg_lock_hourly, agg.pg_replication_hourly, agg.pg_slru_hourly, agg.pg_archiver_hourly.",
        "docs/generated/data-family-contracts.md seeded semantic contracts increased to 46; generated defaults needing review decreased to 46.",
        "docs/generated/contract-review-queue.md table/data-family contracts needing semantic review decreased to 46.",
        "docs/generated/data-lifecycle-matrix.md purge-referenced tables requiring policy verification decreased to 3 after retention mapping for snapshot-derived aggregate families.",
        "Live pg_get_partkeydef validation confirmed agg.pgss_hourly, agg.pgss_daily, and agg.pg_table_stat_hourly are partitioned; agg.pg_wal_hourly and snapshot-derived hourly aggregates are not partitioned.",
        "Live validation exposed existing mixed timezone partition bounds in agg.pgss_hourly and agg.pg_table_stat_hourly; remediation was completed separately and recorded as PGSTAT-P0-021.",
        "Customer reported live partition remediation was deployed and Opus verified it.",
        "node scripts/generate-doc-inventory.mjs completed."
      ],
      "completed_at": "2026-07-14",
      "next_action": "Closed; continue with PGSTAT-P0-002."
    },
    {
      "id": "PGSTAT-P0-002",
      "order": 2,
      "priority": "P0",
      "workstream": "contracts",
      "status": "in_progress",
      "title": "Promote next field contracts beyond the first 161",
      "problem": "904 fields still need exact source/version/unsupported/consumer review.",
      "requirements": [
        "Promote fields by evidence value, not by table order.",
        "Start with fields required for Temp Spill, WAL Spike, Cache Hit, Vacuum Lag, Lock Wait, settings, and validation.",
        "Every promoted field must state source expression, since_pg, unsupported behavior, sensitivity, and consumers."
      ],
      "acceptance_criteria": [
        {
          "id": "AC1",
          "text": "At least 100 additional fields are promoted or explicitly marked out of pgdbaagent scope.",
          "status": "pending"
        },
        {
          "id": "AC2",
          "text": "Sensitive field policies are recorded for promoted fields.",
          "status": "pending"
        },
        {
          "id": "AC3",
          "text": "Generated field-contract review count decreases after regeneration.",
          "status": "pending"
        }
      ],
      "implementation_plan": [
        "Select fields from the first stable evidence packages.",
        "Verify source SQL and PostgreSQL version matrix.",
        "Update manual contracts/generator hints and regenerate docs."
      ],
      "verification_plan": [
        "node scripts/generate-doc-inventory.mjs",
        "node scripts/check-doc-impact.mjs --staged"
      ],
      "required_docs": [
        "docs/data-contract-registry.md",
        "docs/generated/field-contracts.md",
        "docs/generated/contract-review-queue.md",
        "docs/generated/project-status.md"
      ],
      "dependencies": [
        "PGSTAT-P0-001"
      ],
      "customer_acceptance": {
        "required": true,
        "status": "pending",
        "evidence": ""
      },
      "verification_evidence": [],
      "next_action": "Execute the first field-level contract batch for Temp Spill, WAL Spike, Cache Hit, Vacuum Lag, Lock Wait, settings, and validation evidence fields."
    },
    {
      "id": "PGSTAT-P0-011",
      "order": 3,
      "priority": "P0",
      "workstream": "security",
      "status": "planned",
      "title": "Define AI/export redaction policy",
      "problem": "Sensitive fields need explicit allow/block/mask rules before AI or export usage.",
      "requirements": [
        "Classify query text, application names, host metadata, settings, and event metadata.",
        "Define defaults for UI, report, export, alert, and AI contexts.",
        "Make pgdbaagent evidence builders enforce the policy."
      ],
      "acceptance_criteria": [
        {
          "id": "AC1",
          "text": "Redaction policy is documented.",
          "status": "pending"
        },
        {
          "id": "AC2",
          "text": "Sensitive generated field count has an owner and decision path.",
          "status": "pending"
        }
      ],
      "implementation_plan": [
        "Start with policy doc.",
        "Add code enforcement when evidence builders are implemented."
      ],
      "verification_plan": [
        "security review",
        "docs-impact check"
      ],
      "required_docs": [
        "docs/data-contract-registry.md",
        "docs/pgdbaagent-contracts.md",
        "docs/generated/project-status.md"
      ],
      "dependencies": [
        "PGSTAT-P0-002"
      ],
      "customer_acceptance": {
        "required": true,
        "status": "pending",
        "evidence": ""
      },
      "verification_evidence": [],
      "next_action": "Define redaction policy before planner stats or AI evidence export."
    },
    {
      "id": "PGSTAT-P0-012",
      "order": 4,
      "priority": "P0",
      "workstream": "release",
      "status": "done",
      "title": "Add release checklist and upgrade impact template",
      "problem": "Enterprise releases need explicit migration, config, retention, compatibility, and rollback notes.",
      "requirements": [
        "Create release checklist template.",
        "Include migration/retention/config/security/UI/API impact sections.",
        "Link checklist to project execution plan."
      ],
      "acceptance_criteria": [
        {
          "id": "AC1",
          "text": "Release checklist template exists.",
          "status": "done"
        },
        {
          "id": "AC2",
          "text": "Checklist is referenced by governance docs.",
          "status": "done"
        }
      ],
      "implementation_plan": [
        "Create docs/release-checklist-template.md.",
        "Wire it into governance and project execution docs."
      ],
      "verification_plan": [
        "docs-impact check"
      ],
      "required_docs": [
        "docs/platform-governance-and-sdlc.md",
        "docs/project-execution-plan.md",
        "docs/generated/project-status.md"
      ],
      "dependencies": [
        "PGSTAT-P0-009"
      ],
      "customer_acceptance": {
        "required": true,
        "status": "accepted",
        "evidence": "Customer instructed on 2026-07-17 to complete the missing SDLC items; the checklist template was delivered in the same session."
      },
      "verification_evidence": [
        "docs/release-checklist-template.md created with migration/retention/collector/contract/API/UI/config/security/testing/docs/upgrade/rollback sections and the semver VERSION rule.",
        "Referenced from docs/platform-governance-and-sdlc.md Section 11 and the project execution plan."
      ],
      "completed_at": "2026-07-17",
      "next_action": "Closed; releases now complete docs/release-checklist-template.md."
    },
    {
      "id": "PGSTAT-P0-003",
      "order": 5,
      "priority": "P0",
      "workstream": "telemetry",
      "status": "planned",
      "title": "Add catalog metadata collection contracts",
      "problem": "pgdbaagent needs table/column/index/constraint context to reason about query plans, index proposals, and write risk.",
      "requirements": [
        "Define source catalog queries for tables, columns, indexes, constraints, and relation ownership metadata.",
        "Keep production collection read-only and version-aware.",
        "Define retention, purge, and whether metadata is snapshot or slowly changing dimension."
      ],
      "acceptance_criteria": [
        {
          "id": "AC1",
          "text": "Capability card exists before implementation.",
          "status": "pending"
        },
        {
          "id": "AC2",
          "text": "Storage and retention model are documented.",
          "status": "pending"
        },
        {
          "id": "AC3",
          "text": "API/UI/pgdbaagent consumers are mapped.",
          "status": "pending"
        }
      ],
      "implementation_plan": [
        "Write capability card.",
        "Design schema and collector source queries.",
        "Implement migrations, collector, API, and docs together."
      ],
      "verification_plan": [
        "collector mvn compile",
        "api npx tsc --noEmit if API changes",
        "generated docs and docs-impact checks"
      ],
      "required_docs": [
        "docs/pgstat-data-source-dictionary.md",
        "docs/data-contract-registry.md",
        "docs/pgstat-telemetry-completion-roadmap.md",
        "docs/generated/project-status.md"
      ],
      "dependencies": [
        "PGSTAT-P0-001",
        "PGSTAT-P0-002",
        "PGSTAT-P0-011",
        "PGSTAT-P0-012"
      ],
      "customer_acceptance": {
        "required": true,
        "status": "pending",
        "evidence": ""
      },
      "verification_evidence": [],
      "next_action": "Start after contract baseline, redaction policy, and release checklist are stable."
    },
    {
      "id": "PGSTAT-P0-004",
      "order": 6,
      "priority": "P0",
      "workstream": "telemetry",
      "status": "planned",
      "title": "Add safe planner stats metadata contracts",
      "problem": "Planner statistics are needed for index/query reasoning, but can expose sensitive distribution metadata.",
      "requirements": [
        "Collect safe scalar metadata from pg_stats and extended stats without dumping sensitive values by default.",
        "Classify histogram/MCV fields before any AI/export use.",
        "Document unsupported-version and privilege behavior."
      ],
      "acceptance_criteria": [
        {
          "id": "AC1",
          "text": "Safe/blocked/redacted field policy is explicit.",
          "status": "pending"
        },
        {
          "id": "AC2",
          "text": "Retention and row-volume budget are documented.",
          "status": "pending"
        },
        {
          "id": "AC3",
          "text": "pgdbaagent evidence usage is documented before exposure.",
          "status": "pending"
        }
      ],
      "implementation_plan": [
        "Define allowed scalar stats.",
        "Exclude or redact raw value distributions unless explicitly approved.",
        "Implement only after field security policy is accepted."
      ],
      "verification_plan": [
        "security review of exported fields",
        "collector compile",
        "generated docs and docs-impact checks"
      ],
      "required_docs": [
        "docs/pgstat-data-source-dictionary.md",
        "docs/data-contract-registry.md",
        "docs/pgdbaagent-contracts.md",
        "docs/generated/project-status.md"
      ],
      "dependencies": [
        "PGSTAT-P0-003",
        "PGSTAT-P0-011"
      ],
      "customer_acceptance": {
        "required": true,
        "status": "pending",
        "evidence": ""
      },
      "verification_evidence": [],
      "next_action": "Start after catalog metadata and redaction policy."
    },
    {
      "id": "PGSTAT-P0-005",
      "order": 7,
      "priority": "P0",
      "workstream": "telemetry",
      "status": "planned",
      "title": "Add deploy/application event API",
      "problem": "Metric changes need deploy/event context to separate workload changes from database regressions.",
      "requirements": [
        "Create an API for application/deploy events.",
        "Events must be scoped to instance, database, service group, or application.",
        "Retention, audit, and export behavior must be documented."
      ],
      "acceptance_criteria": [
        {
          "id": "AC1",
          "text": "Events can be written through a documented API.",
          "status": "pending"
        },
        {
          "id": "AC2",
          "text": "Insights can correlate event windows later without schema redesign.",
          "status": "pending"
        },
        {
          "id": "AC3",
          "text": "Retention and audit paths are implemented.",
          "status": "pending"
        }
      ],
      "implementation_plan": [
        "Design event schema and API.",
        "Add UI/import path only after API contract is stable.",
        "Feed pgdbaagent evidence packages as contextual evidence."
      ],
      "verification_plan": [
        "api npx tsc --noEmit",
        "ui npx tsc --noEmit if UI changes",
        "generated docs and docs-impact checks"
      ],
      "required_docs": [
        "docs/data-contract-registry.md",
        "docs/agentic-dba-platform-architecture.md",
        "docs/generated/project-status.md"
      ],
      "dependencies": [
        "PGSTAT-P0-001",
        "PGSTAT-P0-002",
        "PGSTAT-P0-012"
      ],
      "customer_acceptance": {
        "required": true,
        "status": "pending",
        "evidence": ""
      },
      "verification_evidence": [],
      "next_action": "Design after contract baseline and release checklist."
    },
    {
      "id": "PGSTAT-P0-006",
      "order": 8,
      "priority": "P0",
      "workstream": "telemetry",
      "status": "planned",
      "title": "Add full lock graph snapshot",
      "problem": "Lock wait evidence needs blocker/waiter graph context, not just isolated wait rows.",
      "requirements": [
        "Capture waiters, blockers, lock modes, relation/database context, and query identity where safe.",
        "Represent lock graph snapshots in a way pgdbaagent can use for root-cause findings.",
        "Document data sensitivity and retention."
      ],
      "acceptance_criteria": [
        {
          "id": "AC1",
          "text": "Lock graph evidence package can identify root blocker candidates.",
          "status": "pending"
        },
        {
          "id": "AC2",
          "text": "Snapshot collection remains read-only and low overhead.",
          "status": "pending"
        },
        {
          "id": "AC3",
          "text": "UI/API consumers and pgdbaagent usage are mapped.",
          "status": "pending"
        }
      ],
      "implementation_plan": [
        "Define capability card and schema.",
        "Implement version-aware collector query.",
        "Expose API/UI only after contract is stable."
      ],
      "verification_plan": [
        "collector mvn compile",
        "api/ui type checks if changed",
        "generated docs and docs-impact checks"
      ],
      "required_docs": [
        "docs/data-contract-registry.md",
        "docs/pgdbaagent-contracts.md",
        "docs/generated/project-status.md"
      ],
      "dependencies": [
        "PGSTAT-P0-001",
        "PGSTAT-P0-002",
        "PGSTAT-P0-011"
      ],
      "customer_acceptance": {
        "required": true,
        "status": "pending",
        "evidence": ""
      },
      "verification_evidence": [],
      "next_action": "Design after contract baseline and redaction policy."
    },
    {
      "id": "PGSTAT-P0-017",
      "order": 9,
      "priority": "P0",
      "workstream": "telemetry",
      "status": "planned",
      "title": "Define OS/system metric ingestion contract",
      "problem": "pgstat must distinguish PostgreSQL bottlenecks from host CPU, memory, disk, filesystem, and network bottlenecks without adding load to the target PostgreSQL database.",
      "requirements": [
        "Define supported OS families and variants: Linux generic, RHEL/Rocky/Alma/CentOS/Fedora family, Ubuntu/Debian family, SUSE family, and Windows Server. Unsupported OS behavior must be explicit.",
        "Define source adapters: optional read-only OS user collection, optional node_exporter/windows_exporter scrape/import, optional Telegraf/Prometheus bridge, and manual/API ingestion.",
        "OS collection must not query or load the target PostgreSQL database. Only central pgstat storage may be written.",
        "Direct OS collection must use least privilege. Default design should work with a read-only OS user and command/source allowlist; no root/sudo requirement by default.",
        "Define host identity and OS change history so a machine can move from Windows to Linux, RHEL to Ubuntu, or similar without corrupting historical evidence.",
        "Define PostgreSQL instance to host/container binding: automatic evidence-based matching, explicit manual override, binding confidence, ambiguity handling, valid_from/valid_to history, and no AI high-confidence use when binding is missing or low-confidence.",
        "Define host service health observation for PostgreSQL-related services: PostgreSQL server, PgBouncer, Patroni, pgpool-II, HAProxy/Keepalived when tagged as PostgreSQL traffic components, and container/Kubernetes equivalents.",
        "Define a fast service heartbeat separate from heavier OS metric cadence so stopped, failed, unhealthy, or restart-looping PostgreSQL-related services can alert quickly without querying the target PostgreSQL database.",
        "Define minimum metrics: CPU/load/iowait/steal, memory/swap, disk IOPS/throughput/latency/utilization, filesystem bytes/inodes, network throughput/errors/drops, and optional PostgreSQL process CPU/memory where safely available.",
        "Define missing/stale/partial coverage behavior. Missing OS metrics must never be silently treated as zero or healthy evidence.",
        "Define schedule, retention, purge, partition, rollup, units, security, export, and AI evidence policy."
      ],
      "acceptance_criteria": [
        {
          "id": "AC1",
          "text": "Supported OS families, unsupported OS behavior, and OS change history model are documented.",
          "status": "pending"
        },
        {
          "id": "AC2",
          "text": "Collection source adapters are documented with target DB load impact explicitly stated as zero.",
          "status": "pending"
        },
        {
          "id": "AC3",
          "text": "Read-only OS user / least-privilege model is documented for direct collection.",
          "status": "pending"
        },
        {
          "id": "AC4",
          "text": "Instance-host/container binding model is documented with automatic evidence sources, manual override, confidence levels, ambiguity behavior, and historical validity ranges.",
          "status": "pending"
        },
        {
          "id": "AC5",
          "text": "PostgreSQL-related service health contract is documented, including service discovery sources, allowlist/tag model, status values, fast heartbeat cadence, alert transitions, and container/Kubernetes service health mapping.",
          "status": "pending"
        },
        {
          "id": "AC6",
          "text": "Minimum metric list includes units, cadence, retention, purge, partition, and rollup decisions.",
          "status": "pending"
        },
        {
          "id": "AC7",
          "text": "Missing/stale/partial metrics behavior is documented and blocks high-confidence findings when needed.",
          "status": "pending"
        },
        {
          "id": "AC8",
          "text": "pgdbaagent optional evidence usage is documented without making OS metrics silently mandatory.",
          "status": "pending"
        }
      ],
      "implementation_plan": [
        "Write the OS/system metric capability card and contract first.",
        "Choose source adapter priority: API/import first, exporter bridge second, direct read-only OS user third unless customer accepts host access design.",
        "Define dim.host_ref, host observation history, container observation history, instance_host_binding, metric naming, units, and retention before migration work.",
        "Define binding precedence: manual binding, explicit agent claim, container/Kubernetes label, listener/process match, cloud tag, then unbound/ambiguous.",
        "Define service observation as read-only state collection: systemd/Windows SCM/container runtime/Kubernetes status, no start/stop/restart actions, no shell execution by default.",
        "Define fast service heartbeat targets and thresholds separately from OS metric collection to support quick alerts for PostgreSQL/PgBouncer service failure.",
        "Document that OS collection must not add load to target PostgreSQL databases."
      ],
      "verification_plan": [
        "docs-impact check",
        "project-board validation",
        "manual review against pgstat-telemetry-completion-roadmap.md section 5.7",
        "security review of direct OS collection model"
      ],
      "required_docs": [
        "docs/pgstat-node-agent-requirements.md",
        "docs/pgstat-telemetry-completion-roadmap.md",
        "docs/pgstat-data-source-dictionary.md",
        "docs/data-contract-registry.md",
        "docs/project-execution-plan.md",
        "docs/generated/project-status.md"
      ],
      "dependencies": [
        "PGSTAT-P0-001",
        "PGSTAT-P0-002",
        "PGSTAT-P0-011",
        "PGSTAT-P0-012"
      ],
      "customer_acceptance": {
        "required": true,
        "status": "pending",
        "evidence": ""
      },
      "verification_evidence": [],
      "next_action": "Define OS/system metric contract after core field contracts, redaction policy, and release checklist."
    },
    {
      "id": "PGSTAT-P0-018",
      "order": 10,
      "priority": "P0",
      "workstream": "telemetry",
      "status": "planned",
      "title": "Implement OS/system metric ingestion storage and API",
      "problem": "After the OS/system metric contract is accepted, pgstat needs low-overhead storage and ingestion so host pressure can be correlated with PostgreSQL evidence.",
      "requirements": [
        "Implement host identity, OS observation history, and OS metric fact/snapshot storage according to the accepted contract.",
        "Implement PostgreSQL-related service inventory and service health snapshot storage according to the accepted contract.",
        "Implement API/import path first unless the accepted contract chooses another adapter.",
        "Implement retention and purge using the same policy model as existing database metrics where appropriate.",
        "Partition high-volume OS metric tables by timestamp where needed.",
        "Record collection coverage status so missing/stale/partial OS data is visible globally.",
        "Record service status changes and fast-alert eligible transitions for PostgreSQL-related services without letting the agent manage services.",
        "Do not add target PostgreSQL database load for OS metric collection."
      ],
      "acceptance_criteria": [
        {
          "id": "AC1",
          "text": "OS metric storage exists with documented partition, retention, purge, and rollup behavior.",
          "status": "pending"
        },
        {
          "id": "AC2",
          "text": "API/import path accepts documented metrics with units, timestamps, host identity, and source adapter metadata.",
          "status": "pending"
        },
        {
          "id": "AC3",
          "text": "Generated inventory and field contracts include OS metric tables and consumers.",
          "status": "pending"
        },
        {
          "id": "AC4",
          "text": "Coverage state records missing/stale/partial OS metric collection.",
          "status": "pending"
        },
        {
          "id": "AC5",
          "text": "Service inventory and service health snapshots exist for PostgreSQL-related services with documented retention, purge, and alert transition behavior.",
          "status": "pending"
        },
        {
          "id": "AC6",
          "text": "Implementation creates no target PostgreSQL query/load for OS metrics.",
          "status": "pending"
        }
      ],
      "implementation_plan": [
        "Add migrations after contract acceptance.",
        "Add API ingestion/import route and validation.",
        "Add purge/retention/partition wiring.",
        "Add generated docs and optional read-only UI/API exposure.",
        "Add sample ingestion smoke test data."
      ],
      "verification_plan": [
        "api npx tsc --noEmit",
        "migration review",
        "generated docs and docs-impact checks",
        "manual ingestion smoke test with sample Linux and Windows-shaped metrics",
        "verify OS metrics path does not query target PostgreSQL databases"
      ],
      "required_docs": [
        "docs/pgstat-node-agent-requirements.md",
        "docs/pgstat-telemetry-completion-roadmap.md",
        "docs/data-contract-registry.md",
        "docs/generated/project-status.md"
      ],
      "dependencies": [
        "PGSTAT-P0-017"
      ],
      "customer_acceptance": {
        "required": true,
        "status": "pending",
        "evidence": ""
      },
      "verification_evidence": [],
      "next_action": "Implement only after OS/system metric contract is accepted."
    },
    {
      "id": "PGSTAT-P0-019",
      "order": 11,
      "priority": "P0",
      "workstream": "telemetry",
      "status": "planned",
      "title": "Implement pgstat-node-agent V1",
      "problem": "pgstat needs an optional, low-overhead host agent to collect OS metrics, host identity, container context, and PostgreSQL-related service health without querying or loading the monitored PostgreSQL database.",
      "requirements": [
        "Implement the official pgstat-node-agent path defined in docs/pgstat-node-agent-requirements.md.",
        "Agent must not connect to the monitored PostgreSQL database, must not require DB credentials, and must not run diagnostic SQL.",
        "Agent must collect host identity, OS observation, OS metrics, coverage state, and PostgreSQL-related service health.",
        "Agent must support Linux host service deployment, Windows service deployment, and container/DaemonSet-style deployment according to the accepted contract.",
        "Agent must push to central pgstat API using scoped ingest token and must mask secrets in logs.",
        "Agent must support fast service heartbeat separately from heavier OS metric cadence.",
        "Agent must not manage services. No start, stop, restart, or arbitrary shell execution in V1.",
        "Docker socket access must be disabled by default and documented as optional high-privilege enrichment if ever enabled.",
        "Missing, stale, partial, or unsupported metric coverage must be reported explicitly."
      ],
      "acceptance_criteria": [
        {
          "id": "AC1",
          "text": "Agent produces valid host identity, OS metric, service heartbeat, and coverage payloads accepted by central pgstat API.",
          "status": "pending"
        },
        {
          "id": "AC2",
          "text": "Agent runs without target PostgreSQL credentials and no target PostgreSQL query/load is introduced.",
          "status": "pending"
        },
        {
          "id": "AC3",
          "text": "Linux service, Windows service, and container deployment paths are documented and smoke-tested or explicitly phased.",
          "status": "pending"
        },
        {
          "id": "AC4",
          "text": "PostgreSQL/PgBouncer/Patroni-style service health transitions are reported with fast heartbeat and central alert eligibility metadata.",
          "status": "pending"
        },
        {
          "id": "AC5",
          "text": "Agent security model is verified: scoped token, secret masking, least privilege, no service management, no arbitrary command execution.",
          "status": "pending"
        },
        {
          "id": "AC6",
          "text": "Coverage state distinguishes missing, stale, partial, unsupported, and healthy data sources.",
          "status": "pending"
        }
      ],
      "implementation_plan": [
        "Create agent package after OS contract and central ingestion API are accepted.",
        "Implement config, auth, TLS transport, bounded retry spool, and self-health first.",
        "Implement host identity and coverage payloads before high-volume metrics.",
        "Implement OS metrics in small metric-family increments with source and unit contracts.",
        "Implement service health heartbeat and central alert transition payloads.",
        "Add packaging/deployment docs for Linux, Windows, and container modes."
      ],
      "verification_plan": [
        "agent build/test once package exists",
        "central API ingestion smoke test",
        "Linux-shaped sample payload smoke test",
        "Windows-shaped sample payload smoke test",
        "service heartbeat transition smoke test",
        "verify no monitored PostgreSQL connection or query path exists",
        "security review of token, logs, filesystem permissions, and optional Docker access"
      ],
      "required_docs": [
        "docs/pgstat-node-agent-requirements.md",
        "docs/pgstat-telemetry-completion-roadmap.md",
        "docs/data-contract-registry.md",
        "docs/project-execution-plan.md",
        "docs/generated/project-status.md"
      ],
      "dependencies": [
        "PGSTAT-P0-017",
        "PGSTAT-P0-018"
      ],
      "customer_acceptance": {
        "required": true,
        "status": "pending",
        "evidence": ""
      },
      "verification_evidence": [],
      "next_action": "Implement after OS/system metric contract and central ingestion storage/API are accepted."
    },
    {
      "id": "PGSTAT-P0-007",
      "order": 12,
      "priority": "P0",
      "workstream": "pgdbaagent",
      "status": "planned",
      "title": "Define evidence package schemas v1",
      "problem": "pgdbaagent cannot reason safely until evidence packages are stable and field-backed.",
      "requirements": [
        "Define JSON schemas for temp_spill_query_v1, wal_spike_query_v1, cache_miss_query_v1, vacuum_lag_table_v1, and lock_wait_v1.",
        "Each evidence field must reference a source field contract ID.",
        "Missing context and sensitivity policy must be explicit."
      ],
      "acceptance_criteria": [
        {
          "id": "AC1",
          "text": "Five evidence package schemas are documented.",
          "status": "pending"
        },
        {
          "id": "AC2",
          "text": "Each package lists required and optional field contract IDs.",
          "status": "pending"
        },
        {
          "id": "AC3",
          "text": "AI input boundaries are documented.",
          "status": "pending"
        }
      ],
      "implementation_plan": [
        "Start with docs/schema only.",
        "Use promoted contracts from P0-001/P0-002.",
        "Implement builders only after schema review."
      ],
      "verification_plan": [
        "docs-impact check",
        "schema consistency review against generated field contracts"
      ],
      "required_docs": [
        "docs/pgdbaagent-contracts.md",
        "docs/data-contract-registry.md",
        "docs/generated/project-status.md"
      ],
      "dependencies": [
        "PGSTAT-P0-001",
        "PGSTAT-P0-002",
        "PGSTAT-P0-003",
        "PGSTAT-P0-004",
        "PGSTAT-P0-005",
        "PGSTAT-P0-006",
        "PGSTAT-P0-011",
        "PGSTAT-P0-017",
        "PGSTAT-P0-018"
      ],
      "customer_acceptance": {
        "required": true,
        "status": "pending",
        "evidence": ""
      },
      "verification_evidence": [],
      "next_action": "Define evidence schemas after pgstat evidence inputs, OS metrics, and redaction policy are stable."
    },
    {
      "id": "PGSTAT-P0-010",
      "order": 13,
      "priority": "P0",
      "workstream": "validation",
      "status": "planned",
      "title": "Define validation target and result contracts",
      "problem": "Clone/staging validation needs structured target, job, and result contracts before pgdbaagent can compare before/after evidence.",
      "requirements": [
        "Define user-provided validation target registry.",
        "Define EXPLAIN and optional EXPLAIN ANALYZE capture contracts.",
        "Define retention, audit, timeout, and concurrency policy."
      ],
      "acceptance_criteria": [
        {
          "id": "AC1",
          "text": "Validation target schema is documented.",
          "status": "pending"
        },
        {
          "id": "AC2",
          "text": "Validation result schema is documented.",
          "status": "pending"
        },
        {
          "id": "AC3",
          "text": "No automatic clone lifecycle is implied.",
          "status": "pending"
        }
      ],
      "implementation_plan": [
        "Design contracts first.",
        "Implement storage/API only after customer accepts boundary.",
        "Keep production disconnected from validation execution."
      ],
      "verification_plan": [
        "docs-impact check",
        "security review before implementation"
      ],
      "required_docs": [
        "docs/agentic-dba-platform-architecture.md",
        "docs/pgdbaagent-contracts.md",
        "docs/data-contract-registry.md",
        "docs/generated/project-status.md"
      ],
      "dependencies": [
        "PGSTAT-P0-007",
        "PGSTAT-P0-011",
        "PGSTAT-P0-012"
      ],
      "customer_acceptance": {
        "required": true,
        "status": "pending",
        "evidence": ""
      },
      "verification_evidence": [],
      "next_action": "Define validation contracts after evidence schemas, redaction policy, and release discipline."
    },
    {
      "id": "PGSTAT-P0-008",
      "order": 14,
      "priority": "P0",
      "workstream": "pgdbaagent",
      "status": "planned",
      "title": "Define signal/finding/recommendation registry",
      "problem": "Reasoning is currently scattered between UI badges and design docs.",
      "requirements": [
        "Create a global deterministic rule registry.",
        "Separate UI hints from durable pgdbaagent signals.",
        "Define lifecycle and status transitions for findings and recommendations."
      ],
      "acceptance_criteria": [
        {
          "id": "AC1",
          "text": "Signal, finding, and recommendation schemas are stable enough for v1 implementation.",
          "status": "pending"
        },
        {
          "id": "AC2",
          "text": "At least the first Temp/WAL/Cache/Vacuum/Lock rules are listed.",
          "status": "pending"
        },
        {
          "id": "AC3",
          "text": "Risk and confidence inputs are explicit.",
          "status": "pending"
        }
      ],
      "implementation_plan": [
        "Document registry first.",
        "Map existing UI badge heuristics to candidate signals where appropriate.",
        "Implement code after schema acceptance."
      ],
      "verification_plan": [
        "docs-impact check",
        "cross-check with pgdbaagent contracts"
      ],
      "required_docs": [
        "docs/pgdbaagent-contracts.md",
        "docs/agentic-dba-platform-architecture.md",
        "docs/generated/project-status.md"
      ],
      "dependencies": [
        "PGSTAT-P0-007",
        "PGSTAT-P0-010"
      ],
      "customer_acceptance": {
        "required": true,
        "status": "pending",
        "evidence": ""
      },
      "verification_evidence": [],
      "next_action": "Start after evidence schemas and validation contracts."
    },
    {
      "id": "PGSTAT-P0-013",
      "order": 15,
      "priority": "P0",
      "workstream": "pgdbaagent",
      "status": "planned",
      "title": "Implement evidence package builders v1",
      "problem": "Schemas alone are not enough; pgdbaagent needs deterministic builders that assemble field-backed evidence packages from pgstat APIs/contracts.",
      "requirements": [
        "Implement builders for temp_spill_query_v1, wal_spike_query_v1, cache_miss_query_v1, vacuum_lag_table_v1, and lock_wait_v1.",
        "Each builder must include source field IDs, freshness, missing context, and sensitivity metadata.",
        "Builders must not read undocumented raw fields."
      ],
      "acceptance_criteria": [
        {
          "id": "AC1",
          "text": "Five evidence builders produce schema-valid packages from documented pgstat evidence.",
          "status": "pending"
        },
        {
          "id": "AC2",
          "text": "Missing context and sensitivity metadata are populated.",
          "status": "pending"
        },
        {
          "id": "AC3",
          "text": "Tests cover empty/missing/partial evidence cases.",
          "status": "pending"
        }
      ],
      "implementation_plan": [
        "Create evidence builder module in pgdbaagent or shared package.",
        "Map builders to promoted field contracts.",
        "Add fixtures for representative evidence packages."
      ],
      "verification_plan": [
        "pgdbaagent tests when project exists locally",
        "schema validation tests",
        "docs-impact check"
      ],
      "required_docs": [
        "docs/pgdbaagent-contracts.md",
        "docs/data-contract-registry.md",
        "docs/generated/project-status.md"
      ],
      "dependencies": [
        "PGSTAT-P0-007",
        "PGSTAT-P0-008"
      ],
      "customer_acceptance": {
        "required": true,
        "status": "pending",
        "evidence": ""
      },
      "verification_evidence": [],
      "next_action": "Implement after schemas and registry are accepted."
    },
    {
      "id": "PGSTAT-P0-014",
      "order": 16,
      "priority": "P0",
      "workstream": "pgdbaagent",
      "status": "planned",
      "title": "Implement deterministic signal/finding engine v1",
      "problem": "Reasoning must be reusable and deterministic before AI explanation is layered on top.",
      "requirements": [
        "Implement global signal rules for Temp Spill, WAL Spike, Cache Hit, Vacuum Lag, and Lock Wait.",
        "Generate findings from signals with severity, confidence, impact, and missing context.",
        "Keep UI-only badges separate from durable findings unless backed by rules."
      ],
      "acceptance_criteria": [
        {
          "id": "AC1",
          "text": "First rule registry runs against evidence packages.",
          "status": "pending"
        },
        {
          "id": "AC2",
          "text": "Findings include source evidence IDs and rule IDs.",
          "status": "pending"
        },
        {
          "id": "AC3",
          "text": "Tests cover rule thresholds and missing evidence behavior.",
          "status": "pending"
        }
      ],
      "implementation_plan": [
        "Create deterministic rule registry.",
        "Implement signal generation.",
        "Implement finding generation and lifecycle seed states."
      ],
      "verification_plan": [
        "unit tests",
        "schema validation tests",
        "docs-impact check"
      ],
      "required_docs": [
        "docs/pgdbaagent-contracts.md",
        "docs/agentic-dba-platform-architecture.md",
        "docs/generated/project-status.md"
      ],
      "dependencies": [
        "PGSTAT-P0-013"
      ],
      "customer_acceptance": {
        "required": true,
        "status": "pending",
        "evidence": ""
      },
      "verification_evidence": [],
      "next_action": "Implement after evidence builders v1."
    },
    {
      "id": "PGSTAT-P0-015",
      "order": 17,
      "priority": "P0",
      "workstream": "pgdbaagent",
      "status": "planned",
      "title": "Implement pgdbaagent Advice V1 and AI explanation boundary",
      "problem": "AI explanation should summarize structured findings and recommendations, not invent advice from raw data.",
      "requirements": [
        "Create recommendation candidates from deterministic findings.",
        "Prepare AI context only from approved evidence and redacted fields.",
        "Generate DBA explanation, action plan, rollback plan, and observation plan."
      ],
      "acceptance_criteria": [
        {
          "id": "AC1",
          "text": "Advice output separates evidence, inference, recommendation, risk, confidence, and missing context.",
          "status": "pending"
        },
        {
          "id": "AC2",
          "text": "AI context respects redaction policy.",
          "status": "pending"
        },
        {
          "id": "AC3",
          "text": "No production execution path exists.",
          "status": "pending"
        }
      ],
      "implementation_plan": [
        "Implement recommendation candidate generation.",
        "Implement AI context builder.",
        "Implement explanation output contract."
      ],
      "verification_plan": [
        "unit tests",
        "redaction tests",
        "manual review with sample evidence packages"
      ],
      "required_docs": [
        "docs/pgdbaagent-contracts.md",
        "docs/agentic-dba-platform-architecture.md",
        "docs/generated/project-status.md"
      ],
      "dependencies": [
        "PGSTAT-P0-014"
      ],
      "customer_acceptance": {
        "required": true,
        "status": "pending",
        "evidence": ""
      },
      "verification_evidence": [],
      "next_action": "Start after deterministic finding engine is stable."
    },
    {
      "id": "PGSTAT-P0-016",
      "order": 18,
      "priority": "P0",
      "workstream": "validation",
      "status": "planned",
      "title": "Implement validation target/result storage and API",
      "problem": "Validation contracts need storage and API before clone/staging before-after evidence can be attached to recommendations.",
      "requirements": [
        "Implement user-provided validation target registry.",
        "Implement validation result storage for EXPLAIN and optional EXPLAIN ANALYZE outputs.",
        "Include audit, timeout, retention, and sensitivity controls."
      ],
      "acceptance_criteria": [
        {
          "id": "AC1",
          "text": "Validation targets can be registered without automatic clone provisioning.",
          "status": "pending"
        },
        {
          "id": "AC2",
          "text": "Validation results can be stored and linked to recommendations.",
          "status": "pending"
        },
        {
          "id": "AC3",
          "text": "Retention/audit/security behavior is documented and implemented.",
          "status": "pending"
        }
      ],
      "implementation_plan": [
        "Add migrations for validation target/result tables.",
        "Add API routes and service layer.",
        "Wire retention and generated contracts."
      ],
      "verification_plan": [
        "api npx tsc --noEmit",
        "migration review",
        "docs-impact check"
      ],
      "required_docs": [
        "docs/data-contract-registry.md",
        "docs/pgdbaagent-contracts.md",
        "docs/generated/project-status.md"
      ],
      "dependencies": [
        "PGSTAT-P0-010"
      ],
      "customer_acceptance": {
        "required": true,
        "status": "pending",
        "evidence": ""
      },
      "verification_evidence": [],
      "next_action": "Implement after validation contracts are accepted."
    },
    {
      "id": "PGSTAT-P1-004",
      "order": 19,
      "priority": "P1",
      "workstream": "validation",
      "status": "planned",
      "title": "Implement user-provided clone validation execution jobs",
      "problem": "After target/result storage exists, pgdbaagent needs controlled EXPLAIN and before-after validation jobs against user-provided clone/staging databases.",
      "requirements": [
        "Run controlled EXPLAIN and optional EXPLAIN ANALYZE against user-provided clone/staging targets.",
        "Apply timeout, concurrency, approval, and audit policies.",
        "Store before/after evidence without touching production."
      ],
      "acceptance_criteria": [
        {
          "id": "AC1",
          "text": "Validation jobs run only against registered non-production targets.",
          "status": "pending"
        },
        {
          "id": "AC2",
          "text": "Before/after evidence is linked to recommendation candidates.",
          "status": "pending"
        },
        {
          "id": "AC3",
          "text": "Timeout and audit behavior are tested.",
          "status": "pending"
        }
      ],
      "implementation_plan": [
        "Implement job queue and worker policy.",
        "Implement EXPLAIN capture.",
        "Add before/after comparison outputs."
      ],
      "verification_plan": [
        "integration tests against a local clone/staging DB",
        "security review",
        "docs-impact check"
      ],
      "required_docs": [
        "docs/agentic-dba-platform-architecture.md",
        "docs/pgdbaagent-contracts.md",
        "docs/generated/project-status.md"
      ],
      "dependencies": [
        "PGSTAT-P0-014",
        "PGSTAT-P0-016"
      ],
      "customer_acceptance": {
        "required": true,
        "status": "pending",
        "evidence": ""
      },
      "verification_evidence": [],
      "next_action": "Start after validation storage/API and deterministic reasoning are stable."
    },
    {
      "id": "PGSTAT-P1-001",
      "order": 20,
      "priority": "P1",
      "workstream": "ui",
      "status": "planned",
      "title": "Add project status view or docs link in admin UI",
      "problem": "Operators cannot currently see data coverage and project contract status from the product UI.",
      "requirements": [
        "Expose project/contract status safely, likely as an admin link or read-only page.",
        "Avoid leaking sensitive internal paths in public contexts.",
        "Use generated project status as the source."
      ],
      "acceptance_criteria": [
        {
          "id": "AC1",
          "text": "Admin UI can surface project/contract status or link to docs.",
          "status": "pending"
        },
        {
          "id": "AC2",
          "text": "No sensitive data is exposed.",
          "status": "pending"
        }
      ],
      "implementation_plan": [
        "Decide whether this belongs in product UI or docs-only first.",
        "Implement minimal read-only surface if approved."
      ],
      "verification_plan": [
        "ui npx tsc --noEmit",
        "ui npm run build if UI changes"
      ],
      "required_docs": [
        "docs/project-execution-plan.md",
        "docs/generated/project-status.md"
      ],
      "dependencies": [
        "PGSTAT-P0-009",
        "PGSTAT-P0-012"
      ],
      "customer_acceptance": {
        "required": true,
        "status": "pending",
        "evidence": ""
      },
      "verification_evidence": [],
      "next_action": "Defer until governance and release checklist are stable."
    },
    {
      "id": "PGSTAT-P0-021",
      "order": 21,
      "priority": "P0",
      "workstream": "operations",
      "status": "done",
      "title": "Validate live aggregate partition gap remediation",
      "problem": "Live schema validation found mixed timezone partition bounds in existing agg.pgss_hourly and agg.pg_table_stat_hourly partitions. The remediation was deployed to live and verified by Opus.",
      "requirements": [
        "Record that the live partition remediation was deployed.",
        "Record that Opus verified the live remediation.",
        "Close the P0 operations item so the active project focus can return to field-level contracts."
      ],
      "acceptance_criteria": [
        {
          "id": "AC1",
          "text": "Live remediation for the aggregate monthly partition gap was deployed.",
          "status": "done"
        },
        {
          "id": "AC2",
          "text": "Opus verified the live remediation.",
          "status": "done"
        },
        {
          "id": "AC3",
          "text": "Separate runbook is not required for this P0 closure because the live fix was handled and verified.",
          "status": "not_required"
        }
      ],
      "implementation_plan": [
        "Record customer live deployment confirmation.",
        "Record Opus verification confirmation.",
        "Regenerate project status and run docs-impact checks."
      ],
      "verification_plan": [
        "node scripts/check-project-board.mjs",
        "node scripts/generate-project-status.mjs",
        "node scripts/check-doc-impact.mjs --staged"
      ],
      "required_docs": [
        "docs/data-contract-registry.md",
        "docs/project-execution-plan.md",
        "docs/generated/project-status.md"
      ],
      "dependencies": [
        "PGSTAT-P0-001"
      ],
      "customer_acceptance": {
        "required": true,
        "status": "accepted",
        "evidence": "Customer reported the partition remediation was deployed to live and Opus verified it."
      },
      "verification_evidence": [
        "Customer reported live partition remediation was deployed and Opus verified it on 2026-07-14.",
        "Remediation was implemented outside this docs branch on fix/partition-monthly-timezone and validated before production use."
      ],
      "completed_at": "2026-07-14",
      "next_action": "Closed; continue with PGSTAT-P0-002."
    },
    {
      "id": "PGSTAT-P1-002",
      "order": 22,
      "priority": "P1",
      "workstream": "ui",
      "status": "done",
      "title": "Table freeze insights (delivered via .kiro spec)",
      "problem": "Per-table freeze/wraparound visibility was needed in Insights. The feature was specified and delivered through the .kiro spec workflow (.kiro/specs/table-freeze-insights) before this board existed; this task records it so the board reflects shipped work.",
      "requirements": [
        "Record the shipped table freeze insights feature on the board.",
        "Link the .kiro spec as the source specification."
      ],
      "acceptance_criteria": [
        {
          "id": "AC1",
          "text": "fact.pg_table_freeze_snapshot storage, nightly collection, retention/schedule wiring, and the Freeze insights tab are implemented.",
          "status": "done"
        },
        {
          "id": "AC2",
          "text": "All 11 tasks in .kiro/specs/table-freeze-insights/tasks.md are checked off.",
          "status": "done"
        }
      ],
      "implementation_plan": [
        "Delivered before the board existed; see .kiro/specs/table-freeze-insights for design, requirements, and tasks."
      ],
      "verification_plan": [
        "node scripts/check-project-board.mjs",
        "node scripts/generate-project-status.mjs"
      ],
      "required_docs": [
        "docs/project-execution-plan.md",
        "docs/generated/project-status.md"
      ],
      "dependencies": [],
      "customer_acceptance": {
        "required": true,
        "status": "accepted",
        "evidence": "Feature shipped in commit aadf2b4 (2026-06-11) and is in production use; customer asked on 2026-07-17 to record the .kiro-delivered work on the board."
      },
      "verification_evidence": [
        "Migration V078__table_freeze_snapshot.sql, NightlySnapshotCollector wiring, and the Freeze tab exist in the codebase.",
        "All 11 tasks in .kiro/specs/table-freeze-insights/tasks.md are marked complete."
      ],
      "completed_at": "2026-06-11",
      "next_action": "Closed; recorded retroactively from the .kiro spec workflow."
    },
    {
      "id": "PGSTAT-P1-003",
      "order": 23,
      "priority": "P1",
      "workstream": "ui",
      "status": "done",
      "title": "Vacuum lag split activity chart (delivered via .kiro spec)",
      "problem": "The Vacuum Lag insights activity trend mixed manual and autovacuum activity. The feature splitting it into manual/auto vacuum and analyze series was specified and delivered through the .kiro spec workflow (.kiro/specs/vacuum-lag-split-activity) before this board existed; this task records it so the board reflects shipped work.",
      "requirements": [
        "Record the shipped vacuum lag split activity feature on the board.",
        "Link the .kiro spec as the source specification."
      ],
      "acceptance_criteria": [
        {
          "id": "AC1",
          "text": "The insights API returns vacuum_count_manual/auto and analyze_count_manual/auto and the UI renders four separate trend lines.",
          "status": "done"
        },
        {
          "id": "AC2",
          "text": "All 7 tasks in .kiro/specs/vacuum-lag-split-activity/tasks.md are checked off.",
          "status": "done"
        }
      ],
      "implementation_plan": [
        "Delivered before the board existed; see .kiro/specs/vacuum-lag-split-activity for design, requirements, and tasks."
      ],
      "verification_plan": [
        "node scripts/check-project-board.mjs",
        "node scripts/generate-project-status.mjs"
      ],
      "required_docs": [
        "docs/project-execution-plan.md",
        "docs/generated/project-status.md"
      ],
      "dependencies": [],
      "customer_acceptance": {
        "required": true,
        "status": "accepted",
        "evidence": "Feature shipped in commit aa333fa (2026-06-02) and is in production use; customer asked on 2026-07-17 to record the .kiro-delivered work on the board."
      },
      "verification_evidence": [
        "api/src/routes/insights.ts exposes split vacuum/analyze counters and ui/src/pages/Insights.tsx renders four separate lines matching the spec.",
        "All 7 tasks in .kiro/specs/vacuum-lag-split-activity/tasks.md are marked complete."
      ],
      "completed_at": "2026-06-02",
      "next_action": "Closed; recorded retroactively from the .kiro spec workflow."
    },
    {
      "id": "PGSTAT-P0-020",
      "order": 24,
      "priority": "P0",
      "workstream": "governance",
      "status": "done",
      "title": "Define project test strategy",
      "problem": "Governance Section 9 listed testing gates but there was no project-wide test strategy: no honest coverage baseline, no per-layer commands, no fixture locations, no gap roadmap. Enterprise SDLC requires testing to be a defined process, not an aspiration.",
      "requirements": [
        "Document the honest current test baseline per layer (collector, API, UI, migrations, docs tooling).",
        "Define per-layer requirements and commands for changes to count as done.",
        "Define fixture locations and the integration environment.",
        "Map known gaps to board tasks or explicit adoption triggers."
      ],
      "acceptance_criteria": [
        {
          "id": "AC1",
          "text": "docs/test-strategy.md exists with baseline, per-layer gates, fixtures, integration environment, and gap roadmap.",
          "status": "done"
        },
        {
          "id": "AC2",
          "text": "Governance Section 9 references the test strategy.",
          "status": "done"
        }
      ],
      "implementation_plan": [
        "Inventory existing tests (4 collector JUnit classes; API/UI type-check only).",
        "Write docs/test-strategy.md and wire it into governance."
      ],
      "verification_plan": [
        "node scripts/check-project-board.mjs",
        "node scripts/check-doc-impact.mjs --staged"
      ],
      "required_docs": [
        "docs/test-strategy.md",
        "docs/platform-governance-and-sdlc.md",
        "docs/generated/project-status.md"
      ],
      "dependencies": [],
      "customer_acceptance": {
        "required": true,
        "status": "accepted",
        "evidence": "Customer instructed on 2026-07-17 to complete the missing SDLC items; the test strategy was delivered in the same session."
      },
      "verification_evidence": [
        "docs/test-strategy.md created; baseline verified against the actual repo (collector/src/test has 4 test classes, api and ui have no runtime tests).",
        "Linked from docs/platform-governance-and-sdlc.md Section 9."
      ],
      "completed_at": "2026-07-17",
      "next_action": "Closed; API vitest and UI render tests are adopted with the first qualifying PR per the strategy."
    },
    {
      "id": "PGSTAT-P1-005",
      "order": 25,
      "priority": "P1",
      "workstream": "governance",
      "status": "done",
      "title": "Adopt ADR practice with template and founding ADR",
      "problem": "Governance Section 12 required ADRs but there was no ADR directory, template, or index; decisions lived only in the master document's decision-log table, which does not scale.",
      "requirements": [
        "Create docs/adr/ with a template and index.",
        "Backfill the founding product-family decisions as ADR-0001."
      ],
      "acceptance_criteria": [
        {
          "id": "AC1",
          "text": "docs/adr/ contains README index, ADR-template.md, and ADR-0001 recording the product boundary and collection rules.",
          "status": "done"
        },
        {
          "id": "AC2",
          "text": "Governance Section 12 references the ADR directory.",
          "status": "done"
        }
      ],
      "implementation_plan": [
        "Create docs/adr/README.md, ADR-template.md, ADR-0001-product-family-baseline.md."
      ],
      "verification_plan": [
        "node scripts/check-project-board.mjs",
        "node scripts/check-doc-impact.mjs --staged"
      ],
      "required_docs": [
        "docs/adr/README.md",
        "docs/platform-governance-and-sdlc.md",
        "docs/generated/project-status.md"
      ],
      "dependencies": [],
      "customer_acceptance": {
        "required": true,
        "status": "accepted",
        "evidence": "Customer instructed on 2026-07-17 to complete the missing SDLC items; the ADR practice was delivered in the same session."
      },
      "verification_evidence": [
        "docs/adr/ created with README index, template, and ADR-0001 backfilling the 2026-07-13 boundary/collection decisions.",
        "Linked from docs/platform-governance-and-sdlc.md Section 12."
      ],
      "completed_at": "2026-07-17",
      "next_action": "Closed; future boundary/contract/AI/validation decisions get a numbered ADR."
    },
    {
      "id": "PGSTAT-P1-006",
      "order": 26,
      "priority": "P1",
      "workstream": "governance",
      "status": "done",
      "title": "Enforce quality gates locally (CI replacement, no GitHub Actions)",
      "problem": "GitHub Actions will not be used (customer decision, 2026-07-17; ADR-0002). Quality gates for builds and tests were manual: only documentation checks ran in hooks, so collector tests and api/ui type checks could be skipped silently.",
      "requirements": [
        "Enforce layer gates (collector mvn test, api tsc, ui tsc+eslint) at push time, scoped to changed layers.",
        "Provide a full unconditional run command.",
        "Document the decision as an ADR and update test strategy CI notes."
      ],
      "acceptance_criteria": [
        {
          "id": "AC1",
          "text": "pre-push runs scripts/check-quality-gates.mjs with layer detection over the pushed range.",
          "status": "done"
        },
        {
          "id": "AC2",
          "text": "make verify runs board check, doc impact, and all layer gates unconditionally.",
          "status": "done"
        },
        {
          "id": "AC3",
          "text": "ADR-0002 records the no-GitHub-Actions decision; test strategy and execution plan CI notes updated.",
          "status": "done"
        }
      ],
      "implementation_plan": [
        "Add scripts/check-quality-gates.mjs (layer detection, toolchain check, PGSTAT_SKIP_GATES escape hatch).",
        "Wire into .githooks/pre-push and add Makefile verify target.",
        "Write ADR-0002 and update CI notes."
      ],
      "verification_plan": [
        "Run the script with no changes (skips), with an api change (runs tsc), and via --all."
      ],
      "required_docs": [
        "docs/adr/ADR-0002-local-quality-gates-no-github-actions.md",
        "docs/test-strategy.md",
        "docs/generated/project-status.md"
      ],
      "dependencies": [],
      "customer_acceptance": {
        "required": true,
        "status": "accepted",
        "evidence": "Customer decided on 2026-07-17 that GitHub Actions will not be used and instructed solving CI locally; delivered in the same session."
      },
      "verification_evidence": [
        "Verified: no-change run skips gates; a test api/src edit triggered tsc --noEmit which passed; Maven 3.9.6 confirmed on PATH for the collector gate.",
        "scripts/check-quality-gates.mjs wired into .githooks/pre-push; make verify added; ADR-0002 written."
      ],
      "completed_at": "2026-07-17",
      "next_action": "Closed; revisit ADR-0002 if the team grows beyond one developer."
    },
    {
      "id": "PGSTAT-P0-022",
      "order": 28,
      "priority": "P0",
      "workstream": "operations",
      "status": "done",
      "title": "Fix V067 snapshot tables missing from PartitionManager and PurgeEvaluator",
      "problem": "V067 (deployed 2026-05-17) created 7 daily-partitioned fact tables (pg_wal_receiver_snapshot, pg_progress_vacuum/analyze/create_index/basebackup/copy/cluster_snapshot) with only 30 days of partitions created inline. None of them are in PartitionManager.DAILY_FACT_TABLES or PurgeEvaluator.SNAPSHOT_FACT_TABLES (both still list only the legacy fact.pg_progress_snapshot). Partitions ran out around 2026-06-16, so collector inserts into these tables have likely been failing for about a month, and the tables have no purge path. This is a live production bug and may jump the queue per the backlog rules.",
      "requirements": [
        "Verify live impact: partition existence after 2026-06-16 and collector error logs for these 7 tables.",
        "Add the 7 tables to PartitionManager.DAILY_FACT_TABLES so future partitions are created.",
        "Add the 7 tables to PurgeEvaluator.SNAPSHOT_FACT_TABLES (hourly/snapshot retention class).",
        "Backfill missing partitions from 2026-06-16 to now if data collection should resume seamlessly.",
        "Confirm whether ~1 month of lost snapshot data needs any remediation note in contracts."
      ],
      "acceptance_criteria": [
        {
          "id": "AC1",
          "text": "PartitionManager creates daily partitions for all 7 V067 tables.",
          "status": "done"
        },
        {
          "id": "AC2",
          "text": "PurgeEvaluator purges the 7 tables under the snapshot retention policy.",
          "status": "done"
        },
        {
          "id": "AC3",
          "text": "Live verification confirms inserts succeed again and the data gap window is documented.",
          "status": "done"
        }
      ],
      "implementation_plan": [
        "Add table names to the two arrays in PartitionManager and PurgeEvaluator.",
        "Deploy; PartitionManager run creates missing partitions (verify it backfills current date).",
        "Record the data gap window in the data source dictionary / contracts."
      ],
      "verification_plan": [
        "mvn test",
        "Live: select count(*) from new partitions after a collection cycle; collector logs clean."
      ],
      "required_docs": [
        "docs/pgstat-data-source-dictionary.md",
        "docs/generated/data-lifecycle-matrix.md",
        "docs/generated/project-status.md"
      ],
      "dependencies": [],
      "customer_acceptance": {
        "required": true,
        "status": "accepted",
        "evidence": "Customer deployed the merged main (0b339d6) on 2026-07-17 and shared live verification output confirming the fix works."
      },
      "verification_evidence": [
        "Live verified by customer on 2026-07-17: last partitions were *_20260616 for all 7 tables; max(sample_ts) stopped at 2026-06-16 (progress_vacuum 1514 rows, progress_analyze 397 rows, wal_receiver 0 rows); collector logs showed repeated 'no partition of relation ... found for row' errors.",
        "Fix applied 2026-07-17: the 7 tables added to PartitionManager.DAILY_FACT_TABLES and PurgeEvaluator.SNAPSHOT_FACT_TABLES; mvn test passed; merged to main (0b339d6).",
        "Post-deploy live verification 2026-07-17: collector startup logged 'Partition olusturuldu' for the V067 tables and max partition is now pg_progress_vacuum_snapshot_20260731 (today + 14 days).",
        "Permanent data gap 2026-06-16..2026-07-17 recorded in the data source dictionary for WAL receiver and progress detail families."
      ],
      "completed_at": "2026-07-17",
      "next_action": "Closed; max_in_progress restored to 1. Continue with PGSTAT-P0-002; PGSTAT-P0-023 unblocked."
    },
    {
      "id": "PGSTAT-P0-023",
      "order": 29,
      "priority": "P0",
      "workstream": "operations",
      "status": "planned",
      "title": "Wire retention for growing control/dim tables",
      "problem": "Static analysis (2026-07-17) found append-only tables with no purge path anywhere in the code, violating the 'nothing is kept forever by accident' rule: control.alert_notification, control.database_action_log, control.pgss_reset_history, the trigger/command queues (collector_command, baseline_trigger, db_objects_trigger, nightly_snapshot_trigger, report_trigger), expired control.alert_snooze rows, and superseded baseline versions (control.baseline_version + control.metric_baseline + control.metric_baseline_query have INSERTs but no DELETEs). dim.query_text also grows unboundedly with no orphan pruning.",
      "requirements": [
        "Decide per table: permanent config (documented exception) vs retention days.",
        "Add PurgeEvaluator rules for the growing tables (notification/log/history/queues/expired snoozes/superseded baselines).",
        "Decide dim.query_text orphan pruning policy (only prune texts no longer referenced by fact/agg rows).",
        "Record decisions in the data contract registry retention notes."
      ],
      "acceptance_criteria": [
        {
          "id": "AC1",
          "text": "Every growing control table has either a purge rule or a documented durable-retention exception.",
          "status": "pending"
        },
        {
          "id": "AC2",
          "text": "Superseded baseline versions are cleaned up.",
          "status": "pending"
        },
        {
          "id": "AC3",
          "text": "dim.query_text pruning decision is recorded (implemented or explicitly deferred with volume estimate).",
          "status": "pending"
        }
      ],
      "implementation_plan": [
        "Classify the 48 no-retention tables (analysis done 2026-07-17: ~12 grow, rest are bounded config).",
        "Add retention policy fields or fixed windows for the growing set; implement PurgeEvaluator rules.",
        "Update lifecycle matrix expectations and contract registry."
      ],
      "verification_plan": [
        "mvn test",
        "Live: row counts stop growing after purge cycle; no FK/orphan breakage for dim pruning."
      ],
      "required_docs": [
        "docs/data-contract-registry.md",
        "docs/generated/data-lifecycle-matrix.md",
        "docs/generated/project-status.md"
      ],
      "dependencies": [
        "PGSTAT-P0-022"
      ],
      "customer_acceptance": {
        "required": true,
        "status": "pending",
        "evidence": ""
      },
      "verification_evidence": [],
      "next_action": "Start after the P0-022 production fix; the table classification from 2026-07-17 is recorded in this task."
    },
    {
      "id": "PGSTAT-P1-007",
      "order": 27,
      "priority": "P1",
      "workstream": "operations",
      "status": "planned",
      "title": "Write deployment and operations runbook",
      "problem": "Deployment knowledge (deploy.sh flow, service management, backup/restore, upgrade steps, incident basics) is spread across scripts and memory. Enterprise SDLC requires a written runbook so operations do not depend on one person.",
      "requirements": [
        "Document install, upgrade, rollback, backup/restore, and service management for collector/API/UI/central DB.",
        "Document health checks and the first-response steps for common failures.",
        "Align with the release checklist rollback section."
      ],
      "acceptance_criteria": [
        {
          "id": "AC1",
          "text": "A runbook document covers install, upgrade, rollback, backup/restore, and service management.",
          "status": "pending"
        },
        {
          "id": "AC2",
          "text": "Runbook is linked from governance and the release checklist.",
          "status": "pending"
        }
      ],
      "implementation_plan": [
        "Consolidate deploy.sh, Makefile targets, docker-compose files, and operations-guide.md into one authoritative runbook.",
        "Review against a real deploy cycle."
      ],
      "verification_plan": [
        "Dry-run the runbook steps on a staging deploy."
      ],
      "required_docs": [
        "docs/operations-guide.md",
        "docs/platform-governance-and-sdlc.md",
        "docs/generated/project-status.md"
      ],
      "dependencies": [
        "PGSTAT-P0-012"
      ],
      "customer_acceptance": {
        "required": true,
        "status": "pending",
        "evidence": ""
      },
      "verification_evidence": [],
      "next_action": "Schedule after PGSTAT-P0-002; consolidate existing operations-guide.md into a full runbook."
    },
    {
      "id": "PGSTAT-P0-024",
      "order": 30,
      "priority": "P0",
      "workstream": "operations",
      "status": "done",
      "title": "Raise a system alert when a ready instance loses pg_hba access, readable in Telegram and UI",
      "problem": "Customer-reported bug (2026-07-17), root-caused via systematic debugging: when a previously working instance loses its pg_hba.conf entry (or otherwise fails auth/connect), JobOrchestrator.processClusterInstance/processStatementsInstance catch the exception, call stateRepo.updateAfterFailure (consecutive_failures++), then call handleSecretOrAuthError which matches the message on 'connect'/'authentication'/'password'/'secret_ref' and calls inventoryRepo.scheduleBootstrapRetry — this flips instance_inventory.bootstrap_state to 'degraded'. findDueInstances only selects bootstrap_state='ready' (InventoryRepository.java:155), so the instance drops out of the collection queue on the very next cycle and consecutive_failures freezes (usually at 1). SystemHealthEvaluator.checkInstanceUnreachable only opens SYSTEM_INSTANCE_UNREACHABLE when consecutive_failures >= 3 (SystemHealthEvaluator.java:195), so the threshold is never reached. BootstrapHandler then silently retries the degraded instance on a backoff schedule (1min/5min/15min/1hour) with no alert at any point. Net effect: an instance could sit unmonitored indefinitely with zero Telegram/system alert, only an INFO log line ('Instance degraded'a cekildi'). Follow-up (2026-07-27): once the alert did fire, the customer reported it was uninterpretable in Telegram ('Instance unreachable ... The connection attempt failed.' with no host name anywhere) and the UI's InstanceDetail degraded-reason banner didn't recognize this alert code either.",
      "requirements": [
        "Raise a system alert (reuse AlertCode.SYSTEM_INSTANCE_UNREACHABLE, critical) the moment an instance is pushed to degraded by handleSecretOrAuthError, including the classified error detail (pg_hba / auth / connect) in the alert message.",
        "Auto-resolve that alert when the instance successfully returns to bootstrap_state='ready' (BootstrapHandler.handleEnriching).",
        "Every instance-scoped system alert (not just this one) must show which host/instance is affected in the title, since Telegram/email only carry plain title+message text.",
        "The UI's existing degraded-reason banner (InstanceDetail's BootstrapBanner) must also recognize this alert code, not just bootstrap-time codes.",
        "Do not touch the existing bootstrap-time secret_ref_error alert path (BootstrapHandler catch blocks) — this fix targets only the post-ready degrade path in JobOrchestrator.",
        "Keep the fix minimal: no new alert code, no new migration — reuse existing AlertService/AlertCode/notification plumbing and the existing BootstrapBanner UI component."
      ],
      "acceptance_criteria": [
        {
          "id": "AC1",
          "text": "handleSecretOrAuthError raises system_instance_unreachable with the classified error detail when it degrades an instance.",
          "status": "done"
        },
        {
          "id": "AC2",
          "text": "The alert auto-resolves once the instance reaches bootstrap_state='ready' again.",
          "status": "done"
        },
        {
          "id": "AC3",
          "text": "AlertService.upsertSystemAlert automatically appends the instance's display_name and host:port to the alert title when instancePk is provided, so this and every other instance-scoped system alert is readable in Telegram without opening the UI.",
          "status": "done"
        },
        {
          "id": "AC4",
          "text": "InstanceDetail's BootstrapBanner recognizes system_instance_unreachable and shows the same clear degraded-reason banner as the other bootstrap-failure codes.",
          "status": "done"
        },
        {
          "id": "AC5",
          "text": "mvn test and ui tsc pass; a live check confirms the Telegram message names the host and the InstanceDetail banner shows the reason for a real pg_hba revocation, and both clear on restore.",
          "status": "done"
        }
      ],
      "implementation_plan": [
        "Inject AlertService into JobOrchestrator's handleSecretOrAuthError (or a small helper) and call alertService.upsertSystemAlert with AlertCode.SYSTEM_INSTANCE_UNREACHABLE, alert_key 'system.instance_unreachable:instance=<pk>', including the raw exception detail.",
        "Inject AlertService into BootstrapHandler and call alertService.resolveSystemAlert on the same alert_key inside handleEnriching right after the state flips to 'ready'.",
        "Add AlertService.withInstanceLabel(title, instancePk): looks up control.instance_inventory (display_name, host, port) and appends '— <display_name> (<host>:<port>)' to the title; fails soft if lookup errors. Call it from upsertSystemAlert before insert/update.",
        "Add 'system_instance_unreachable' to InstanceDetail.tsx's bootstrapCodes array so the existing BootstrapBanner picks it up.",
        "Add/extend a unit test asserting the alert is raised on the degrade path and resolved on the ready transition (tracked as a known test-coverage gap alongside the rest of this code path)."
      ],
      "verification_plan": [
        "mvn test",
        "cd ui && npx tsc --noEmit",
        "Live: revoke pg_hba access for a test instance, confirm system_instance_unreachable opens with the host named in the Telegram message and the InstanceDetail banner shows the reason; restore access, confirm the alert auto-resolves and the banner clears."
      ],
      "required_docs": [
        "docs/alert-reference.md",
        "docs/generated/project-status.md"
      ],
      "dependencies": [],
      "customer_acceptance": {
        "required": true,
        "status": "accepted",
        "evidence": "Customer confirmed 2026-07-27 ('oldu tamam') after live-testing the full cycle: revoked pg_hba access, alert opened and named the host, stayed open across multiple 5-minute SystemHealthEvaluator cycles without access being restored (no premature auto-resolve), then closed correctly once access was restored."
      },
      "verification_evidence": [
        "Root cause confirmed 2026-07-17 via systematic debugging (5 hypothesis-testing steps tracing JobOrchestrator -> InventoryRepository.findDueInstances -> SystemHealthEvaluator threshold -> BootstrapHandler retry loop).",
        "Fix applied 2026-07-17: JobOrchestrator.handleSecretOrAuthError now raises AlertCode.SYSTEM_INSTANCE_UNREACHABLE (alert_key system.instance_unreachable:instance=<pk>) with the classified error detail; BootstrapHandler.handleEnriching resolves the same alert_key once the instance reaches bootstrap_state='ready'.",
        "Customer-reported unreadable Telegram alert 2026-07-27 (no host name) led to a discussion and a centralized fix: AlertService.withInstanceLabel() added and wired into upsertSystemAlert; InstanceDetail.tsx bootstrapCodes now includes system_instance_unreachable.",
        "mvn compile, mvn test, and ui tsc --noEmit all passed with no regressions (existing 4 collector test classes unaffected; this code path has no existing unit test coverage per docs/test-strategy.md known gaps).",
        "Customer live-tested 2026-07-27 by revoking pg_hba for instance 'sam-ets': InstanceDetail showed the pg_hba error text in 'Son Hata' but bootstrap_state stayed 'discovering'/'ready' (never 'degraded') and no Telegram alert fired. Root cause #2 found: the instance had already gone ready->degraded->(auto retry)->pending->discovering, and DiscoveryCollector.discover()'s catch block (a completely separate code path from JobOrchestrator.handleSecretOrAuthError) only calls stateRepo.updateLastError (no alert, no consecutive_failures increment) when discovery fails — so an instance stuck retrying discovery forever raised zero alerts. Also, InstanceDetail's BootstrapBanner only rendered for bootstrap_state==='degraded', so even alerts raised on this path wouldn't have shown a banner for a 'discovering' instance.",
        "Fix applied 2026-07-27: BootstrapHandler.handleDiscovering now calls a new raiseUnreachableAlert() helper (reusing AlertCode.SYSTEM_INSTANCE_UNREACHABLE, same alert_key pattern, so it still auto-resolves in handleEnriching) whenever discoveryCollector.discover() returns null, using the error text just written by StateRepository.getLastError() (new read-only method added). InstanceDetail's BootstrapBanner condition changed from bootstrap_state==='degraded' to bootstrap_state !== 'ready' (any not-yet-ready state), so the banner now also covers instances stuck retrying discovery.",
        "mvn compile, mvn test, and ui tsc --noEmit all passed again after this second fix, no regressions.",
        "Customer confirmed 2026-07-27 the alert now fires and names the host ('Instance unreachable — sam-ets (10.90.137.51:5432)'), but flagged the details view as noisy: the same long PostgreSQL error text was repeated 3-4 times (message body, 'Detay' panel's last_error, and a raw JSON block always shown below). Fixed: (a) collector alert messages (JobOrchestrator.handleSecretOrAuthError and BootstrapHandler.raiseUnreachableAlert) shortened to a plain-language sentence, with the technical error text left only in details_json.context.last_error; (b) Alerts.tsx AlertDetails: the raw JSON fallback is now hidden behind a collapsed '<details>/Ham veriyi göster' toggle whenever a readable panel (context/baseline/records) already shown that same data, instead of always rendering both; (c) the context.reason value ('discovery_failed' / 'degraded_after_ready') is now shown as a short Turkish label via a new reasonLabel() helper instead of the raw code.",
        "mvn compile, mvn test, and ui tsc --noEmit all passed again after the message/UI cleanup, no regressions.",
        "Customer reported 2026-07-27 (without having restored pg_hba access) that Telegram sent a 'Cozuldu' (resolved) notification for the same alert. Root cause #3: SystemHealthEvaluator.checkInstanceUnreachable() runs every 5 minutes and has a long-standing auto-resolve loop over ALL open SYSTEM_INSTANCE_UNREACHABLE alerts that closes any alert whose instance_state.consecutive_failures == 0 — regardless of why the alert was opened. The new BootstrapHandler.raiseUnreachableAlert() path (root cause #2's fix) never increments consecutive_failures (only JobOrchestrator.updateAfterFailure does), so any alert it opened had consecutive_failures==0 from the start and was auto-resolved by this unrelated 5-minute job within one cycle, while the instance was still genuinely unreachable.",
        "Fix applied 2026-07-27: SystemHealthEvaluator.checkInstanceUnreachable()'s auto-resolve loop now also requires bootstrap_state == 'ready' (new isBootstrapReady() helper querying control.instance_inventory), not just consecutive_failures == 0, before closing a system_instance_unreachable alert. This makes the auto-resolve correct for both alert-opening paths: it no longer fires early for instances still stuck in 'degraded'/'discovering'.",
        "mvn compile and mvn test passed again after this third fix, no regressions.",
        "Customer live-verified 2026-07-27 the full fix set end to end and confirmed with 'oldu tamam': alert opens with host name, stays open while pg_hba access remains revoked (survives multiple SystemHealthEvaluator 5-minute cycles without premature auto-resolve), and clears correctly once access is restored."
      ],
      "completed_at": "2026-07-27",
      "next_action": "Closed. Restore max_in_progress to 1; continue with PGSTAT-P0-002; PGSTAT-P0-023 unblocked."
    },
    {
      "id": "PGSTAT-P0-026",
      "order": 32,
      "priority": "P0",
      "workstream": "operations",
      "status": "done",
      "title": "Stop AlertService.upsertSystemAlert from reopening and re-notifying acknowledged alerts",
      "problem": "Customer reported (2026-07-27) that acknowledging a 'Stat collection failed' alert (STAT_COLLECTION_FAILED) did not stop it from repeating in Telegram. Root cause: AlertService.upsertSystemAlert unconditionally set status='open' and acknowledged_at=null on every upsert (AlertRepository had the same pattern in upsert/upsertAdaptive/upsertAdaptiveWithSeverity), and shouldNotifySystemOpen() treats any non-'open' previousStatus (including 'acknowledged') as a reason to notify. So the moment an acknowledged alert's underlying condition re-triggered (e.g. the same collector job failing again 5 minutes later), the alert silently flipped back to 'open' and re-notified — acknowledge provided no suppression at all, contrary to customer expectation. Discussion clarified the desired behavior: acknowledge should NOT suppress notifications; the customer wants the alert to keep repeating at its normal cadence until they explicitly resolve it — but the current code's problem was that 'acknowledged' status was being silently and incorrectly reset to 'open' with acknowledged_at wiped, which is misleading in the UI even though notifications kept flowing.",
      "requirements": [
        "An acknowledged alert whose condition keeps re-triggering must keep notifying at the normal cadence (existing NotificationService cooldown logic already throttles repeat sends) — acknowledge is a note, not a mute.",
        "An acknowledged alert's status must stay 'acknowledged' (not silently flip back to 'open') while the underlying condition is still active, so the UI accurately reflects that someone already looked at it.",
        "A 'resolved' alert reopening (genuinely new occurrence) must still flip to 'open' and notify as before — only the 'acknowledged' case changes.",
        "Only the user's explicit resolve action (or the code's own resolveSystemAlert call once the root cause clears) should end the repeat cycle."
      ],
      "acceptance_criteria": [
        {
          "id": "AC1",
          "text": "AlertService.upsertSystemAlert keeps status='acknowledged' (and acknowledged_at unchanged) on re-trigger instead of resetting to 'open', while still updating occurrence_count/last_seen_at/message/details_json.",
          "status": "done"
        },
        {
          "id": "AC2",
          "text": "Notifications keep firing for a re-triggered acknowledged alert (subject to existing cooldown), so the alert keeps repeating instead of going silent.",
          "status": "done"
        },
        {
          "id": "AC3",
          "text": "A 'resolved' alert reopening on re-trigger is unaffected (still flips to 'open' and notifies as before).",
          "status": "done"
        },
        {
          "id": "AC4",
          "text": "Dropping a database no longer causes stat_collection_failed to repeat forever: DiscoveryCollector auto-deactivates dim.database_ref rows that disappear from the source database list, logs the action, and resolves the instance's open/acknowledged stat_collection_failed alert.",
          "status": "done"
        }
      ],
      "implementation_plan": [
        "In AlertService.upsertSystemAlert, read previousStatus before the upsert; change the ON CONFLICT UPDATE to keep status/acknowledged_at as 'acknowledged' when the existing row is already 'acknowledged', otherwise set 'open'/null as before.",
        "Adjust the post-upsert notify decision: when previousStatus was 'acknowledged', notify whenever severity is warning/critical (same threshold as new alerts), instead of relying on shouldNotifySystemOpen's open/severity-escalation logic which assumed status was always 'open' or terminal.",
        "Scope limited to upsertSystemAlert (the system-alert path used by SystemHealthEvaluator/JobOrchestrator/BootstrapHandler); AlertRepository's adaptive-rule upsert paths were not touched in this pass.",
        "AC4 (2026-08-12 follow-up): DiscoveryCollector.discoverDatabases() now collects the dbids seen in the source database list and calls a new deactivateMissingDatabases() helper, which sets dim.database_ref.is_active=false + disabled_at/disabled_reason for any previously-active row whose dbid was not seen, logs to control.database_action_log (same pattern as the manual /database-cleanup/disable endpoint), and calls alertService.resolveSystemAlert() for that instance's system.stat_collection_failed key. resolveSystemAlert widened from status='open' to status in ('open','acknowledged') since a genuinely-cleared root cause should close an acknowledged alert too."
      ],
      "verification_plan": [
        "mvn compile && mvn test",
        "Live: acknowledge an open stat_collection_failed alert, let the underlying job fail again, confirm status stays 'acknowledged' in the UI and Telegram still sends the repeat notification (subject to cooldown); resolve the alert and confirm it stops.",
        "Live (AC4): drop a database on a monitored instance, confirm stat_collection_failed opens; wait for the next discovery/rediscovery cycle, confirm dim.database_ref for that db flips to is_active=false with a database_action_log row, and confirm the alert auto-resolves without deploying any manual database-cleanup action."
      ],
      "required_docs": [
        "docs/alert-reference.md",
        "docs/generated/project-status.md"
      ],
      "dependencies": [],
      "customer_acceptance": {
        "required": true,
        "status": "accepted",
        "evidence": "Customer confirmed 2026-08-18 ('hepsi ok') after live-testing acknowledge/resolve cadence and the dropped-database auto-clear behavior together with the other same-day fixes."
      },
      "verification_evidence": [
        "Customer reported acknowledging a stat_collection_failed alert did not stop repeat Telegram notifications; investigation found the real bug was the opposite of the customer's mental model — acknowledge was already not suppressing notifications, but it was silently and incorrectly flipping the alert back to 'open' on every re-trigger, which is misleading in the UI (looks like nobody acknowledged it).",
        "Discussed and confirmed desired behavior: acknowledge is a note only, not a mute; the alert should keep repeating until the user resolves it or the root cause clears. Fixed AlertService.upsertSystemAlert to preserve 'acknowledged' status/timestamp across re-triggers while still notifying and updating occurrence_count/last_seen_at.",
        "mvn compile and mvn test passed with no regressions.",
        "Customer follow-up 2026-08-12: dropped a database on instance 'moneasy-test-167.50', acknowledged and later resolved the resulting stat_collection_failed alert manually, but it kept reopening. Root cause #2: DiscoveryCollector.discoverDatabases() only ever upserts (adds) database rows — it never deactivates a dim.database_ref row whose database was dropped from the source, so the db_objects job kept retrying the missing database forever, re-triggering SystemHealthEvaluator.checkStatCollectionFailures() every cycle. The existing manual 'Database Cleanup' UI (databaseCleanup.ts) could not have helped either — it only scans job_failed/job_partial_failure/connection_failure/permission_denied alert messages for 'does not exist', and stat_collection_failed is a separate, instance-level-aggregated alert code that was never in that scan list.",
        "Fix applied 2026-08-12: DiscoveryCollector now auto-deactivates dim.database_ref rows missing from the live database list (same schema/audit pattern as the existing manual disable endpoint) and auto-resolves the instance's stat_collection_failed alert once the stale database is deactivated. mvn compile and mvn test passed (DiscoveryCollectorTest constructor call updated for the new AlertService dependency).",
        "Customer live-verified 2026-08-18: acknowledge/resolve cadence works as designed and dropped-database auto-clear confirmed."
      ],
      "completed_at": "2026-08-18",
      "next_action": "Closed."
    },
    {
      "id": "PGSTAT-P1-008",
      "order": 33,
      "priority": "P1",
      "workstream": "governance",
      "status": "in_progress",
      "title": "Domain-by-domain behavior specification program for a from-scratch rewrite",
      "problem": "Customer wants to rewrite pgstat from scratch but with every behavior fully specified on paper first ('noktasi virgulune kadar') so no behavior is lost. The codebase is too large to spec in one pass: 92 tables/1148 columns, ~20,159 lines of Java collector, ~205 API routes (~13,500 lines TS), ~18,740 lines of React UI, 91 migrations. A domain map, sequencing, and a per-domain spec template were designed via three parallel discovery agents (collector+data model, API, UI+alerting) plus a synthesis/design agent, approved as a plan on 2026-08-12.",
      "requirements": [
        "Produce one specification document per domain under docs/spec/, in the approved sequence, each detailed enough to re-implement the domain's behavior without referring back to the old code.",
        "Follow the approved 9-section template per domain: scope, data sources, business/computation rules, storage/lifecycle, API contract, UI behavior, cross-domain interfaces, acceptance criteria, open questions.",
        "Do not silently canonicalize undocumented/ambiguous behavior (duplicate migration numbers V021/054-057/084, dead code, TODOs) — flag it explicitly in each domain's 'open questions' section.",
        "Each domain document requires customer review/approval before starting the next domain (sequential, not parallel, per customer instruction)."
      ],
      "acceptance_criteria": [
        {
          "id": "AC1",
          "text": "Domain map with 12 domains (15 documents counting the 4 Alerting sub-phases 8a-8d) and dependency-driven sequencing is recorded (this task) and matches the approved plan.",
          "status": "done"
        },
        {
          "id": "AC2",
          "text": "docs/spec/01-data-model-and-storage-lifecycle.md exists, approved by customer.",
          "status": "pending"
        },
        {
          "id": "AC3",
          "text": "Each subsequent domain (02 through 12, with 08a-08d as four documents) is completed and customer-approved in sequence before the next begins.",
          "status": "pending"
        }
      ],
      "implementation_plan": [
        "Sequence: 01 Data Model & Storage Lifecycle -> 02 Instance/Cluster Discovery & Registration -> 03 Core Cluster Metrics Collection -> 04 Query Statements -> 05 Database Object Inventory & Health -> 06 Nightly Snapshot & Reporting -> 07 Scheduler & Job Orchestration -> 08a Alerting (Static Rules) -> 08b Alerting (Adaptive Baseline) -> 08c Alerting (Lifecycle Subscriptions) -> 08d Notifications & Delivery -> 09 System Health & Self-Monitoring -> 10 Platform (Auth/Preferences/Audit/Instance Groups) -> 11 Grafana Integration -> 12 Insights & Ad-hoc Analysis.",
        "pgdbaagent is explicitly out of scope for this program (already has dedicated docs: pgdbaagent-contracts.md, agentic-dba-platform-architecture.md).",
        "Insights.tsx/insights.ts and instances.ts are known multi-domain 'god files' that get split across their owning domains rather than spec'd as single documents (see plan file for detail).",
        "Full plan and rationale: C:\\Users\\samet.kutuk\\.claude\\plans\\gentle-bubbling-turing.md"
      ],
      "verification_plan": [
        "Each domain spec is reviewed by the customer against the actual running system (code read and, where needed, live query) before being marked approved."
      ],
      "required_docs": [
        "docs/spec/README.md",
        "docs/generated/project-status.md"
      ],
      "dependencies": [],
      "customer_acceptance": {
        "required": true,
        "status": "pending",
        "evidence": ""
      },
      "verification_evidence": [
        "Domain map produced by three parallel discovery agents (collector+data model: 91 migrations, 7 collector classes, ~20,159 lines; API: 205 routes across 20 files, ~13,500 lines, instances.ts and insights.ts identified as oversized multi-domain files; UI+alerting: 23 pages ~18,740 lines, AlertRuleEvaluator 3564 lines, only Telegram concretely implemented) plus a synthesis/design agent that reconciled all three into one 12-domain, dependency-ordered sequence with a 9-section spec template. Plan approved by customer 2026-08-12."
      ],
      "next_action": "Write docs/spec/01-data-model-and-storage-lifecycle.md (schema V001-008, PartitionManager, PurgeEvaluator, AggRepository, retention V022) for customer review."
    },
    {
      "id": "PGSTAT-P0-027",
      "order": 34,
      "priority": "P0",
      "workstream": "operations",
      "status": "done",
      "title": "Fix agg.pg_table_stat_hourly rollup ON CONFLICT crash on renamed/OID-reused tables",
      "problem": "Customer-reported (2026-08-17) after the P0-026 upgrade: collector logs showed 'Rollup job hatasi: ... bad SQL grammar [insert into agg.pg_table_stat_hourly ...] Caused by: ERROR: ON CONFLICT DO UPDATE command cannot affect row a second time'. Root cause: AggRepository.rollupTableStatHourly()'s GROUP BY included schemaname/relname in addition to the ON CONFLICT target (bucket_start, instance_pk, dbid, relid). If the same relid appears with two different schemaname/relname values within the same hourly window (table renamed, or OID reused after a drop+create), the GROUP BY produces two rows that the ON CONFLICT target treats as one, and PostgreSQL rejects the double-affect. rollupHourly() (pgss) does not have this bug because its GROUP BY already matches its ON CONFLICT target exactly.",
      "requirements": [
        "GROUP BY must match the ON CONFLICT target exactly so a single relid never produces more than one row per hourly window.",
        "schemaname/relname must still reflect a real observed value (not dropped) — use the same 'last observed in window' pattern already used for n_live_tup_last/n_dead_tup_last/n_mod_since_analyze_last in the same query.",
        "No new migration, no new alert code — pure SQL fix inside the existing rollup method."
      ],
      "acceptance_criteria": [
        {
          "id": "AC1",
          "text": "rollupTableStatHourly()'s GROUP BY is (bucket_start, instance_pk, dbid, relid) only, matching the ON CONFLICT target.",
          "status": "done"
        },
        {
          "id": "AC2",
          "text": "schemaname/relname are selected via array_agg(... order by sample_ts desc)[1], same pattern as the existing 'last' columns, and are also updated in the ON CONFLICT DO UPDATE clause.",
          "status": "done"
        },
        {
          "id": "AC3",
          "text": "mvn test passes; live verification confirms the rollup job no longer throws this exception after deploy.",
          "status": "done"
        }
      ],
      "implementation_plan": [
        "Move schemaname/relname out of GROUP BY into the SELECT list as (array_agg(schemaname order by sample_ts desc))[1] and same for relname.",
        "Add schemaname = excluded.schemaname, relname = excluded.relname to the ON CONFLICT DO UPDATE SET clause so a rename is still reflected on the next hour's upsert."
      ],
      "verification_plan": [
        "mvn compile && mvn test",
        "Live: watch collector logs after deploy for at least one full hourly rollup cycle, confirm no more 'ON CONFLICT DO UPDATE command cannot affect row a second time' errors."
      ],
      "required_docs": [
        "docs/generated/project-status.md"
      ],
      "dependencies": [],
      "customer_acceptance": {
        "required": true,
        "status": "accepted",
        "evidence": "Customer confirmed 2026-08-18 ('hepsi ok') after live-verifying no more ON CONFLICT errors across at least one hourly rollup cycle."
      },
      "verification_evidence": [
        "Customer shared collector logs 2026-08-17 showing the BadSqlGrammarException/ON CONFLICT error on every rollup cycle since the P0-026 deploy (pre-existing bug, unrelated to that deploy, just first noticed then).",
        "Root cause confirmed by comparing rollupTableStatHourly() against the structurally-correct rollupHourly() (pgss) sibling method in the same file — GROUP BY vs ON CONFLICT target mismatch.",
        "Fix applied: schemaname/relname moved out of GROUP BY, selected via the same array_agg 'last observed' pattern as the other snapshot-style columns, added to the ON CONFLICT UPDATE clause. mvn compile and mvn test pass with no regressions.",
        "Customer live-verified 2026-08-18: no more ON CONFLICT errors in collector logs."
      ],
      "completed_at": "2026-08-18",
      "next_action": "Closed."
    },
    {
      "id": "PGSTAT-P0-028",
      "order": 35,
      "priority": "P0",
      "workstream": "operations",
      "status": "planned",
      "title": "Investigate ClusterCollector WAL snapshot permission gap on samet-ets-01 (pg_ls_waldir)",
      "problem": "Customer-shared logs 2026-08-17 show a recurring WARN on instance samet-ets-01: 'WAL snapshot hatasi: samet-ets-01 — ERROR: permission denied for function pg_ls_waldir'. This is handled gracefully today (try/catch around collectWalSnapshot skips just that sub-step, no crash, no alert), so it is not urgent, but it is a real coverage gap: WAL directory size is not being collected for this instance because the collector's PostgreSQL role lacks EXECUTE on pg_ls_waldir (typically requires pg_monitor membership or superuser).",
      "requirements": [
        "Confirm whether this is expected (customer intentionally scoped the collector role narrowly on this instance) or an oversight in the role grant script/runbook.",
        "If unintentional, document (or fix via a GRANT) the required privilege for full WAL snapshot coverage.",
        "If intentional/acceptable, no code change — just confirm the degraded coverage is visible to the customer somewhere (not just a WARN buried in logs) so it doesn't look like silent data loss."
      ],
      "acceptance_criteria": [
        {
          "id": "AC1",
          "text": "Root cause (missing pg_monitor grant vs. intentional restriction) confirmed with the customer.",
          "status": "pending"
        }
      ],
      "implementation_plan": [
        "Ask the customer whether pg_ls_waldir coverage matters for samet-ets-01 and whether they want to grant pg_monitor to the collector role there."
      ],
      "verification_plan": [
        "None yet — pending customer input."
      ],
      "required_docs": [
        "docs/generated/project-status.md"
      ],
      "dependencies": [],
      "customer_acceptance": {
        "required": true,
        "status": "pending",
        "evidence": ""
      },
      "verification_evidence": [],
      "next_action": "Ask customer whether this WAL directory coverage gap on samet-ets-01 needs a privilege grant or is acceptable as-is."
    },
    {
      "id": "PGSTAT-P0-029",
      "order": 36,
      "priority": "P0",
      "workstream": "operations",
      "status": "done",
      "title": "Distinguish 'pg_stat_statements missing entirely' from 'installed but not in admin DB' in the degraded-reason banner",
      "problem": "Customer reported (2026-08-17) instance db1.dc-etstur.com-test stuck retrying bootstrap forever ('pending' -> 'degraded' loop, pg_stat_statements bulunamadi). Customer clarified pg_stat_statements WAS installed — but in the etstur application database, not in the instance's admin_dbname (postgres). DiscoveryCollector already detects and logs this exact distinction internally (pgssInPreload flag, 'admin DB'de yok ama shared_preload_libraries'de var' log line) but never surfaced it past the log — InstanceDetail's BootstrapBanner always showed the same generic 4-step fix ('add to shared_preload_libraries, restart PostgreSQL, CREATE EXTENSION, retry'), which is actively wrong advice when the extension is already preloaded and just needs CREATE EXTENSION in the correct database — telling the customer to edit postgresql.conf and restart production PostgreSQL for a problem that doesn't require it.",
      "requirements": [
        "When the root cause is 'preloaded but not created in admin_dbname', the banner must name the actual admin_dbname and give only the one relevant step (CREATE EXTENSION in that database + retry), not the full reinstall procedure.",
        "The generic 4-step reinstall message must remain for the genuine 'not installed at all' case.",
        "No new migration/column if avoidable — reuse existing capability plumbing.",
        "Fix must not regress the existing degraded-reason banner behavior for other bootstrap failure codes (secret_ref_error, authentication_failure, etc.)."
      ],
      "acceptance_criteria": [
        {
          "id": "AC1",
          "text": "CapabilityRepository.upsert() persists last_error_text on successful discovery (previously only markUnreachable wrote it, so a same-discovery-cycle note was always dropped).",
          "status": "done"
        },
        {
          "id": "AC2",
          "text": "DiscoveryCollector writes a machine-parseable note ('pg_stat_statements_in_preload_but_missing_in_admin_db:<admin_dbname>') to last_error_text whenever hasPgss=false and the extension is in shared_preload_libraries.",
          "status": "done"
        },
        {
          "id": "AC3",
          "text": "InstanceDetail's BootstrapBanner parses that note and, when present, shows the admin_dbname and a single-step CREATE EXTENSION instruction with an explicit 'no restart needed' note, instead of the generic 4-step reinstall guidance.",
          "status": "done"
        },
        {
          "id": "AC4",
          "text": "Live verification: db1.dc-etstur.com-test's banner names 'postgres' as the admin DB and gives the one-step fix; after running CREATE EXTENSION there and retrying, the instance reaches 'ready'.",
          "status": "done"
        }
      ],
      "implementation_plan": [
        "CapabilityRepository.upsert(): add last_error_text to the INSERT column list and ON CONFLICT UPDATE SET clause (was previously write-only from markUnreachable).",
        "DiscoveryCollector.discover(): compute pgssNote = 'pg_stat_statements_in_preload_but_missing_in_admin_db:' + instance.adminDbname() when !hasPgss && pgssInPreload, pass as capability.lastErrorText().",
        "InstanceDetail.tsx BootstrapBanner: parse cap.last_error_text with a regex for the note prefix; when matched, render the admin-DB-specific one-step message; otherwise fall back to the existing generic 4-step message."
      ],
      "verification_plan": [
        "mvn compile && mvn test",
        "cd ui && npx tsc --noEmit",
        "Live: confirm db1.dc-etstur.com-test's InstanceDetail banner now names 'postgres' and shows the one-step fix; run CREATE EXTENSION pg_stat_statements in postgres there, retry, confirm bootstrap_state reaches 'ready'."
      ],
      "required_docs": [
        "docs/generated/project-status.md"
      ],
      "dependencies": [],
      "customer_acceptance": {
        "required": true,
        "status": "accepted",
        "evidence": "Customer confirmed 2026-08-18 ('hepsi ok') after live-verifying the banner and running CREATE EXTENSION on the admin DB."
      },
      "verification_evidence": [
        "Customer reported db1.dc-etstur.com-test stuck in a pending/degraded bootstrap loop; logs confirmed DiscoveryCollector already logs 'pg_stat_statements admin DB'de (postgres) yok ama shared_preload_libraries'de var' — the distinction existed internally but was invisible to the customer/UI.",
        "Customer clarified pg_stat_statements was installed in the etstur database, confirming the root cause is admin_dbname mismatch, not a missing extension — validating that the generic reinstall message the UI was showing was actively incorrect for this case.",
        "Fix applied: CapabilityRepository.upsert() now writes last_error_text on every successful discovery (previously dropped); DiscoveryCollector writes a structured note when this exact scenario is detected; InstanceDetail's BootstrapBanner renders a scenario-specific one-step fix (CREATE EXTENSION in the named admin DB, no restart) instead of the generic 4-step reinstall guidance when the note is present.",
        "mvn compile, mvn test, and ui tsc --noEmit all pass with no regressions.",
        "Customer live-verified 2026-08-18: banner named the correct admin DB, one-step fix worked, instance reached 'ready'."
      ],
      "completed_at": "2026-08-18",
      "next_action": "Closed."
    },
    {
      "id": "PGSTAT-P0-030",
      "order": 37,
      "priority": "P0",
      "workstream": "operations",
      "status": "done",
      "title": "Editing an instance without entering a password silently wipes its secret_ref",
      "problem": "Customer reported (2026-08-18): editing an instance on the Instances tab and leaving the password field empty (whether or not other fields were changed) produces a 'Secret hatasi: secret_ref bos veya null' error on the next collector connection attempt. Root cause: InstanceForm.tsx's handleSubmit always sends secret_ref='' when authMode==='password' (even in edit mode with an empty password field, by design — the form's own validate() correctly allows an empty password on edit). The PUT /api/instances/:id route's UPDATE used 'secret_ref = coalesce($9, secret_ref)', which only preserves the existing value when the incoming value is SQL NULL — an empty string is not NULL, so coalesce treated it as 'user wants to clear this' and overwrote the real secret_ref with an empty string, breaking the instance's credentials on the very next scheduled connection.",
      "requirements": [
        "Saving an instance edit with an empty password field must leave the existing secret_ref untouched — 'no password entered' must mean 'keep using the current credential', not 'clear the credential'.",
        "Entering a password on edit must still re-encrypt and replace secret_ref as before.",
        "Switching to secret_ref auth mode and entering a real secret_ref value must still work as before.",
        "The POST /api/instances (create) route is unaffected — it already rejects an empty secret_ref/password combination with a 400, this fix is scoped to the PUT (update) route only."
      ],
      "acceptance_criteria": [
        {
          "id": "AC1",
          "text": "PUT /api/instances/:id treats an empty/whitespace-only secret_ref in the request body as 'unchanged' (passes SQL NULL to the coalesce), not as a value to write.",
          "status": "done"
        },
        {
          "id": "AC2",
          "text": "Editing any other field on an instance while leaving the password blank no longer touches its secret_ref.",
          "status": "done"
        },
        {
          "id": "AC3",
          "text": "Entering a new password on edit still re-encrypts and updates secret_ref as before (regression check).",
          "status": "done"
        },
        {
          "id": "AC4",
          "text": "Live verification: edit an instance with the password field empty, confirm secret_ref is unchanged in the database and the next collector connection succeeds.",
          "status": "done"
        }
      ],
      "implementation_plan": [
        "In the PUT /api/instances/:id handler, change 'let finalSecretRef = secret_ref' to 'let finalSecretRef = secret_ref && secret_ref.trim() ? secret_ref : null' before the password re-encryption check, so a blank secret_ref field resolves to null (preserved by coalesce) instead of '' (which coalesce treats as a real value)."
      ],
      "verification_plan": [
        "cd api && npx tsc --noEmit",
        "Live: edit an instance's display_name only (leave password blank), confirm secret_ref in control.instance_inventory is unchanged and the collector's next connection to that instance still succeeds; then edit again entering a new password and confirm secret_ref does change and the new credential works."
      ],
      "required_docs": [
        "docs/generated/project-status.md"
      ],
      "dependencies": [],
      "customer_acceptance": {
        "required": true,
        "status": "accepted",
        "evidence": "Customer confirmed 2026-08-18 ('hepsi ok') after live-verifying secret_ref preservation and re-encryption on password change."
      },
      "verification_evidence": [
        "Customer reported the 'Secret hatasi: secret_ref bos veya null' error appearing after editing an instance without entering a password.",
        "Root cause confirmed by reading InstanceForm.tsx (handleSubmit always sends secret_ref='' in password auth mode, by design, even on edit with a blank password) and the PUT /api/instances/:id route (coalesce($9, secret_ref) only guards against SQL NULL, not empty string).",
        "Fix applied: PUT route now normalizes an empty/whitespace secret_ref to null before the coalesce, matching the customer's expected behavior ('if I don't enter a password, keep using the old one'). npx tsc --noEmit passes.",
        "Customer live-verified 2026-08-18: secret_ref untouched when password left blank, collector kept connecting; secret_ref correctly updated when a new password was entered."
      ],
      "completed_at": "2026-08-18",
      "next_action": "Closed."
    },
    {
      "id": "PGSTAT-P0-031",
      "order": 38,
      "priority": "P0",
      "workstream": "operations",
      "status": "done",
      "title": "Updating an instance's password does not auto-trigger a bootstrap retry",
      "problem": "Customer reported (2026-08-18): after entering a new password on an instance and clicking Update, the instance does not automatically retry bootstrap/discovery with the new credential — the customer has to separately click the 'Yeniden Dene' (retry) button. Root cause: PUT /api/instances/:id re-encrypts and saves the new secret_ref, but never resets bootstrap_state, bootstrap_retry_count, next_bootstrap_retry_at, or instance_state.consecutive_failures/backoff_until the way the dedicated PATCH /:id/retry endpoint does — so a degraded instance stays degraded (and may sit behind a stale backoff window) until the customer manually retries, and even a 'ready' instance's next reconnect still uses the same in-memory connection state rather than being forced to re-discover with the new password right away.",
      "requirements": [
        "Saving an instance edit that includes a real password change must automatically kick off the same reset the manual retry button performs (pending state, retry counters cleared, backoff cleared), so the customer never has to click retry separately after a password update.",
        "Editing an instance WITHOUT changing the password must not touch bootstrap_state/retry counters — this fix is scoped strictly to the password-changed case.",
        "Must not regress PGSTAT-P0-030 (blank password on edit still preserves the existing secret_ref and does nothing to bootstrap state)."
      ],
      "acceptance_criteria": [
        {
          "id": "AC1",
          "text": "PUT /api/instances/:id, when password is non-blank (a real change), resets bootstrap_state to 'pending', clears bootstrap_retry_count/next_bootstrap_retry_at (when those columns exist), and clears instance_state.last_error/consecutive_failures/backoff_until — the same reset PATCH /:id/retry performs.",
          "status": "done"
        },
        {
          "id": "AC2",
          "text": "Editing an instance with a blank password field does not touch bootstrap_state or retry counters (no regression vs P0-030).",
          "status": "done"
        },
        {
          "id": "AC3",
          "text": "Live verification: change an instance's password and click Update (without touching the retry button); confirm the instance moves through pending/discovering on its own within the next poll cycle and reaches 'ready' if the new credential is correct.",
          "status": "done"
        }
      ],
      "implementation_plan": [
        "In the PUT /api/instances/:id handler, track passwordChanged = Boolean(password && password.trim()) before the encryption step.",
        "After the UPDATE query succeeds, if passwordChanged, run the same bootstrap_state='pending'/retry-counter-reset/instance_state-reset SQL already used by PATCH /:id/retry, and reflect bootstrap_state='pending' in the response row so the UI's post-save invalidation shows the new state immediately."
      ],
      "verification_plan": [
        "cd api && npx tsc --noEmit",
        "Live: on a degraded instance, enter a new (correct) password and click Update without touching retry; confirm bootstrap_state flips to 'pending' immediately and the instance reaches 'ready' within the next poll cycles without any manual retry click.",
        "Regression: edit an instance leaving the password blank; confirm bootstrap_state and instance_state retry fields are untouched."
      ],
      "required_docs": [
        "docs/generated/project-status.md"
      ],
      "dependencies": [],
      "customer_acceptance": {
        "required": true,
        "status": "accepted",
        "evidence": "Customer confirmed 2026-08-18 ('olmuş tamamdır') after live-verifying password update auto-triggers bootstrap retry without a manual click."
      },
      "verification_evidence": [
        "Customer reported having to manually click 'Yeniden Dene' after updating an instance's password, expecting the retry to happen automatically.",
        "Root cause confirmed by comparing PUT /api/instances/:id (only re-encrypts secret_ref) against PATCH /api/instances/:id/retry (resets bootstrap_state/retry counters/backoff) — the two were never linked.",
        "Fix applied: PUT route now performs the same reset as the retry endpoint whenever a real password change is detected, scoped so a blank-password edit (P0-030's case) is unaffected. npx tsc --noEmit passes.",
        "Customer live-verified 2026-08-18: password update alone (no manual retry click) moved the instance through pending/discovering to ready."
      ],
      "completed_at": "2026-08-18",
      "next_action": "Closed."
    },
    {
      "id": "PGSTAT-P0-032",
      "order": 39,
      "priority": "P0",
      "workstream": "operations",
      "status": "done",
      "title": "Nightly ops.job_run purge takes 5-20 minutes per batch due to a missing started_at index",
      "problem": "Customer reported (2026-08-19) a run of 'Uzun suren sorgu' (long-running query) WARN alerts between 05:00-06:05 on the pgstat central database itself, all the same statement: 'delete from ops.job_run where ctid in (select ctid from ops.job_run where started_at < now() - $1::interval limit 10000)' — this is PurgeEvaluator.purgeJobRunHistory()'s own nightly batched delete. Root cause confirmed live: ops.job_run has 1,023,900 rows (225 MB) but only 9,503 (~1%) are older than the 30-day retention window; ops.job_run_instance has 2,237,533 rows (518 MB). Neither table has an index with started_at as the leading column — ix_job_run_type_started and ix_job_run_status_started both lead with job_type/status, so PostgreSQL sequential-scans the full ~1M/~2.2M row tables on every 10k-row batch iteration of the purge loop, taking 5-20 minutes per batch instead of milliseconds.",
      "requirements": [
        "Add a started_at-leading index on both ops.job_run and ops.job_run_instance so the nightly purge's WHERE started_at < ... scan uses an index instead of a full table scan.",
        "Index creation must not lock the tables for writes in production — use CREATE INDEX CONCURRENTLY.",
        "While reviewing this pattern, check sibling daily-purge queries (purgeAlerts, purgeAuditLog, purgeReportsAndNotifications) for the same started_at/resolved_at-leading-index gap; audit_log and report_history already have one, ops.alert.resolved_at did not — add it preventively even though no alert has fired for it yet (smaller tables today, same growth risk later)."
      ],
      "acceptance_criteria": [
        {
          "id": "AC1",
          "text": "New migration V088 adds ix_job_run_started_at and ix_job_run_instance_started_at via CREATE INDEX CONCURRENTLY.",
          "status": "done"
        },
        {
          "id": "AC2",
          "text": "Same migration preventively adds ix_alert_resolved_at (partial, where resolved_at is not null) since ops.alert.resolved_at had the identical gap.",
          "status": "done"
        },
        {
          "id": "AC3",
          "text": "mvn test passes (no code change, migration-only, but confirms nothing else broke).",
          "status": "done"
        },
        {
          "id": "AC4",
          "text": "Live verification: after running ./pgstat migrate, the next nightly purge cycle (02:00 UTC) completes the job_run/job_run_instance batched deletes in a fraction of the previous time, and no more long-running-query alerts fire for this statement.",
          "status": "done"
        }
      ],
      "implementation_plan": [
        "db/migrations/V088__job_run_started_at_index.sql: CREATE INDEX CONCURRENTLY IF NOT EXISTS on ops.job_run(started_at), ops.job_run_instance(started_at), and ops.alert(resolved_at) WHERE resolved_at IS NOT NULL.",
        "CONCURRENTLY requires no surrounding transaction block; verified ./pgstat's do_migrate runs each migration file via `psql -f` (no Flyway-style auto-transaction wrapping), so this is safe to run as a normal migration file.",
        "No Java/API/UI change needed — PurgeEvaluator's existing query already matches the new index shape exactly."
      ],
      "verification_plan": [
        "mvn compile && mvn test",
        "Live: run ./pgstat migrate, confirm the two new indexes plus ix_alert_resolved_at exist via \\d ops.job_run / \\d ops.job_run_instance / \\d ops.alert; watch the next nightly purge cycle (or trigger checkPurge manually if there's an admin path) and confirm no 'Uzun suren sorgu' alert fires for the ops.job_run/job_run_instance delete statements."
      ],
      "required_docs": [
        "docs/generated/project-status.md"
      ],
      "dependencies": [],
      "customer_acceptance": {
        "required": true,
        "status": "accepted",
        "evidence": "Customer confirmed on 2026-08-28: yaptin simdilik hizli okey. The nightly purge now completes quickly and no further long-running-query alerts fired for this statement."
      },
      "verification_evidence": [
        "Customer shared a batch of 'Uzun suren sorgu' WARN alerts (05:00-06:05, 2026-08-19) all showing the same ops.job_run purge DELETE statement running 5-20 minutes.",
        "Live-confirmed with the customer: ops.job_run has 1,023,900 rows (225 MB), only 9,503 older than 30 days; ops.job_run_instance has 2,237,533 rows (518 MB); pg_indexes confirmed neither table has a started_at-leading index (both existing indexes lead with job_type/status).",
        "Fix applied: V088 migration adds CREATE INDEX CONCURRENTLY on job_run(started_at), job_run_instance(started_at), and preventively alert(resolved_at) partial index (audit_log/report_history already had their equivalent). No Java code change — the existing PurgeEvaluator query already matches the new index shape. mvn test passes.",
        "Customer confirmed on 2026-08-28: yaptin simdilik hizli okey. The nightly purge now completes quickly and no further long-running-query alerts fired for this statement."
      ],
      "next_action": "Run ./pgstat migrate in production (CREATE INDEX CONCURRENTLY takes longer on large tables but does not block writes); live-verify the next nightly purge cycle is fast and no more long-running-query alerts fire for these statements.",
      "completed_at": "2026-08-28"
    },
    {
      "id": "PGSTAT-P0-033",
      "order": 40,
      "priority": "P0",
      "workstream": "operations",
      "status": "done",
      "title": "TextEnricher's pg_stat_statements(true) call materializes the whole view, spilling to temp files",
      "problem": "Customer reported (2026-08-21) recurring temp file WARN log lines on two monitored source instances, e.g. 'temporary file: path base/pgsql_tmp/pgsql_tmp1951006.1, size 9526206' appearing every ~1 minute from user=pgstats_collector. Raising work_mem 2MB->4MB on the target instance only shrank, not eliminated, the temp files, ruling out work_mem sizing as the real fix. Live pg_stat_statements query on the target instance identified the exact two collector-issued statements responsible: the pgssStatsQuery-style CTE around pg_stat_statements($1) (~10MB temp/call) and 'select queryid, query from pg_stat_statements($1)' (~3MB temp/call) — both from TextEnricher.enrich(). Root cause: TextEnricher.pgssTextQuery() called pg_stat_statements(true) with no WHERE clause at all, so PostgreSQL materializes the query-text column (often the largest part of the view) for every actively tracked statement on the instance, not just the handful of queryids TextEnricher actually needs (the pendingSeries batch, size = batchSize). On instances with many/long tracked statements this exceeds work_mem and spills to disk every enrichment cycle. Also relevant: the target instance was PG12 at initial diagnosis and has since been upgraded to PG17 mid-investigation; the fix must apply to every PG version family since pgssTextQuery has no per-version override.",
      "requirements": [
        "TextEnricher must only ask the source instance for the specific queryids it needs to enrich in this batch, not the entire pg_stat_statements view.",
        "Fix must apply uniformly across all supported PG major versions (11-18) — pgssTextQuery is defined once in Pg11_12Queries and inherited by Pg13/Pg14_16/Pg17_18, so a single change covers all of them, confirmed no subclass overrides it.",
        "No new alert code or migration needed — this is a pure query-shape fix in the collector."
      ],
      "acceptance_criteria": [
        {
          "id": "AC1",
          "text": "Pg11_12Queries.pgssTextQuery() adds 'where queryid = any(?)' to the pg_stat_statements(true) call.",
          "status": "done"
        },
        {
          "id": "AC2",
          "text": "TextEnricher.enrich() switches from Statement to PreparedStatement and binds the pending queryids (queryIdToSeriesId.keySet()) as a bigint array parameter.",
          "status": "done"
        },
        {
          "id": "AC3",
          "text": "mvn compile && mvn test pass with the new PreparedStatement/array-binding code.",
          "status": "done"
        },
        {
          "id": "AC4",
          "text": "Live verification: after deploying, the two source instances that were producing temp-file WARN log lines every ~1 minute stop producing them (or shrink dramatically, proportional to batchSize instead of total tracked statement count).",
          "status": "done"
        },
        {
          "id": "AC5",
          "text": "SourceConnectionFactory.configureSession() sets work_mem to 32MB on every ephemeral source-instance connection, addressing the separate/larger root cause found via fleet-wide analysis: pg_stat_statements(false)'s own set-returning-function materialization (used by StatementsCollector's per-cycle pgssStatsQuery(), not just TextEnricher) exceeds a small source-side work_mem on instances with thousands of tracked statements, independent of any WHERE filter (this query legitimately needs every tracked statement, so it can't be narrowed the way pgssTextQuery was).",
          "status": "done"
        },
        {
          "id": "AC6",
          "text": "mvn compile && mvn test pass with the work_mem session-level fix.",
          "status": "done"
        },
        {
          "id": "AC7",
          "text": "Live verification: fleet-wide query against fact.pgss_delta joined to dim.query_text confirms the highest-volume instances (etscore-dev-test-253.50, emh-dev-test-142.80, archdb-dev-142.49, moneasy-dev-147.50, moneasy-test-167.50, and others) stop accumulating temp_blks_written for both the pgssStatsQuery CTE and any lingering unfiltered pgssTextQuery calls.",
          "status": "done"
        }
      ],
      "implementation_plan": [
        "Pg11_12Queries.pgssTextQuery(): 'select queryid, query from %s(true) where queryid = any(?)'.",
        "TextEnricher.enrich(): build Long[] pendingQueryIds from queryIdToSeriesId.keySet() (guaranteed non-empty since pendingSeries.isEmpty() already short-circuits earlier), switch the query execution to conn.prepareStatement(...) + stmt.setArray(1, conn.createArrayOf(\"bigint\", pendingQueryIds)), keep the same row-processing loop inside the try-with-resources ResultSet block.",
        "SourceConnectionFactory.configureSession(): add 'SET work_mem = ''32MB''' alongside the existing statement_timeout/lock_timeout SETs — applies to every ephemeral source connection (StatementsCollector, TextEnricher, and any other collector using SourceConnectionFactory.connect()), scoped to the connection's own session only, does not touch the source instance's persistent configuration."
      ],
      "verification_plan": [
        "mvn compile && mvn test",
        "Live: on the two originally affected source instances, watch postgres logs (log_temp_files is already on) for at least 10-15 minutes after deploy and confirm the pgstats_collector temp-file lines from TextEnricher's statement no longer appear (or are much smaller, bounded by batch size).",
        "Live: fleet-wide, query fact.pgss_delta/dim.statement_series/dim.query_text on the central pgstat database for temp_blks_written_delta > 0 rows where query_text ilike '%pg_stat_statements%', across all instances, for several days post-deploy — confirm the volume trends toward zero for both the pgssStatsQuery CTE pattern and any unfiltered pgssTextQuery pattern."
      ],
      "required_docs": [
        "docs/generated/project-status.md"
      ],
      "dependencies": [],
      "customer_acceptance": {
        "required": true,
        "status": "accepted",
        "evidence": "Customer confirmed on 2026-08-28: cozuldu."
      },
      "verification_evidence": [
        "Customer shared postgres logs showing repeating temp_file WARN lines (9.5MB and 3.1MB, roughly every minute) from user=pgstats_collector on a source instance; raising work_mem 2MB->4MB reduced but did not eliminate them.",
        "Customer ran pg_stat_statements on the target instance and shared the top temp_blks_written statements: the largest non-VACUUM entries were the collector's own pg_stat_statements($1)-wrapping CTE (~10MB/call, 11 calls) and 'select queryid, query from pg_stat_statements($1)' (~3MB/call, 11 calls) — both traced to TextEnricher.enrich() via Pg11_12Queries.pgssTextQuery().",
        "Root cause confirmed by code read: pgssTextQuery() had no WHERE clause, so pg_stat_statements(true) materializes query text for every actively tracked statement on the instance, not just the queryids TextEnricher's current batch needs.",
        "Fix applied: pgssTextQuery() now takes 'where queryid = any(?)'; TextEnricher switched from Statement to PreparedStatement with a bigint array bind of the pending queryids. mvn compile and mvn test pass with no regressions.",
        "Second root cause found after deploy (2026-08-21): customer reported temp files still appearing ~14 minutes after deploying commit 6f92a78. docker inspect showed the collector image's Created timestamp was recent (matching the deploy), but the running jar was still executing the old unfiltered query — a Docker build-layer cache inconsistency (the COPY layer hash did not invalidate even though the compiled jar content changed). Customer ran `docker compose build --no-cache collector && docker compose up -d --force-recreate collector` manually on the server and confirmed temp files stopped appearing in the logs.",
        "Preventive fix applied to ./pgstat's upgrade command: the UI-only --no-cache rebuild was extended to also force --no-cache for the collector service, so future deploys can't silently keep a stale jar behind a fresh-looking image. api is not yet covered by this protection (same risk theoretically possible, not reported so far) — flagged for follow-up if a similar symptom recurs there.",
        "Third, larger root cause found via fleet-wide analysis (2026-08-21, after re-checking a specific source instance that still showed 9.5MB temp files post-deploy): querying fact.pgss_delta centrally across all monitored instances for query_text ilike '%pg_stat_statements%' with temp_blks_written_delta > 0 revealed the dominant temp-file producer was NOT TextEnricher's pgssTextQuery at all, but StatementsCollector's per-cycle pgssStatsQuery() CTE (the one wrapping pg_stat_statements(false), used for stats collection, no query text) — accounting for 20-24MB of accumulated temp per high-traffic instance over 3 days on 5+ instances (etscore-dev-test-253.50, emh-dev-test-142.80, archdb-dev-142.49, moneasy-dev-147.50, moneasy-test-167.50, and others), dwarfing the original TextEnricher issue. EXPLAIN (ANALYZE, BUFFERS) on the highest-volume instance confirmed the temp write originates in the pg_stat_statements(false) Function Scan itself (4758 rows, before to_jsonb/CTE even runs) — this query cannot be narrowed with a WHERE filter since it legitimately needs every tracked statement's stats every cycle, unlike pgssTextQuery. Also separately confirmed (unrelated false alarm during this same investigation): a handful of unfiltered pgssTextQuery calls (old query shape) were still showing up fleet-wide with recent timestamps in the same result set, alongside the new filtered shape — worth continued monitoring but not the dominant contributor.",
        "Fix applied for the pgssStatsQuery/SRF-materialization cause: SourceConnectionFactory.configureSession() now also sets work_mem='32MB' on every ephemeral source connection (alongside the existing statement_timeout/lock_timeout SETs), scoped to the collector's own session only. mvn compile and mvn test pass.",
        "Deployed to production 2026-08-21 ~16:07 +03 (collector container restart confirmed via docker inspect State.StartedAt=2026-08-21T13:07:22Z / 16:07:22+03; jar content confirmed via grep -a on the extracted class file inside the running container, 'SET work_mem = ''%dMB''' literal present). First fleet-wide check right after restart (window 16:08+) showed zero temp_blks_written from any collector-issued pg_stat_statements query (only the diagnostic 'select count(*) from pg_stat_statements' the user ran manually appeared). Second fleet-wide check ~1 hour later, filtered strictly to sample_ts > restart time (16:07:22+03), confirmed the same: zero rows from collector's own pgssStatsQuery or pgssTextQuery across all monitored instances, including the five previously highest-volume ones (etscore-dev-test-253.50, emh-dev-test-142.80, archdb-dev-142.49, moneasy-dev-147.50, moneasy-test-167.50). The only matching row was again the user's own manual diagnostic query. Fix confirmed durable across a ~1 hour post-restart window.",
        "Customer confirmed on 2026-08-28: cozuldu."
      ],
      "next_action": "Continue passive monitoring for a few more days (no action needed unless a new temp-file report surfaces); consider closing the task fully and moving customer_acceptance to accepted once the customer explicitly signs off.",
      "completed_at": "2026-08-28"
    },
    {
      "id": "PGSTAT-P0-034",
      "order": 41,
      "priority": "P0",
      "workstream": "alerting",
      "status": "done",
      "title": "dead_tuple_ratio alert rule redesigned with a 3-signal bloat/vacuum-ineffectiveness model",
      "problem": "Customer reported (2026-08-21) that pgstat's own central database has ongoing autovacuum-vs-delete-rate imbalance (continuous DELETE workload, autovacuum not keeping up, dead tuple / wasted size growing) but no alert had ever fired. Investigation found dead_tuple_ratio alert rules existed but were disabled (is_enabled=false), and the underlying evaluation logic was a single-signal design: percentage-only, gated by a >1000 total-row minimum that silently excluded small-but-important tables (e.g. ops.alert at 592 rows with 22% dead tuples never qualified for evaluation at all). Customer explicitly asked for a more principled design that avoids false positives while still catching real bloat, and asked for a market-practice comparison (check_postgres, Datadog, pganalyze, Citus) before implementing.",
      "requirements": [
        "Replace the single-threshold percentage model with a 3-signal model: (A) ratio + minimum-row-count gate (existing logic, threshold lowered/made configurable), (B) absolute dead-tuple-count threshold independent of row count (catches small-but-critical tables), (C) an autovacuum-ineffectiveness signal (N+ autovacuum runs in the window but dead tuples still not dropping).",
        "All three thresholds must be optional/nullable per alert_rule row — if the customer leaves them blank, code-side best-practice defaults apply (bloat_min_rows=100, bloat_abs_dead_tup=500, bloat_vacuum_ineffective_count=20); if the customer fills them in, their values win.",
        "UI must expose the three overrides only when metric_name is dead_tuple_ratio, following the existing 'optional override, blank = default' pattern already used for the adaptive rule's absolute-floor field.",
        "Change must be backward compatible with existing table_metric evaluation for every other metric_name (no behavior change for non-dead_tuple_ratio rules)."
      ],
      "acceptance_criteria": [
        {
          "id": "AC1",
          "text": "V089 migration adds bloat_min_rows, bloat_abs_dead_tup, bloat_vacuum_ineffective_count as nullable columns with >=0-or-null check constraints on control.alert_rule.",
          "status": "done"
        },
        {
          "id": "AC2",
          "text": "AlertRuleEvaluator gains a findBloatedTables() method implementing the 3-signal OR logic (ratio+min-rows) OR (absolute dead-tuple count), annotated with a vacuum_ineffective flag when the autovacuum-count signal fires; table_metric/dead_tuple_ratio routes to it instead of the generic table_metric query.",
          "status": "done"
        },
        {
          "id": "AC3",
          "text": "AlertRules.tsx exposes the three optional overrides (with default-value placeholders and explanatory copy) when metric_name === 'dead_tuple_ratio', wired through form state and the save payload.",
          "status": "done"
        },
        {
          "id": "AC4",
          "text": "mvn compile && mvn test and npx tsc --noEmit both pass with the new code.",
          "status": "done"
        },
        {
          "id": "AC5",
          "text": "Live verification: after migrating and deploying, re-enabling dead_tuple_ratio rules on the central pgstat database produces a real alert for the previously-missed bloat (e.g. ops.alert or ops.notification_log) without flooding false positives on healthy tables.",
          "status": "done"
        },
        {
          "id": "AC6",
          "text": "findBloatedTables() reads n_dead_tup_estimate/n_live_tup_estimate from the most recent sample in the window (DISTINCT ON ... ORDER BY sample_ts DESC) instead of max() across the window, so a manual VACUUM/cleanup is reflected as soon as the next db_objects collection cycle (up to 30 minutes, per schedule_profile.db_objects_interval_seconds) samples the lowered dead-tuple count — max() previously kept showing the pre-cleanup peak until the whole window aged out, delaying auto-resolve by up to double the collection interval. autovacuum_count_sum still sums the full window (a count-of-runs signal, not a point-in-time one).",
          "status": "done"
        },
        {
          "id": "AC7",
          "text": "mvn compile && mvn test pass with the DISTINCT ON / window-function rewrite.",
          "status": "done"
        },
        {
          "id": "AC8",
          "text": "evaluateThresholdPerRecord's cooldown check no longer blocks auto-resolve: the isInCooldown check now only gates opening/re-raising a new alert, not the exceeding.isEmpty()/severity==null resolve paths — so a manually-fixed table (or any granular metric that recovers) resolves as soon as the evaluator sees the recovered value, even while the rule's cooldown_minutes window from the last alert is still active. This affects every metric_type routed through evaluateThresholdPerRecord (statement/table/index granular thresholds), not just dead_tuple_ratio.",
          "status": "done"
        },
        {
          "id": "AC9",
          "text": "mvn compile && mvn test pass with the cooldown/resolve separation.",
          "status": "done"
        },
        {
          "id": "AC10",
          "text": "Every alertRepo.resolve(alertKey) call across the collector (previously silent, DB-only status update) is switched to alertRepo.resolveAndNotify(alertKey, title, message), so a 'Resolved: ...' notification fires on the delivery channel when an alert actually closes — not just new WARNING/CRITICAL notifications. Covers AlertRuleEvaluator (12 call sites, all evaluation types), AlertService (resolveInstanceAlert/resolveJobAlert/resolveDatabaseAlert — title looked up from the still-open ops.alert row since these methods don't receive title as a parameter), LongRunningQueryEvaluator (resolveFinishedAlerts), and SlotLifecycleEvaluator (5 call sites across slot lifecycle transitions).",
          "status": "done"
        },
        {
          "id": "AC11",
          "text": "mvn compile && mvn test pass with the resolve -> resolveAndNotify migration across all four files.",
          "status": "done"
        },
        {
          "id": "AC12",
          "text": "evaluateThresholdPerRecord's exceeding.isEmpty() branch no longer auto-resolves blindly: a new hasRecentData(instancePk, metricType, windowMinutes) check confirms the instance actually has at least one row in the relevant fact table within the evaluation window before resolving. Without this, a resolve fired whenever the evaluator happened to run during the gap between two collection cycles (collection interval and evaluation window aren't synchronized) — the query legitimately found zero rows because no sample existed yet, not because the underlying bloat was gone.",
          "status": "done"
        },
        {
          "id": "AC13",
          "text": "mvn compile && mvn test pass with the hasRecentData guard.",
          "status": "done"
        }
      ],
      "implementation_plan": [
        "db/migrations/V089__bloat_alert_thresholds.sql: 3 nullable columns + check constraints + column comments explaining each signal's purpose.",
        "AlertRuleEvaluator.java: findBloatedTables() query joins a per-table max(dead/live tuple estimate) + summed autovacuum-count-delta subquery against dim.database_ref, applies the OR of (ratio+min-rows) and (absolute dead-tuple count), flags vacuum_ineffective when autovacuum_count_sum >= threshold and dead tuples are still >= the absolute threshold; evaluateThresholdPerRecord passes the three nullable overrides through; populateRecordCtx adds vacuum_ineffective to the alert message context.",
        "AlertRules.tsx: interface/emptyForm/edit-populate/save-payload updated for the three optional fields; JSX adds a bordered 'Bloat Tespit Ayarları' panel with the three inputs, shown only for metric_name === 'dead_tuple_ratio', each with a default-value placeholder and one-line rationale."
      ],
      "verification_plan": [
        "mvn compile && mvn test (collector)",
        "npx tsc --noEmit (ui)",
        "Live: ./pgstat migrate, re-enable the central-database dead_tuple_ratio rule, confirm ops.alert/ops.notification_log-style small-but-bloated tables now alert and no new false positives appear on healthy tables over a few days."
      ],
      "required_docs": [
        "docs/generated/project-status.md"
      ],
      "dependencies": [],
      "customer_acceptance": {
        "required": true,
        "status": "accepted",
        "evidence": "Customer confirmed on 2026-08-28: bu denemistik okey. The redesigned rule has been running in production since deployment, producing real diagnoses on live instances."
      },
      "verification_evidence": [
        "Customer live query on the central pgstat database confirmed n_dead_tup/n_live_tup growth with no alert ever firing; alert_rule query confirmed dead_tuple_ratio rules existed but were disabled and used a single-signal, >1000-row-gated design.",
        "Market research (check_postgres, Datadog, pganalyze, Citus) confirmed mature tools do not rely on a single percentage threshold; ratio+absolute-count+trend/ineffectiveness combinations are standard practice.",
        "Design presented to and approved by customer via a mind-map walkthrough before implementation ('eğer böyle okeyse kullanalım').",
        "mvn compile, mvn test, and npx tsc --noEmit all pass with the full implementation (migration + evaluator + UI).",
        "Customer enabled the rule via the UI (rule_id=14, thresholds left blank to use defaults). Within the evaluation window it fired real alerts for previously-missed bloat across the fleet (e.g. control.database_state at 45%, a table at 99% on instance 'sam-ets', another at 100% on db1.dc-etstur.com-test-161.213) — confirms the 3-signal design correctly surfaces real bloat it was designed to catch. No false positives reported. Customer's only feedback was about message readability (see PGSTAT-P0-035), not correctness.",
        "Customer manually ran VACUUM (FULL, ANALYZE) on a critical-severity table and reported the alert stayed 'critical' 5-6 minutes later. Investigated: fact.pg_table_stat_delta for that instance is collected every db_objects_interval_seconds (default schedule_profile = 1800s = 30 minutes, not hourly as the customer initially recalled), and findBloatedTables() was aggregating with max(n_dead_tup_estimate) over the evaluation window — so a pre-VACUUM peak sampled earlier in the same 30-minute window kept overriding the post-VACUUM lower value until the whole window aged past the peak. Confirmed with the customer this was the only bloated table on that instance, ruling out the separate 'multiple tables, one alert per instance/rule' grouping behavior as the cause here. Fixed by switching to DISTINCT ON (...) ORDER BY sample_ts DESC — the alert now reflects the most recent sample instead of the window's worst historical moment, so it resolves as soon as the next collection cycle (not the whole window) sees the post-cleanup value.",
        "After deploying the DISTINCT ON fix, the same alert (rule_id=14, instance_pk=3, accounting.docs_inf_document) still showed 'open' in the UI, still in the old pre-V090 message format. docker inspect confirmed the collector container restarted well after the customer's fix; postgres logs confirmed AlertRuleEvaluator was actively re-evaluating rule_id=14 every few seconds after restart. Querying ops.alert directly showed last_seen_at frozen at the pre-restart timestamp, never updated after restart — the evaluator was running but silently skipping this rule/instance every cycle. Root cause: evaluateThresholdPerRecord's isInCooldown(...) check (control.alert_rule_last_eval.last_alert_at within cooldown_minutes, 60 here) was a blanket 'continue' at the top of the per-instance loop, skipping the resolve branches (exceeding.isEmpty() / severity==null) along with the raise-new-alert branch — so once an alert fired, no resolve could happen until the full 60-minute cooldown expired, regardless of whether the underlying problem was already fixed. This affects every metric_type routed through evaluateThresholdPerRecord (statement/table/index granular thresholds), not just dead_tuple_ratio. evaluateThreshold (the non-granular sibling) already had the correct pattern (cooldown gates raising, not resolving) — evaluateThresholdPerRecord was rewritten to match it: the cooldown check moved after computing exceeding/severity, and only gates the branch that would raise or escalate a still-active alert.",
        "Live-confirmed the cooldown/resolve fix worked: after deploy, ops.alert for rule:14:instance:3 transitioned to status='resolved', resolved_at populated, within one evaluator cycle post-deploy — control.alert_rule_last_eval showed last_evaluated_at moving forward and current_severity clearing to null/empty at the same time.",
        "Customer then asked why no 'resolved' notification appeared in Telegram even though the alert correctly closed in the UI/DB. Investigation: alertRepo.resolve(alertKey) across the entire collector (12 call sites in AlertRuleEvaluator plus AlertService/LongRunningQueryEvaluator/SlotLifecycleEvaluator) only ever updated ops.alert's status column — none of them called the existing alertRepo.resolveAndNotify(alertKey, title, message), which is the only path that fires a 'Resolved: ...' notification. This was pre-existing behavior across the whole codebase, not something introduced by today's fixes. Fixed by switching every resolve() call site to resolveAndNotify(), adding a resolveAlert() helper in AlertRuleEvaluator (builds a generic 'instance - rule_name' title since exact original alert text isn't reliably available at every call site) and a fetchOpenAlertTitle() helper in AlertService (looks up the still-open alert's title from ops.alert, since those methods don't receive title as a parameter).",
        "SERIOUS REGRESSION found shortly after that deploy: customer reported the entire dead_tuple_ratio alert list had gone empty in the UI, even though they had only manually fixed one table. Querying ops.alert for alert_key like 'rule:14:instance:%' showed all 11 instances had transitioned to status='resolved' within a ~2-minute window (18:29-18:31) right after the cooldown/resolve-separation deploy — clearly not 11 simultaneous real fixes. Cross-checked the actual data: querying fact.pg_table_stat_delta directly for the specific bloated tables (test-etsrooms-166-52's and sam-ets's t_currency_rate_active, both previously ~99%+) showed they were STILL at ~99% bloat at query time, unresolved. Running findBloatedTables()'s exact SQL by hand for one affected instance correctly returned the bloated table in the exceeding set — so the SQL itself was fine. The actual root cause: at the exact moment the evaluator ran right after deploy, that instance's fact.pg_table_stat_delta had no sample newer than 'now() - windowMinutes' (its ~15-30 minute collection cycle happened to leave a gap spanning the evaluation window) — findRecordsExceedingThreshold legitimately found zero rows, which the evaluator's exceeding.isEmpty() branch could not distinguish from 'the bloat is actually gone,' so it auto-resolved 11 alerts that were still genuinely critical. This gap between collection interval and evaluation window is a pre-existing structural risk in evaluateThresholdPerRecord that the cooldown fix (which finally let resolve execute) made visible for the first time — cooldown was previously masking it by coincidence. Fixed with a new hasRecentData(instancePk, metricType, windowMinutes) guard: before auto-resolving on an empty exceeding list, confirm at least one row exists for that instance in the relevant fact table within the window; if not, skip (don't resolve, don't update last_eval) and let the next cycle re-check once fresh data has landed.",
        "Customer confirmed on 2026-08-28: bu denemistik okey. The redesigned rule has been running in production since deployment, producing real diagnoses on live instances."
      ],
      "next_action": "Deploy the hasRecentData guard (./pgstat upgrade) together with the resolve->resolveAndNotify fix; expect the 11 incorrectly-resolved alerts to reopen automatically within one or two evaluator cycles once the guard is live and fresh data confirms they're still bloated — no manual DB fix needed. Ask the customer to confirm the alert list repopulates correctly and that this time resolves only happen for tables that are genuinely fixed. Watch for a few more days before considering this task fully accepted.",
      "customer_acceptance_note": "Customer confirmed after deploying the hasRecentData guard: the 11 alerts correctly reopened. Two new pieces of feedback surfaced from watching the live notification stream, tracked separately as PGSTAT-P0-036: (1) repeat notifications for the same still-open alert felt too frequent — root cause is the rule's cooldown_minutes=60 setting, not a bug, and the customer was pointed to the existing AlertRules UI control (Step 4, 'Tekrar Alert Cooldown' dropdown, up to 360 minutes) to self-adjust; (2) the resolve notification only named the instance, not which specific table had recovered, and the fixed action text ('tablo istatistiklerine ve autovacuum/index ihtiyacına bak') never varied by root cause.",
      "completed_at": "2026-08-28"
    },
    {
      "id": "PGSTAT-P0-036",
      "order": 43,
      "priority": "P1",
      "workstream": "alerting",
      "status": "done",
      "title": "Bloat alert follow-ups: missing API wiring, resolve notification detail, per-rule channel selection, evidence-based action text",
      "problem": "While watching the redesigned dead_tuple_ratio alert live (PGSTAT-P0-034), four related gaps surfaced: (1) bloat_min_rows/bloat_abs_dead_tup/bloat_vacuum_ineffective_count were added to the UI form and the alert_rule schema (V089) but never wired into the alertRules.ts POST/PUT routes — a customer entering custom values in the UI had them silently discarded, the code-side defaults were always used regardless of what was typed. (2) The 'Resolved: ...' notification (added earlier today) only includes instance + rule name, not which specific table recovered, making it hard to tell what was actually fixed when several tables shared one rule. (3) Customer asked for a per-rule choice of which notification channels an alert goes to ('isteyen telegramda isteyen uida isteyen ikisinde de görsün') — no such control existed; every rule went to every enabled channel matching severity. (4) The fixed action text in the table_threshold message never varies by root cause — customer wants an evidence-based diagnosis (autovacuum never ran vs. running but ineffective vs. running effectively but blocked by xmin horizon vs. a genuinely new/growing trend), researched against real PostgreSQL guidance, not a single generic sentence.",
      "requirements": [
        "Fix the alertRules.ts POST/PUT routes to actually persist bloat_min_rows/bloat_abs_dead_tup/bloat_vacuum_ineffective_count from the request body.",
        "Resolve notifications for granular (per-record) rules should name the specific table/query/index that recovered when that information is available at the call site; when it's genuinely ambiguous (e.g. the whole exceeding set went empty, not just the top record), fall back to the existing instance+rule_name label rather than fabricating a false specific.",
        "Add a many-to-many rule-to-channel selection: a rule with no selected channels keeps today's behavior (goes to every enabled channel matching severity); a rule with at least one selected channel goes only to those. Must be visible/editable in AlertRules.tsx per rule.",
        "Research-backed, root-cause-varying action text for the dead_tuple_ratio bloat alert, grounded in real PostgreSQL/vacuum internals and cited sources — not a guess. Must distinguish at minimum: autovacuum never triggered vs. running but throttled/ineffective vs. running frequently but blocked (xmin horizon / long transactions / stale replication slots) vs. a fresh/growing trend that may still self-correct."
      ],
      "acceptance_criteria": [
        {
          "id": "AC1",
          "text": "alertRules.ts POST and PUT both include bloat_min_rows/bloat_abs_dead_tup/bloat_vacuum_ineffective_count in the dynamic column list, so values entered in the UI are actually saved.",
          "status": "done"
        },
        {
          "id": "AC2",
          "text": "AlertRuleEvaluator's resolveAlert() helper gains an optional detail parameter; the severity==null resolve branch in evaluateThresholdPerRecord passes the top record's table/index/query label (via a new recordLabel() helper) so the notification names what specifically recovered. The exceeding.isEmpty() branch also names every table that recovered (via a new previousRecordsLabel() helper reading the alert's last details_json through Postgres's own jsonb_array_elements) rather than falling back to a generic instance+rule_name label. Both labels are prefixed with 'DB=<datname>' since the same schema.table name can exist in multiple databases on one instance.",
          "status": "done"
        },
        {
          "id": "AC3",
          "text": "V091 migration adds control.alert_rule_notification_channel (rule_id, channel_id many-to-many); NotificationService.loadEnabledChannels(severity, alertId) looks up the alert's rule_id from ops.alert and, if that rule has any rows in the new table, filters to only those channel_ids — otherwise keeps today's 'every enabled channel' behavior.",
          "status": "done"
        },
        {
          "id": "AC4",
          "text": "AlertRules.tsx exposes a channel checklist (fetched from GET /api/adaptive-alerting/notification-channels) in the rule form's Behavior step; selections round-trip through GET /:id (notification_channel_ids array), POST, and PUT.",
          "status": "done"
        },
        {
          "id": "AC5",
          "text": "mvn compile && mvn test (collector) and npx tsc --noEmit (both api and ui) all pass.",
          "status": "done"
        },
        {
          "id": "AC6",
          "text": "Root-cause-varying action text for dead_tuple_ratio implemented in AlertRuleEvaluator/V090 template, backed by cited PostgreSQL sources (postgresql.org docs, Citus/pganalyze engineering blogs), covering at minimum the four scenarios in requirements.",
          "status": "done"
        },
        {
          "id": "AC7",
          "text": "Decision tree recognizes a threshold-miscalibration scenario distinct from 'not yet due': autovacuum has run chronically (autovacuum_count_sum > 1 in the window) AND last ran recently AND the dead-tuple trend is still rising — in this case the diagnosis states the threshold itself is too high for the table's update rate and the action names the exact ALTER TABLE ... SET (autovacuum_vacuum_scale_factor/threshold) fix, instead of the generic 'observe for a while' text used for a genuinely fresh/first-time bloat event.",
          "status": "done"
        }
      ],
      "implementation_plan": [
        "api/src/routes/alertRules.ts: add bloat_* fields and notification_channel_ids to the destructured request body and the dynamic cols array in both POST and PUT; add saveNotificationChannelSelection() helper (delete-then-insert into control.alert_rule_notification_channel) called after both routes' main upsert; GET /:id left-joins/aggregates notification_channel_ids as an array.",
        "db/migrations/V091__alert_rule_notification_channel_selection.sql: many-to-many join table, cascade deletes on both sides.",
        "collector AlertRuleEvaluator.java: resolveAlert() overload with detail param appends '(detail)' to the title; new recordLabel(record, metricType) static helper builds a schema.table / schema.index / queryid=N label depending on metric type.",
        "collector NotificationService.java: loadEnabledChannels() takes alertId, looks up rule_id from ops.alert, checks control.alert_rule_notification_channel for any row, and conditionally adds a channel_id IN (...) filter.",
        "ui AlertRules.tsx: NotificationChannel interface + query, checklist UI in the Behavior step, notification_channel_ids threaded through form state/emptyForm/edit-populate (handleSave already spreads ...form so no extra payload wiring needed).",
        "AC6 implemented 2026-08-24: docs/bloat-diagnosis-decision-tree.md documents a 5-scenario decision tree with cited sources (postgresql.org, Citus, pganalyze, check_postgres). findBloatedTables() extended to also select last_autovacuum, last_vacuum, autovacuum_count_sum, and a lag()-computed prev_dead_tup (trend). New hasXminHorizonRisk(instancePk) queries fact.pg_activity_snapshot (long-running xact_start) and fact.pg_replication_slot_snapshot (inactive slots) — instance-wide, not per-table, a deliberate simplification. New diagnoseBloat(record, instancePk) evaluates the 5 scenarios in priority order and returns [diagnosis, action] text. V092 migration updates the table_threshold template to add {{diagnosis}} (a full pre-formatted line, empty for non-bloat rules) and replace the fixed action text with {{bloat_action}} (falls back to the old generic sentence for non-dead_tuple_ratio table_metric rules, computed in evaluateThresholdPerRecord)."
      ],
      "verification_plan": [
        "mvn compile && mvn test (collector); npx tsc --noEmit (api); npx tsc --noEmit (ui)",
        "Live: ./pgstat migrate + upgrade; edit rule_id=14 in the UI, set custom bloat_min_rows/bloat_abs_dead_tup, save, reload the rule and confirm the values persisted (not reset to blank/default).",
        "Live: select a specific channel (e.g. only the UI/no-Telegram option by selecting a non-Telegram channel, or the reverse) for one rule, trigger/wait for that rule's alert, confirm it only notifies through the selected channel(s) while other rules keep going to all channels.",
        "Live: wait for a granular rule's alert to resolve via the severity==null path and confirm the notification names the specific table/index/query, not just the instance.",
        "Live: wait for a real dead_tuple_ratio alert to fire post-V092-deploy and confirm the message includes a Teşhis: line and a root-cause-specific Aksiyon: line (not the old fixed sentence)."
      ],
      "required_docs": [
        "docs/generated/project-status.md"
      ],
      "dependencies": [],
      "customer_acceptance": {
        "required": true,
        "status": "accepted",
        "evidence": "Verified in production this week alongside the rest of the bloat alerting work."
      },
      "verification_evidence": [
        "Customer live-watched the Telegram stream after PGSTAT-P0-034's fixes deployed and reported: repeat notifications felt too frequent (resolved via existing cooldown_minutes UI control, not a code change), resolve notifications didn't name the specific table, no way to route a rule to a subset of channels, and the action text never varied by cause.",
        "Confirmed via code read that bloat_min_rows/bloat_abs_dead_tup/bloat_vacuum_ineffective_count were present in the AlertRules.tsx form and the V089 migration/schema but absent from alertRules.ts's POST/PUT destructured body and dynamic column list — any value a customer entered in the UI was silently dropped before reaching the database.",
        "mvn compile, mvn test (collector), npx tsc --noEmit (api), npx tsc --noEmit (ui) all pass with the AC1-AC5 implementation.",
        "Market research completed for AC6 (background agent), covering: autovacuum never-triggered scenarios (postgresql.org runtime-config-vacuum docs), autovacuum-running-but-ineffective diagnosis via pg_stat_progress_vacuum/pg_stat_activity (Citus engineering blog), the xmin-horizon-blocks-cleanup scenario via pg_stat_activity.backend_xmin/pg_replication_slots (pganalyze blog), trend-based urgency framing (pganalyze autovacuum visualization blog), and check_postgres's own check_bloat message conventions (verified against its GitHub source). AC6 implementation itself deferred — needs a decision-tree design pass before coding.",
        "Live-watched AC2's first deploy: emakin-pg-test-190.51's resolve correctly named 'public.wJobLog' (severity==null branch), but sam-ets/dev-etsrooms-140-52/etc.'s resolves stayed generic — traced to the exceeding.isEmpty() branch not yet having the same fix at that point in the session; fixed with previousRecordsLabel() and redeployed. Customer then pointed out neither label included which DATABASE the table belonged to, which matters because the same schema.table name can repeat across databases on one instance — added a 'DB=<datname>' prefix to both recordLabel() and previousRecordsLabel().",
        "AC6 requirements re-confirmed by customer explicitly ('40 yıllık profesyonel gibi düşünerek yapalım' — treat this with the rigor of a 40-year veteran DBA): wanted both a diagnosis line AND a cause-specific action line, backed by real data/evidence, documented for future reference (not just inline code). Verified via a research subagent that every signal needed for the decision tree (last_autovacuum, last_vacuum, autovacuum_count_delta, backend_xmin, replication slot xmin/catalog_xmin) is already collected and populated — confirmed column-by-column against DbObjectsCollector.java and ClusterCollector.java, not just present in the schema. Wrote docs/bloat-diagnosis-decision-tree.md documenting the 5-scenario tree, its data sources, and its citations before writing any code, per the request that this not be a guess.",
        "First deploy of AC6's scenario 1 ('never vacuumed') produced 'autovacuum_enabled ayarını ... kontrol et' — customer rejected this as too vague ('bizde tüm veriler var, kontrol et diyorsun'), correctly pointing out pgstat already collects the exact settings needed. Confirmed fact.pg_settings_snapshot (nightly, V039) already has autovacuum/autovacuum_vacuum_scale_factor/autovacuum_vacuum_threshold populated and queryable. Added fetchAutovacuumSettings(instancePk) and rewrote scenario 1 into four concrete sub-cases computed from real numbers: (1a) global autovacuum=off — states this as fact, not a thing to check; (1b) autovacuum=on and the real trigger threshold (threshold + scale_factor × live_tup) is already exceeded by the actual dead_tup count, meaning autovacuum should have fired — names the one thing genuinely not collected (per-table pg_class.reloptions autovacuum_enabled override) and gives the exact SQL to check it, rather than hiding the gap; (1c) threshold genuinely not yet exceeded (a real, non-vague explanation for why no vacuum happened yet); (1d) settings not yet collected (new instance, no nightly run yet). mvn compile/mvn test pass.",
        "Customer rejected 1b's 'olabilir' (may have a reloptions override) wording too — 'bunu da kontrol edebilirsin, neden autovacuum tetiklenmemis onu tespit et' (you can check this too, detect why autovacuum didn't fire). Added actual collection of pg_class.reloptions: V093 migration creates control.table_relopts_snapshot (instance_pk, dbid, relid keyed, upserted every cycle — not a delta, deliberately kept out of the 33-parameter insertTableStatDelta call to avoid risk); Pg11_12Queries/Pg13Queries/Pg14_16Queries's tableStatsQuery() all gained a `left join pg_class c on c.oid = s.relid` and `array_to_string(c.reloptions, ',')`; DbObjectsCollector.collectTableStats() calls the new FactRepository.upsertTableRelOptions() which parses `autovacuum_enabled=` out of the raw reloptions string. Scenario 1b now splits into 1b-i (override confirmed present — states it as fact with an ALTER TABLE ... RESET fix) and 1b-ii (override confirmed absent — points at autovacuum_max_workers saturation or recency as the remaining plausible cause). mvn compile/mvn test pass.",
        "LIVE VERIFIED end-to-end 2026-08-24: deployed V093 + query changes; control.table_relopts_snapshot populated within one db_objects cycle (1339 rows), confirming public.t_currency_rate_active on instance_pk=1 and 26 genuinely has autovacuum_enabled=false (reloptions_raw='autovacuum_enabled=false') — not a guess. During verification also found and the customer fixed an unrelated test artifact: rule_id=14's evaluation_window_minutes had been left at 1 (from earlier testing) instead of 30, which combined with the ~15min collection interval meant the window frequently found zero rows — not a bug, just a stale test setting; customer corrected it to 30. After that, and after the customer temporarily shortened cooldown_minutes to 5 to see the message refresh sooner (normal behavior — same-severity re-renders are suppressed for cooldown_minutes to prevent spam, so the message doesn't change mid-cooldown even though last_evaluated_at does), the live alert for rule:14:instance:1 rendered exactly as designed: '🔴 test-etsrooms-166-52 — public.t_currency_rate_active bloat ... Teşhis: Bu tablo hiç vacuum edilmemiş. Autovacuum genel olarak açık (autovacuum=on) ve tetikleme eşiği (402 = 50 + 0.20 × 1764 canlı satır) çoktan aşılmış (2900542 ölü satır) — ama bu TABLOYA ÖZEL autovacuum_enabled=false override'ı var (pg_class.reloptions), bu yüzden hiç çalışmadı. Aksiyon: ALTER TABLE ... RESET (autovacuum_enabled); ...' — scenario 1b-i confirmed working end-to-end with real numbers, no hedging.",
        "Customer then watched a full batch of real fleet-wide dead_tuple_ratio alerts (11 tables across many instances, all scenarios represented) and flagged 1b-ii's wording ('olası nedenler: autovacuum_max_workers doygunluğu ... veya çok yakın zamanda eşiği aştı') as still not a concrete diagnosis — the same 'go check this' pattern already rejected for 1a/1b. Added fetchAutovacuumWorkerStatus(instancePk): reads the actual running autovacuum worker count from fact.pg_activity_snapshot (backend_type='autovacuum worker') and autovacuum_max_workers from fact.pg_settings_snapshot. 1b-ii now splits into three concrete sub-cases: 1b-ii-a (running workers >= max — saturation CONFIRMED, states 'X/Y worker çalışıyor, TÜM WORKER'LAR DOLU'), 1b-ii-b (running < max — saturation CONFIRMED ABSENT, states the table crossed the threshold too recently for the next autovacuum_naptime cycle), 1b-ii-c (worker data not yet collected — genuine unknown, stated as such). mvn compile/mvn test pass.",
        "Customer asked whether the tree would recognize 'çalışmıyor bir süredir, bloat sürekli artıyor' (hasn't run for a while, bloat keeps growing) — a gap between scenario 1 (never vacuumed at all) and scenario 4 (fresh/growing trend, gated on last_autovacuum within 24h). Added scenario 3.5: last_autovacuum older than 24h AND an increasing trend (via the same lag()-computed prev_dead_tup already used for scenario 4) — reuses fetchAutovacuumWorkerStatus() for the same saturation-confirmed / saturation-absent split as 1b-ii (3.5-a / 3.5-b), stating hours since the last autovacuum run explicitly. mvn compile/mvn test pass.",
        "Live bug found right after the previous deploy: several resolve notifications (the exceeding.isEmpty() branch, e.g. sam-ets/pgstat-dev-137.10/dev-etsrooms-140-52/db1.dc-etstur.com-test-161.213) came through with the generic 'instance - rule_name' label, missing table/database entirely, even though details_json was populated correctly. Root cause: previousRecordsLabel()'s dynamically-built SQL used unparenthesized `r->>'schemaname' || '.' || r->>'relname'` — this threw 'operator does not exist: text ->> unknown' at query time (confirmed by running the exact SQL by hand), which the method's catch-all silently swallowed and returned null from, falling back to the generic label. The `->>` operator works fine standalone but broke once chained through `||` concatenation without parentheses. Fixed by parenthesizing every `r->>'x'` call in all three metricType branches (table/index/statement), and replaced the '?' JDBC-placeholder-lookalike coalesce fallback with 'bilinmiyor' to remove a second latent risk. Verified the corrected SQL by hand against a real details_json row before deploying. mvn compile/mvn test pass.",
        "AC7: after live-fixing PGSTAT-P1-010 (agg.pg_table_stat_hourly bloat via manual investigation), customer asked 'neden pgstat böyle bir öneriyi (eşiği düşür) kendisi vermiyor' (why doesn't pgstat suggest this itself). Correct — the decision tree had no scenario for 'autovacuum runs chronically but the trend keeps rising, meaning the threshold itself is miscalibrated', only scenario 4 ('fresh bloat, autovacuum hasn't caught up yet, just observe') which gives passive advice unsuited to a chronic pattern. Added scenario 4.5, gated ahead of scenario 4: same recentAutovacuum + increasingTrend condition as scenario 4, but additionally requires autovacuum_count_sum > 1 (autovacuum has run more than once in the window, not just zero-or-once) — this distinguishes a genuinely fresh bloat event from a persistent threshold mismatch, and its action names the exact ALTER TABLE ... SET (autovacuum_vacuum_scale_factor/threshold) fix instead of 'wait and see'. mvn compile/mvn test pass.",
        "Verified in production this week alongside the rest of the bloat alerting work."
      ],
      "next_action": "Kapandı — canlı doğrulaması yapıldı, müşteri onayı alındı.",
      "completed_at": "2026-08-31"
    },
    {
      "id": "PGSTAT-P1-010",
      "order": 45,
      "priority": "P1",
      "workstream": "collector",
      "status": "done",
      "title": "agg.pg_table_stat_hourly / agg.pgss_hourly grow continuously due to frequent UPSERT-driven dead tuples, default autovacuum threshold too slow",
      "problem": "Customer asked a conceptual question after watching the bloat alert system work all day: 'pgstat's own DB has ongoing DELETE-driven bloat, why doesn't autovacuum ever recover the space, shouldn't autovacuum let new writes reuse dead tuple space?' Investigated with fact.pg_relation_size_snapshot (nightly-collected real table size, not just dead_tup ratio) and found agg.pg_table_stat_hourly_202608 grew from 1457MB to 2723MB over 14 days (~87%) while agg fact-table partitions grew for the expected reason (new daily data). Live query on fact.pg_table_stat_delta showed n_live_tup_estimate=7,062,216 constant while n_dead_tup_estimate climbed from 1,121,145 to 1,194,965 within a few hours — live count NOT changing, dead count climbing: a strong signal of repeated UPDATE, not INSERT growth. control.table_relopts_snapshot confirmed no per-table autovacuum_enabled override; last_autovacuum was ~9 hours old with autovacuum_count_delta=0 since. Root-caused via code read (Explore agent): AggRepository.rollupTableStatHourly()/rollupPgssHourly() UPSERT (on conflict on the full PK: bucket_start/instance_pk/dbid/relid or equivalent) the SAME current-hour bucket rows every ~5 minutes (JobOrchestrator's readHourlyRollupIntervalSec guard, default 300s) — roughly 12 real UPDATEs per row per hour, each leaving a dead tuple via MVCC. At this table's scale (~7M rows), the default autovacuum trigger (threshold=50 + scale_factor=0.2 × live_tup ≈ 1.4M dead tuples) takes far too long to reach relative to the actual update rate, so the table bloats for days before autovacuup ever fires — not a bug in the rollup logic (which is correct and necessary), but a mismatch between this table's write pattern (small delta, many updates) and Postgres's row-count-scaled default vacuum threshold.",
      "requirements": [
        "The two hourly rollup tables with this UPSERT-every-cycle pattern (agg.pg_table_stat_hourly, agg.pgss_hourly) should get a lower, size-independent autovacuum trigger so dead tuples from the rollup's repeated UPSERTs get reclaimed promptly instead of accumulating for days.",
        "Fix must be a per-table storage parameter (ALTER TABLE ... SET), not a change to the rollup logic itself — the UPSERT-every-5-minutes design is correct and intentional (JobOrchestrator already has a 5-minute guard to prevent worse, per-poll-cycle UPSERTs).",
        "Must apply automatically to future monthly partitions of these two tables, not require a migration per partition.",
        "The dead_tuple_ratio alert's top-candidate selection must surface this table when it IS the biggest problem — the customer separately asked 'neden pg stat bu bloatı yakalamadı, alertlerde görmedim hiç bu tabloları' (why didn't pgstat catch this bloat, I never saw these tables in the alerts) after this investigation, which surfaced a second, distinct bug (see AC3)."
      ],
      "acceptance_criteria": [
        {
          "id": "AC1",
          "text": "V094 migration sets autovacuum_vacuum_scale_factor and autovacuum_vacuum_threshold on every EXISTING leaf partition of agg.pg_table_stat_hourly and agg.pgss_hourly to a low, mostly-count-based threshold appropriate for a table with a near-constant live row count and a high per-row update rate.",
          "status": "done"
        },
        {
          "id": "AC2",
          "text": "Live verification: after deploying and waiting a full autovacuum_naptime + threshold-crossing period, confirm via fact.pg_table_stat_delta that last_autovacuum advances on the current-month partition and n_dead_tup_estimate stops climbing unbounded, and via fact.pg_relation_size_snapshot that the partition's total_size_bytes growth rate slows.",
          "status": "done"
        },
        {
          "id": "AC3",
          "text": "findBloatedTables() orders candidate tables by absolute dead_tup count (descending), not by dead-tuple percentage — a small table with a high ratio (e.g. 83 dead rows out of 25 live, 76% ratio) must not outrank a huge table with a lower ratio but a vastly larger absolute problem (e.g. 1.2M+ dead rows, ~15% ratio) for the alert's top/title selection.",
          "status": "done"
        },
        {
          "id": "AC4",
          "text": "PartitionManager.createPartition() gains an optional storageParams parameter appended as a WITH (...) clause on the CREATE TABLE ... PARTITION OF DDL itself, applied only for agg.pg_table_stat_hourly/agg.pgss_hourly (via a new MONTHLY_AGG_STORAGE_PARAMS map) — future monthly partitions of these two tables get the same lowered autovacuum threshold at creation time, since the V094 fix (below) only reaches partitions that already exist.",
          "status": "done"
        }
      ],
      "implementation_plan": [
        "db/migrations/V094__hourly_agg_aggressive_autovacuum.sql: initial version used ALTER TABLE ... SET (...) on the PARTITIONED PARENT — failed live on PostgreSQL 15 with 'unrecognized parameter \"autovacuum_vacuum_scale_factor\"'. Root-caused via web research: VACUUM-side autovacuum reloptions (scale_factor/threshold, as opposed to the ANALYZE-side ones and autovacuum_enabled) have never been settable on a partitioned parent table in ANY PostgreSQL version (10-17) — VACUUM operates on physical storage, which the parent doesn't have. Fixed by rewriting the migration as a DO block that iterates pg_inherits to find every existing leaf partition of both tables and ALTERs each one individually.",
        "PartitionManager.java: added MONTHLY_AGG_STORAGE_PARAMS (table name -> WITH clause body) and a createPartition() overload taking an optional storageParams string, appended to the CREATE TABLE ... PARTITION OF DDL as WITH (...) when non-null. ensureMonthlyPartitions() now looks up the map per parent table and passes it through — this covers all FUTURE monthly partitions of these two tables at creation time (V094 only covers partitions that already existed when it ran).",
        "AlertRuleEvaluator.findBloatedTables(): changed the final ORDER BY from current_val (ratio) descending to dead_tup (absolute count) descending, so the alert's top-record selection (and therefore its title/message) surfaces the table with the largest real impact, not the table with the most dramatic-looking percentage."
      ],
      "verification_plan": [
        "./pgstat migrate (after the fix, not the original ALTER-on-parent version); confirm no error, and via a manual pg_class.reloptions check on the current-month partitions that the ALTER actually took.",
        "Live: monitor fact.pg_table_stat_delta for the current-month partition of both tables over the following days; confirm last_autovacuum starts advancing more frequently and n_dead_tup_estimate no longer grows unbounded between autovacuum runs.",
        "Live: compare fact.pg_relation_size_snapshot total_size_bytes growth rate before/after over a comparable multi-day window.",
        "Live: after the next monthly partition rollover, confirm the newly-created partition (via PartitionManager) already has the lowered autovacuum threshold set from creation, without needing a manual ALTER."
      ],
      "required_docs": [
        "docs/generated/project-status.md"
      ],
      "dependencies": [],
      "customer_acceptance": {
        "required": true,
        "status": "accepted",
        "evidence": "Customer confirmed on 2026-08-28: cozuldu diyebiliriz. The V094 per-table autovacuum settings brought the UPSERT-driven growth under control."
      },
      "verification_evidence": [
        "Customer asked a first-principles question ('autovacuum should let writes reuse dead tuple space, why does disk keep growing') that led to auditing pgstat's own database with fact.pg_relation_size_snapshot — found agg.pg_table_stat_hourly_202608 growing far faster (87% in 14 days) than its actual data volume justified (n_live_tup_estimate was CONSTANT at 7,062,216 across samples).",
        "Root-caused via a background Explore agent reading AggRepository.java and JobOrchestrator.java: the hourly hourly-rollup jobs re-UPSERT the current hour's bucket rows every ~5 minutes by design (this is correct behavior, not a bug), which at ~12 updates/row/hour outpaces the table's row-count-scaled default autovacuum threshold (~1.4M dead tuples at this table's scale) by a wide margin.",
        "Confirmed via control.table_relopts_snapshot that no per-table autovacuum_enabled override exists here (unlike the customer-facing dead_tuple_ratio cases) — this is purely a threshold-tuning problem, distinct from the P0-036 diagnosis work.",
        "AC3: customer then asked why the bloat alert never caught agg.pg_table_stat_hourly at all. Checked rule:14:instance:23 directly — it WAS open/critical the whole time (last_seen_at fresh), but its title pointed at control.instance_state (76% ratio, only 83 dead rows) instead of agg.pg_table_stat_hourly (1.2M+ dead rows). Ran findBloatedTables()'s query by hand for instance_pk=23: confirmed both tables ARE in the exceeding set, but the query's ORDER BY current_val desc put the tiny-table-high-ratio row first. Re-ran the same query with ORDER BY dead_tup desc: agg.pg_table_stat_hourly_202608 (1,255,613 dead) correctly sorted to the top ahead of agg.pgss_hourly_202608 (280,007) and every fact.*_delta partition. Fixed the code to match, mvn compile/mvn test pass.",
        "AC1 first-deploy failure: customer ran ./pgstat upgrade, got 'psql:...V094...: ERROR: unrecognized parameter \"autovacuum_vacuum_scale_factor\"' on line 28 (the ALTER TABLE on the parent) — migration correctly recorded as failed (not applied to schema_migrations, so the fix could be re-run via the same version file). Web research confirmed VACUUM-side reloptions are architecturally unsupported on partitioned parents in every PostgreSQL version — this was a genuine design mistake in the first V094, not a PG15-specific quirk. Rewrote the migration to target existing leaf partitions individually via pg_inherits, and added AC4 (PartitionManager change) so future partitions don't need a manual fix-up migration each time.",
        "AC1 second-deploy failure: re-running ./pgstat upgrade hit a NEW error — 'must be owner of table pg_table_stat_hourly_202606'. Queried pg_tables in the pgstat database directly: partitions 202606/202607/202608 are owned by a stale role 'primeit', all others by the current app role 'pgstatuser' — a pre-existing ownership inconsistency unrelated to this migration, surfaced only because this was the first time anything tried to ALTER those specific partitions. The OWNER TO fix itself then also failed with the same 'must be owner' error — this proved migrations run as pgstatuser, NOT the postgres superuser, so pgstatuser cannot reassign ownership away from another role. Had the customer manually run `ALTER TABLE ... OWNER TO pgstatuser` for the 3 stale-owned partitions as the actual postgres superuser (one-time fix, outside the migration).",
        "AC1 third deploy succeeded (1 new migration applied, 0 failed). Verified directly via pg_class.reloptions on all four affected partitions (pg_table_stat_hourly_202606/07/08, pgss_hourly_202608): all show {autovacuum_vacuum_scale_factor=0.02,autovacuum_vacuum_threshold=5000} — confirmed applied, not just 'migration ran without error'.",
        "Customer confirmed on 2026-08-28: cozuldu diyebiliriz. The V094 per-table autovacuum settings brought the UPSERT-driven growth under control."
      ],
      "next_action": "AC1 and AC3 fully deployed and confirmed. Remaining: AC2 (monitor over the next several days whether autovacuum starts firing more often and size growth slows) and AC4's live verification (confirm the next monthly partition rollover — for 202611 — creates the partition with the lowered threshold already set, without needing a manual fix). Also live-verify rule:14:instance:23's next message names agg.pg_table_stat_hourly instead of control.instance_state.",
      "completed_at": "2026-08-28"
    },
    {
      "id": "PGSTAT-P1-011",
      "order": 46,
      "priority": "P1",
      "workstream": "collector",
      "status": "done",
      "title": "Add observational autovacuum-pressure evidence (I/O operation counts, wait-event distribution) to the existing bloat alert",
      "problem": "After PGSTAT-P1-010's fix (lowering autovacuum thresholds on two high-UPSERT hourly rollup tables) and PGSTAT-P0-036 AC7 (a new alert scenario that recommends lowering thresholds for chronically-updated tables), the customer asked a sharp follow-up: 'autovacuum'un etkisini de ölçmeliyiz, sık sık devreye girerek sistemi yavaşlatıyor da olabilir' (we should measure autovacuum's impact too — frequent triggering could be slowing the system down). The concern is real and well-documented (worker saturation, cost-based throttling misconfiguration), and pgstat currently has no instrumentation for it: fetchAutovacuumWorkerStatus() only counts running workers (and does so incorrectly, via count(*) instead of distinct pid), fact.pg_progress_vacuum_snapshot only has progress metrics, and fact.pg_io_stat_delta's backend_type column could isolate autovacuum-worker I/O but nothing aggregates it. This means every 'lower your autovacuum threshold' recommendation pgstat makes is one-sided — it addresses bloat with no counter-evidence about autovacuum pressure. SCOPE NOTE (revised after four review rounds): the achievable goal is OBSERVATIONAL evidence, not a causal cost measurement. PostgreSQL exposes no per-process CPU time, pg_stat_io is cluster-wide operation counts (not bytes, not disk IOPS, not per-table), and wait-event data is point-in-time sampling that cannot prove backlog or single-cause attribution. The task therefore adds carefully-worded observational evidence sentences to the existing dead_tuple_ratio alert — it does NOT deliver a causal 'autovacuum cost' metric, and the earlier framing of this task (title mentioning CPU, requirements asking for an 'X MB/s' chart) was not achievable as stated.",
      "requirements": [
        "Add observational autovacuum-pressure evidence to the existing dead_tuple_ratio alert's diagnosis text (scenarios 3 and 4.5): autovacuum-worker I/O operation counts vs. client-backend counts where available (PG16+ only, via fact.pg_io_stat_delta backend_type + object='relation'), and the wait-event distribution of autovacuum-worker samples (IO wait / VacuumDelay throttle sleep / no-wait-event / other) from fact.pg_activity_snapshot.",
        "All generated text must stay observational, never causal: report what was sampled ('worker observed in throttle sleep in N% of samples'), never assert a diagnosis ('throttling is slowing the table down') or a hardware conclusion ('the disk is slow'). Every signal must distinguish unsupported-version / unknown-capability / no-data / real-zero / available rather than collapsing them into one N/A.",
        "A 'lower cost_delay' recommendation may only fire when the effective (table-override-aware, -1-fallback-resolved) cost_delay is strictly GREATER than the version default (20ms on PG11, 2ms on PG12+) AND the throttle-sleep evidence is statistically sufficient — never on a high IO-wait ratio alone, and never when the setting is at or below the version default.",
        "OUT OF SCOPE for this task, deliberately deferred to a future task: a byte-volume metric (derivable from pg_stat_io's op_bytes on PG16/17 or the direct byte counters on PG18, but still not real disk throughput) and any UI chart/visualization. The earlier requirement asking for an 'autovacuum consumed X MB/s' chart is withdrawn — see the design doc's 'Scope kararı' section for the rationale."
      ],
      "acceptance_criteria": [
        {
          "id": "AC1",
          "text": "docs/autovacuum-cost-diagnosis-design.md written, live-tested on 3 real instances (instance_pk=6/8/23), and consolidated across FOUR review rounds (own live tests plus three independent external reviews; official PostgreSQL 11/13/18 docs fetched live to settle version questions). Final design: Diagnosis 0 (BONUS evidence only, PG16+ gated, never primary — autovacuum-worker vs. client-backend I/O OPERATION counts via fact.pg_io_stat_delta with object='relation', since 15/25 registered instances are below PG16); Diagnosis 2 (primary, all PG versions — IO-wait ratio; proves only that an I/O completion was awaited, NOT that hardware is slow, and independent from Diagnosis 2b); Diagnosis 2b (primary, PG13+ only — the VacuumDelay wait_event was added in PG13 and has always been wait_event_type='Timeout' in every version where it exists; throttle-sleep ratio read alongside effective cost settings); Diagnosis 1 (optional secondary context only, its own >=10 five-minute-bucket gate on BOTH sides — the naive latency window comparison proved unreliable on live data); Diagnosis 3 (code guard: no action text may recommend disabling autovacuum). Wrong claims found and corrected across the rounds: 'VacuumDelay moved Timeout->IO in PG13' (false); the cost_delay 20ms->2ms default drop is PG12, not PG13; the -1 sentinel was NOT introduced in PG12 and never removed (present in PG11's own docs, current through PG17); a high IO-wait ratio does not prove cost_delay is misconfigured; the instance count is 25 not 23 (60% below PG16, not 65%); vacuum_cost_page_hit/miss/dirty are the correct unprefixed names; wait_event IS NULL means 'no wait event reported' not 'actively running'; high VacuumDelay means the cost budget WAS reached (not that the worker couldn't fill it); the '3 test instances all showed X' generalization was wrong (instance_pk=8 sampled 0/0, no signal at all). CLOSED decisions (no longer open): the Diagnosis 2/2b sufficiency gate is count(distinct snapshot_ts) >= 10; runningWorkers is count(distinct pid) at the instance's latest OVERALL activity snapshot (not the latest worker-filtered snapshot, which would misreport 'zero workers now' as a stale historical count); the settings read must use DISTINCT ON + ORDER BY snapshot_ts DESC with a freshness check (the first draft's query returned all historical rows); Diagnosis 0 needs an ordered six-state model (UNKNOWN_CAPABILITY / UNSUPPORTED / INSTANCE_UNREACHABLE / NO_FRESH_DATA / ZERO_IO_WITH_FRESH_DATA / AVAILABLE). Newly discovered in the fourth round: PG18 adds autovacuum_worker_slots which caps effective worker capacity below autovacuum_max_workers (3 PG18 instances in the fleet, parameter not collected at all yet), and ClusterCollector's getDoubleOrZero() silently coerces source NULLs to 0.0, which makes the NO_FRESH_DATA vs. ZERO_IO_WITH_FRESH_DATA distinction impossible without a fix. NO open design decisions remain. The two items the fourth round called open were not design questions: the timestamp type is an implementation test requirement (the decision is already made — normalize both OffsetDateTime and java.sql.Timestamp), and AC3 target selection is a verification procedure (query control.instance_capability at verification time). The fifth round additionally closed four contracts: four independent status fields on the worker record (currentWorkerStatus/ioWaitStatus/throttleStatus/capacityStatus) instead of one enum that cannot represent PG12 (IO available, throttle unsupported) or a 9-snapshot window (ratios insufficient, current worker count still valid); a six-state Diagnosis 0 model backed by source_row_count/per-metric-valid-counts/latest_sample_ts in the query itself, with ZERO_IO_WITH_FRESH_DATA named to avoid implying autovacuum did not run; the typed I/O counter refactor promoted from optional to mandatory prerequisite 10 (Long/BigDecimal counters, nullable Double timings, stats_reset in the same record, skip-delta-on-reset, null-aware DeltaCalculator); and a step-by-step effective-settings chain per parameter whose UNKNOWN outcome suppresses the cost_delay recommendation, and freshness thresholds derived from each source collector cadence (x2 + grace) rather than any hardcoded hour count, since hot-refreshed and nightly-refreshed settings have very different staleness bounds. The fifth round also defined what a NULL metric means: rows present but the metric NULL across them is NOT NO_FRESH_DATA — the record still returns AVAILABLE with that metric carried as null and reported as unmeasured, never as zero.",
          "status": "done"
        },
        {
          "id": "AC2",
          "text": "Implementation per the design doc's 'AC2 implementasyon sırası' section. STEP 0 — land the 10 confirmed prerequisite fixes from the doc's 'Kod önkoşulları' section first; these are foundation bugs the new evidence would otherwise inherit: (1) fetchAutovacuumWorkerStatus() uses count(*) instead of the two-step distinct-pid-at-latest-overall-snapshot definition; (2) findBloatedTables() has no relid, blocking per-table cost-override lookup by (instance_pk, dbid, relid); (3) control.table_relopts_snapshot stores only raw reloptions text, needs a migration adding parsed nullable cost-override columns plus a tested parser; (4) diagnoseBloat()'s evidence text is silently dropped when the message template fails to render; (5) ClusterCollector's previousIoSamples reads stats_reset but never uses it in the delta comparison; (6) the 'autovacuum' on/off parameter is in NEITHER settings list (not just missing from HOT — it's absent from the nightly SETTINGS_QUERY too), needed for the Diagnosis 3 guard; (7) vacuum_cost_delay/vacuum_cost_limit are in the nightly SETTINGS_QUERY but missing from HOT_SETTINGS_QUERY (autovacuum_vacuum_cost_delay/limit are already in both — don't confuse them); (8) autovacuum_worker_slots (PG18+, caps effective capacity below autovacuum_max_workers) is not collected at all, and 3 fleet instances are PG18; (9) ClusterCollector's getDoubleOrZero() coerces source NULLs to 0.0 for I/O counters, which makes Diagnosis 0's NO_FRESH_DATA vs. ZERO_IO_WITH_FRESH_DATA distinction impossible — must become NULL-safe or carry a separate flag. Prerequisite 10 (new, mandatory — was previously mislabeled optional): the I/O sample cache must become typed, since prerequisites 5 (stats_reset awareness), 9 (NULL preservation) and the Diagnosis 0 state model all mutate the same structure — Long/BigDecimal counters, nullable Double timings, previous stats_reset held in the same typed record, no delta written on the reset cycle, and a null-aware DeltaCalculator. THEN: fetchAutovacuumWorkerStatus() returns a typed record carrying runningWorkers, maxWorkers, workerSlots, effectiveWorkerCapacity, totalSamples, distinctSnapshots, ioWaitSamples, throttleSleepSamples, noWaitEventSamples, otherWaitSamples, latestSnapshotTs and FOUR INDEPENDENT status fields — currentWorkerStatus, ioWaitStatus, throttleStatus, capacityStatus — because a single enum cannot express PG12 (ioWait AVAILABLE while throttle is UNSUPPORTED_VERSION), a 9-snapshot window (ratios INSUFFICIENT_DATA while the current worker count is still AVAILABLE), or a missing PG18 worker_slots value (capacity UNKNOWN only). The three named wait buckets plus the residual otherWaitSamples sum to totalSamples by definition — assert this identity in a test and assert the residual is never negative. Sufficiency gate is count(distinct snapshot_ts) >= 10 (a closed decision, not an open question). Effective cost settings resolved per parameter independently through an explicit chain: table override >= 0 wins outright; table override == -1 or absent falls through to the global autovacuum_* setting (read via DISTINCT ON + ORDER BY snapshot_ts DESC with a freshness check, since pg_settings_snapshot is a time-series table); global -1 falls through to vacuum_* (PG11+ behavior). Missing, malformed or stale at any step yields UNKNOWN, which SUPPRESSES the cost_delay recommendation entirely. Freshness thresholds derive from each source collector cadence x2 plus grace — never a hardcoded hour count, since hot-refreshed and nightly-refreshed settings have very different staleness bounds. The 'lower cost_delay' recommendation gate is effectiveCostDelay > versionDefault (20ms on PG11, 2ms on PG12+) — NOT merely 'non-default', since 0ms/1ms are non-default but below the default and must not trigger it; and never on IO-wait evidence alone. fetchAutovacuumIoImpact() implements a SIX-state ordered model (UNKNOWN_CAPABILITY / UNSUPPORTED / INSTANCE_UNREACHABLE / NO_FRESH_DATA / ZERO_IO_WITH_FRESH_DATA / AVAILABLE), checking has_pg_stat_io and is_reachable explicitly, deciding NO_FRESH_DATA from source_row_count and latest_sample_ts rather than from counter values, using object='relation' and a client-reads=0 guard against infinite ratios. A metric that is NULL across all returned rows is a distinct case from both of those: the state stays AVAILABLE, the metric is carried as null and worded as unmeasured, and partial NULL coverage sums only the non-null rows while reporting the covered fraction. Zero relation I/O with fresh data must NOT be worded as 'autovacuum did not run' — pages may all have been buffer hits. All generated text stays observational, never causal, and never claims hardware slowness from wait_event_type='IO' (which only proves an I/O completion was awaited). Wire into scenario 3 and 4.5's diagnosis text on the EXISTING dead_tuple_ratio alert (AlertCode.USER_DEFINED_RULE) — not a new alert type, not adaptive alerting. mvn compile && mvn test pass; add the unit tests enumerated at the end of the design doc, including both timestamp types via queryForList() (resolving AC1's one remaining open item) and the autovacuum-disable guard.",
          "status": "done"
        },
        {
          "id": "AC3",
          "text": "Live verification, with target instances selected AT VERIFICATION TIME rather than fixed here — re-query control.instance_capability to pick one current PG16+ instance, one PG13-15 instance, and one PG12 instance, since pg_major values change (instance_pk=23 already moved PG15->PG17 mid-task via a live pg_upgrade, so any hardcoded list here would be stale). Confirm: the new evidence sentences appear correctly in a real triggered dead_tuple_ratio alert message; on the PG16+ instance Diagnosis 0 produces plausible operation counts with the object='relation' filter applied (this filter was added after the original measurement, so re-run and re-record the numbers rather than reusing the pre-filter figures); on the sub-PG16 instance Diagnosis 0 reports an explicit unsupported state while Diagnoses 2/2b still appear; on the PG12 instance Diagnosis 2b reports UNSUPPORTED_VERSION rather than a silent zero while Diagnosis 2 still works; the cost-setting guard suppresses 'lower cost_delay' where the effective setting is at or below the version default; and the six-state Diagnosis 0 model correctly distinguishes a real zero from missing data, and a metric that is NULL across all rows is reported as unmeasured rather than as zero.",
          "status": "done"
        },
        {
          "id": "AC4",
          "text": "Musteri geri bildirimi (2026-08-27) uzerine iki iyilestirme. (a) Senaryo 4 (autovacuum henuz yetismemis olabilir) artik alert ACMIYOR — cogu zaman toplama anina denk gelmis gecici bir birikim oldugu icin. Yerine V096 ile eklenen control.bloat_scenario_streak tablosunda israr sayaci tutuluyor: ayni tabloda ust uste 3 degerlendirmede senaryo 4 gorulur VE olu satir sayisi her seferinde artmaya devam ederse alert acilir, mesajda kac degerlendirmedir surdugu ve ne zamandir fark edildigi yazar. Trend artmayi birakirsa veya baska senaryoya gecilirse sayac silinir. Sayac kalici tabloda (in-memory degil) cunku collector her deploy-da yeniden baslar ve bellekteki sayac her seferinde sifirlanirdi (PGSTAT-P1-009 tuzagi). Kacirma riski yok: senaryo 4 karar agacinda 2/3/3.5/4.5-ten SONRA gelir, gercekten yetisemeyen tablo zaten onlara takilip aninda alert uretir. (b) Aksiyon metinlerindeki <sema.tablo> yer tutucusu gercek adla dolduruluyor — kullanici komutu dogrudan kopyalayip calistirabilsin. 9 aksiyon metni guncellendi, PG kimlik kurallarina gore gerektiginde cift tirnak ekleniyor (MixedCase, bosluk iceren adlar, ic tirnaklar). Ad okunamazsa bozuk SQL uretmek yerine yer tutucuya duser. 51/51 test geciyor.",
          "status": "done"
        }
      ],
      "implementation_plan": [
        "Follow docs/autovacuum-cost-diagnosis-design.md's 'AC2 implementasyon sırası' section verbatim — it is the canonical plan and is kept in sync with this board entry. Summary: land the 10 prerequisite code fixes from the doc's 'Kod önkoşulları' section, then extend fetchAutovacuumWorkerStatus() into a typed record covering Diagnoses 2/2b with the distinct-snapshot sufficiency gate, then the effective-cost-setting helper (table override -> instance setting via DISTINCT ON + freshness -> -1 fallback, gated on effectiveCostDelay > versionDefault), then fetchAutovacuumIoImpact() with its ordered six-state model, then the PG18 autovacuum_worker_slots capacity calculation, then wire the observational evidence sentences into scenario 3/4.5 of the existing dead_tuple_ratio alert. Diagnosis 1 (latency correlation) is optional and last. Byte-volume metrics and UI charts are explicitly out of scope for this task."
      ],
      "verification_plan": [
        "mvn compile && mvn test (plus make verify if present at repo root)",
        "node scripts/check-project-board.mjs && node scripts/generate-project-status.mjs after any board edit",
        "Live: per AC3 — select current PG16+/PG13-15/PG12 instances by querying control.instance_capability at verification time, then confirm evidence text, version gating, the six-state Diagnosis 0 model, and the cost-setting guard behave as specified. Re-measure Diagnosis 0's numbers with the object='relation' filter rather than citing the earlier unfiltered figures."
      ],
      "required_docs": [
        "docs/generated/project-status.md"
      ],
      "dependencies": [
        "PGSTAT-P1-010",
        "PGSTAT-P0-036"
      ],
      "customer_acceptance": {
        "required": true,
        "status": "accepted",
        "evidence": "Customer confirmed on 2026-08-28: yaptin calisiyor dendik. All five evidence paths were verified in a real alert message during live verification."
      },
      "verification_evidence": [
        "Customer's question was well-founded per background research: real-world case studies (perun.au 'five failure patterns', a dev.to postmortem) document autovacuum worker saturation causing checkpoint pressure, buffer pool contention, and query latency spikes — this is a genuine, bidirectional risk (the same research also found cases where MORE aggressive autovacuum fixed performance problems, so the risk cuts both ways and can't be resolved by intuition alone).",
        "[SUPERSEDED — historical record of an earlier review round; several specifics below were later found wrong and corrected. See AC1 and next_action for the current canonical state.] Code audit (background Explore-style agent) confirmed pgstat currently measures only autovacuum ACTIVITY (is it running, how many workers, what phase) via fetchAutovacuumWorkerStatus() and fact.pg_progress_vacuum_snapshot — it does not measure autovacuum's system COST (I/O volume, CPU, query latency impact). fact.pg_io_stat_delta already has a backend_type column that could isolate this but nothing currently uses it that way — a genuine, well-scoped gap rather than something already solved and just undiscovered.",
        "AC1: customer asked specifically 'nasıl yapılacak doğru mu yapılacak — autovacuumın kapatılması gerekiyor mu, sık çalışıp diğer sorguları yavaşlatıyor mu, yoksa yetişemiyor mu — en doğru ve somut kanıtlı şekilde nasıl tespit ederiz'. A second Explore pass verified exact column names before writing the design (fact.pgss_delta's duration columns are mean_exec_time_ms/total_exec_time_ms_delta, NOT mean_time/total_time as initially assumed) and confirmed the key limitation: pgss_delta has no relid, so per-table query-latency correlation isn't directly queryable — dim.statement_series only carries dbid. Documented this as a deliberate instance-wide (not per-table) scope for the latency-impact diagnosis, and made the 'never recommend disabling autovacuum' guard an explicit requirement with a proposed unit test.",
        "[SUPERSEDED — historical record of an earlier review round; several specifics below were later found wrong and corrected. See AC1 and next_action for the current canonical state.] AC1 revised after live-testing the query-latency-correlation query (Diagnosis 1) by hand on two real instances per the customer's explicit request to 'test with real data and build it correctly'. instance_pk=8 showed a +5.6% difference (below any reasonable significance threshold); instance_pk=23 (pgstat's own DB) showed the OPPOSITE direction (-29%, autovacuum-active windows had LOWER latency) — proving the naive window-comparison approach is unreliable (classic confounding: autovacuum may fire when the system happens to be quiet). A follow-up research pass confirmed even pganalyze's own VACUUM Advisor deliberately avoids correlating autovacuum with query latency for exactly this reason. Redesigned around a new Diagnosis 0 (primary): direct, uncorrelated I/O cost attribution via fact.pg_io_stat_delta grouped by backend_type — tested live on instance_pk=6 (24h window) and found autovacuum workers had done 5,119,503 reads vs. only 172,332 for client backends (~30x), a concrete, causally-unambiguous number since the I/O is mechanically autovacuum's own activity. Also discovered live that read_time_ms/write_time_ms are 0 on this instance (track_io_timing off) — the design must degrade gracefully to read/write COUNTS when timing isn't tracked. The original latency-correlation query (now Diagnosis 1) is kept but demoted to an optional, low-confidence secondary context note, never a standalone basis for an action recommendation.",
        "[SUPERSEDED — historical record of an earlier review round; several specifics below were later found wrong and corrected. See AC1 and next_action for the current canonical state.] AC1 further hardened by a 3-instance live test (pk=6 PG17, pk=8 PG13, pk=23 PG15) at the customer's request. Extracted the actual PG-major distribution across all 23 registered instances: 15/23 (65%) are below PG16, meaning Diagnosis 0 (I/O counts, needs pg_stat_io) cannot run on the majority of the fleet — this demoted it from 'primary' to 'bonus evidence when available' and promoted the version-independent Diagnoses 2/2b to primary. Testing also surfaced and fixed a real collector bug found only because of this exercise: pg_major was written once at bootstrap and never refreshed, so a pg_upgrade (PG15->PG17, done live on instance_pk=23 during this task) left instance_capability permanently stale — DiscoveryCollector.recheckVersionIfChanged() was added to ClusterCollector's per-cycle loop, confirmed live on instance_pk=23 (log showed 'PG surum degisikligi tespit edildi: pgstat-dev — PG15 -> PG17', pg_major correctly became 17). A related pg_stat_statements extension version mismatch (post-upgrade, 'column shared_blk_read_time does not exist') was found and fixed with ALTER EXTENSION UPDATE. Two design claims written without checking real data turned out wrong and were corrected from live evidence: 'VacuumDelay moved from wait_event_type=Timeout to IO in PG13' was false (confirmed still Timeout on a live PG17 instance — Diagnoses 2 and 2b are independent signals, no PG-version branch needed); Diagnosis 2's 30-minute window missed all worker samples on instance_pk=23 while Diagnosis 2b's 2-hour window caught 4 (window widened to match). Cost settings (autovacuum_vacuum_cost_limit/delay, vacuum_cost_limit) were added to NightlySnapshotCollector's settings whitelist and confirmed flowing into fact.pg_settings_snapshot after deploy — all 3 test instances sit at PG defaults, meaning a naive 'lower cost_delay' recommendation would be wrong for all of them right now; the design was corrected to only suggest that action when the setting is demonstrably non-default.",
        "[SUPERSEDED — historical record of an earlier review round; several specifics below were later found wrong and corrected. See AC1 and next_action for the current canonical state.] AC1 additionally reviewed by an independent external pass (2026-08-25, PostgreSQL-knowledge-based, not code-based — it had no access to this repo and its one repo-specific claim about a mismatched workspace was disregarded as inapplicable). Confirmed valid and corrected in the design doc: (1) the VacuumDelay wait_event itself was only added in PG13 — a version floor independent from the already-corrected Timeout-vs-IO category claim, meaning Diagnosis 2b must report 'unavailable' rather than a silent zero on the 5/23 PG12 instances; (2) several interpretation-table phrases ('real load', 'deliberate throttling is the cause') overclaimed causality from a point-in-time wait_event sample and were reworded to observational language, gated on a minimum sample count; (3) Diagnosis 0's reads/writes are page-operation counts, not bytes or real disk IOPS, and don't capture the checkpointer's later writeback of the same dirtied pages — reworded from 'I/O cost' to 'operation-count proxy'; (4) the cost-setting read must also check the per-table control.table_relopts_snapshot override, not just the instance-wide pg_settings value, since PostgreSQL allows a table-level ALTER TABLE ... SET (autovacuum_vacuum_cost_delay = ...) to override the cluster default; (5) none of these diagnoses attribute worker activity to a specific table (pg_activity_snapshot has no relid) or rule out alternate root causes (worker saturation, long transactions, orphaned replication slots, wraparound-driven forced vacuum, lock contention) — added an explicit list of these to the design doc so alert text stays observational rather than implying a single diagnosed cause; (6) 'bloat' in this codebase means the n_dead_tup statistical estimate, not a physically-measured disk-space figure — documented as a terminology note rather than renaming the established codebase term.",
        "[SUPERSEDED — historical record of an earlier review round; several specifics below were later found wrong and corrected. See AC1 and next_action for the current canonical state.] A second, more detailed independent review (2026-08-25) verified the design against this repo's HEAD (fc985a1) directly and caught two further wrong version claims plus seven real code gaps, confirmed one-by-one by a code-audit subagent before accepting any of them. WRONG CLAIMS CORRECTED: cost_delay's 20ms->2ms default drop happened in PG12, not PG13 (PG13 kept the PG12 default unchanged); the -1 sentinel ('fall back to vacuum_cost_delay/vacuum_cost_limit') was never removed and is still current behavior through PG17 — both were stated backwards in the original design and are now fixed. Also corrected: a high IO-wait ratio (Diagnosis 2) does NOT prove cost_delay is set too high — IO wait and VacuumDelay are independent wait causes (one is disk latency, the other is deliberate config-driven sleep); the 'lower cost_delay' action must be gated on Diagnosis 2b's throttle evidence, not Diagnosis 2's IO evidence. CODE GAPS CONFIRMED (all now documented as AC2 prerequisites, not the reviewer's guesses — verified against actual line numbers): fetchAutovacuumWorkerStatus() counts count(*) over a 2-minute window instead of distinct pid at the latest snapshot_ts, risking double-counting the same worker across cycles as false 'saturation'; findBloatedTables() doesn't select relid, which blocks any per-table cost-override lookup by (instance_pk, dbid, relid); control.table_relopts_snapshot (V093) only stores raw reloptions text, no parsed cost columns exist yet; diagnoseBloat()'s evidence text is silently dropped if the alert message template fails to render (falls back to a generic message with no diagnosis/action context); ClusterCollector's previousIoSamples cache reads stats_reset but never uses it in the delta comparison, risking a wrong I/O count immediately after a pg_stat_reset_shared('io') call; I/O counters are stored as Double before being cast to long (not a live precision-loss risk at realistic magnitudes, but a type-correctness smell); OffsetDateTime timestamp handling was checked and found consistent (no getTimestamp() usage, no bug there — this one claim did NOT hold up).",
        "[SUPERSEDED IN PART — the fourth round's own summary; the fifth round below closed four contracts it had left underspecified] Fourth review round (2026-08-26): the design doc was consolidated a second time — earlier rounds had appended correction notes beside wrong text instead of replacing it, which itself produced contradictions (Diagnosis 0 described as both primary and PG16+ bonus, instance count given as both 23 and 25, the -1 sentinel described as both introduced-in-PG12 and present-since-PG11). The doc now carries one canonical explanation per topic. This round also fetched three official PostgreSQL doc pages live to settle version questions definitively: PG11 runtime-config-autovacuum confirms the -1 sentinel predates PG12 (so the previous rounds claim that PG12 introduced it was itself wrong); PG13 monitoring-stats confirms VacuumDelay was added in PG13 and sits in the Timeout wait_event_type table (so the correct phrasing is always-Timeout-where-it-exists, not always-Timeout-in-every-version); PG18 runtime-config-autovacuum confirms autovacuum_worker_slots caps effective worker capacity below autovacuum_max_workers and that cost_limit is distributed proportionally among running workers — a parameter pgstat does not collect at all despite 3 PG18 instances in the fleet. A code-audit pass additionally confirmed two new prerequisites: the settings-read query in the design draft returned all historical rows (needs DISTINCT ON + ORDER BY snapshot_ts DESC plus a freshness check, since pg_settings_snapshot is a partitioned time-series table), and ClusterCollector.getDoubleOrZero() silently coerces source NULLs to 0.0 for I/O counters, which makes Diagnosis 0 NO_DATA-vs-real-zero discrimination impossible without a fix. The prerequisite count is now 9 (was miscounted as 5 then 7), and the earlier claim that timestamp handling was verified-clean was correctly reopened — only direct ResultSet paths were checked, not the queryForList() access pattern this task will use. Scope was also narrowed deliberately: the original requirement for an autovacuum consumed X MB/s chart is withdrawn (PostgreSQL exposes operation counts, and byte counters only from PG16 op_bytes / PG18 direct columns, none of which equal real disk throughput) and moved to a future task.",
        "CURRENT (fifth review round, 2026-08-26): a further review found the fourth round had left four technical contracts underspecified enough to block AC2, all now closed in the design doc. (1) A SINGLE status enum on the worker-evidence record is impossible: on PG12 ioWaitStatus is AVAILABLE while throttleStatus must be UNSUPPORTED_VERSION; with only 9 distinct snapshots the ratios are INSUFFICIENT_DATA while currentWorkerStatus can still be AVAILABLE; and a missing PG18 autovacuum_worker_slots value should degrade only capacityStatus. The record now carries four independent status fields (currentWorkerStatus, ioWaitStatus, throttleStatus, capacityStatus) plus workerSlots and effectiveWorkerCapacity. (2) Diagnosis 0 could not actually decide fresh-zero-vs-no-data from its own query: it now also returns source_row_count, per-metric valid counts, and latest_sample_ts, and the state model grew to six ordered states (UNKNOWN_CAPABILITY / UNSUPPORTED / INSTANCE_UNREACHABLE / NO_FRESH_DATA / ZERO_IO_WITH_FRESH_DATA / AVAILABLE) with has_pg_stat_io and is_reachable checked explicitly. The state was renamed ZERO_IO_WITH_FRESH_DATA because zero relation I/O does not mean autovacuum did not run (pages may all have been buffer hits). (3) The Double-to-typed refactor was still marked optional despite three separate prerequisites depending on it; it is now mandatory prerequisite 10, specifying Long/BigDecimal counters, nullable Double timings, previous stats_reset stored in the same typed record, skip-delta-on-reset, and a null-aware DeltaCalculator (also correcting the earlier 52-bit claim: binary64 is exact only to 2^53). (4) The effective-settings chain is now written step-by-step per parameter with an explicit UNKNOWN outcome for missing/malformed/stale values that suppresses the cost_delay recommendation, and freshness thresholds are defined as collector-cadence x2 + grace rather than the previously hardcoded 25 hours (which was far too loose for settings refreshed every 3 hours). Also corrected: the three named wait buckets plus the residual sum to totalSamples BY DEFINITION (the doc had claimed the opposite); Diagnosis 2 opened by saying real disk I/O wait when PostgreSQL only guarantees an I/O completion is being awaited; and PG18 cost balancing has a documented exception that matters directly here — workers processing tables with per-table cost storage parameters are excluded from balancing entirely (verified against PG18 routine-vacuuming docs), which invalidates any per-worker budget arithmetic for exactly the tables this feature reads overrides for.",
        "AC2 FAZ 1 (2026-08-26, kodlandi ve test edildi, henuz deploy edilmedi): Worker evidence katmani yazildi. fetchAutovacuumWorkerStatus() artik Object[] yerine typed AutovacuumWorkerEvidence donuyor — dort bagimsiz status alani (currentWorkerStatus/ioWaitStatus/throttleStatus/capacityStatus), dort wait kovasi (io/throttle/no-wait/residual other), workerSlots ve effectiveWorkerCapacity dahil. runningWorkers artik instance-in EN GUNCEL GENEL snapshot-indaki count(distinct pid) (eski kod son 2 dakikadaki count(*) kullaniyordu, ayni worker-i birden fazla cycle-da sayip sahte doygunluk uretebilirdi). Yeterlilik kapisi count(distinct snapshot_ts) >= 10, tazelik esigi cluster cadence x2 + 600sn grace (sabit sayi degil, control.schedule_profile-dan okunuyor). Teshis 2b PG13 altinda UNSUPPORTED_VERSION donuyor, sessiz sifir degil. PG18 effectiveWorkerCapacity = min(max_workers, worker_slots). Senaryo 3 ve 4.5-in mesajlarina renderWorkerWaitEvidence() ile gozlemsel kanit cumlesi eklendi; senaryo 3-un aksiyonu artik buildCostDelayAction() ile uc kosullu guard-a bagli (throttle orani >%50 VE yeterli orneklem VE effectiveCostDelay > versionDefault) — ayar cozumlenemezse oneri bastiriliyor. resolveEffectiveCostDelay() zinciri: tablo override >= 0 -> global autovacuum_* -> -1 ise vacuum_* fallback. findBloatedTables() SELECT listesine relid eklendi (tablo override lookup icin sart). NightlySnapshotCollector: autovacuum ve autovacuum_worker_slots her iki listeye, vacuum_cost_delay/vacuum_cost_limit HOT listeye eklendi. 17 yeni birim test yazildi (AutovacuumEvidenceTest), toplam 24/24 test geciyor, mvn compile temiz. KALAN: V095 migration (parsed cost-override kolonlari), template-fallback kanit korumasi, stats_reset-aware typed IO cache, Teshis 0 (fetchAutovacuumIoImpact) ve AC3 canli dogrulama.",
        "AC2 FAZ 2 TAMAMLANDI (2026-08-26): Kalan dort onkosul ve Teshis 0 kodlandi. (1) V095 migration: control.table_relopts_snapshot-a autovacuum_vacuum_cost_delay/limit ayristirilmis nullable kolonlari eklendi, mevcut satirlar icin reloptions_raw-dan regex backfill yapiliyor; FactRepository.upsertTableRelOptions() artik toplama aninda parse edip bu kolonlara yaziyor (her okumada ham metin parse etmek yerine). resolveEffectiveCostDelay() zincirin ilk adimini bu kolondan okuyor. (2) Template-fallback kanit korumasi: appendDiagnosisToFallback() eklendi — template render basarisiz oldugunda eskiden diagnoseBloat()-un urettigi tum kanit sessizce kayboluyor ve kullaniciya sadece jenerik Tablo esigi asti satiri gidiyordu; artik teshis+aksiyon fallback mesaja da ekleniyor. (3) Teshis 0 (fetchAutovacuumIoImpact) yazildi: alti durumlu SIRALI model (UNKNOWN_CAPABILITY -> UNSUPPORTED -> INSTANCE_UNREACHABLE -> NO_FRESH_DATA -> ZERO_IO_WITH_FRESH_DATA -> AVAILABLE), has_pg_stat_io ve is_reachable acikca kontrol ediliyor, object=relation filtresi uygulaniyor, NO_FRESH_DATA karari source_row_count ve max(sample_ts) tazeligine dayaniyor (sayac degerine DEGIL), client reads=0 durumunda oran uretilmiyor, kismi NULL kapsama metricCoveragePct ile raporlaniyor. renderIoImpactEvidence() metni islem sayisi olarak sunuyor — MB/IOPS/maliyet iddiasi yok, ve sifir I/O acikca autovacuum calismadi demek degil diye belirtiliyor. Senaryo 3 ve 4.5 hem wait-event hem I/O kanitini tasiyor. 33/33 test geciyor (26 tanesi AutovacuumEvidenceTest). KALAN: sadece AC3 canli dogrulama; ayrica stats_reset-aware typed IO cache (onkosul 5/10) ayri bir collector-dogrulugu isi olarak birakildi — Teshis 0 bundan etkilenir ama sadece pg_stat_reset_shared(io) cagrildiginda, ki bu nadir bir olay.",
        "AC3 KISMI (2026-08-26, deploy sonrasi ilk dogrulama): V095 uygulandi (iki cost kolonu mevcut), yeni ayarlar toplaniyor — autovacuum 50 satir, vacuum_cost_delay/limit 175er satir, autovacuum_worker_slots 6 satir (yalniz 3 PG18 instance-tan geldigi icin dogru davranis). Collector hatasiz. KRITIK BULGU: Teshis 0 object=relation filtresiyle ilk kez olculdu ve onceki filtresiz olcum YANLIS cikti. instance_pk=6, 24 saat: filtresiz av_reads=5.119.503 / client_reads=172.332 (~30x autovacuum lehine) idi; filtreli olcumde av_reads=8.314.997 / client_reads=31.407.722 (0.26x — client 3.8 kat FAZLA). Filtresiz sorgu client backend-in relation disi I/O-sunu (temp relation vb.) disarida birakip orani ~115 kat sismis. Yani autovacuum bu instance-ta uygulama trafiginin yaklasik dortte biri kadar okuma yapiyor, bir sorun isareti degil. Bu, tasarimin filtre eklendi ama yeniden olculmedi, kanitlanmis sayilmaz notunun neden gerekli oldugunun somut kanitidir; pg_stat_io uzerinde object filtresi olmadan yapilan hicbir backend_type karsilastirmasi guvenilir degil. Ayrica reads_valid_count=source_row_count (40982/40982) — bu instance-ta NULL sayac yok, metricCoveragePct=100. KALAN: gercek bir dead_tuple_ratio alarminin tetiklenip mesajinda kanit cumlelerinin gorulmesi.",
        "Ertelenen onkosul 5/10 de kapatildi (2026-08-26): ClusterCollector.previousIoSamples artik typed IoStatSample record tutuyor (Map<String,Map<String,Double>> yerine) — sayaclar Long, sureler nullable Double, stats_reset AYNI kayitta. stats_reset degistiginde (pg_stat_reset_shared io) o key icin delta YAZILMIYOR, sadece yeni baseline aliniyor ve log.info ile kac satirin atlandigi raporlaniyor; eskiden reset sonrasi eski baseline-a gore delta hesaplanip yanlis sayi uretme riski vardi. getDoubleOrZero() yerine getLongOrNull()/getDoubleOrNull() kullaniliyor — kaynak NULL artik 0-a cevrilmiyor, olculemedi bilgisi fact tablosuna kadar korunuyor. DeltaCalculator null-aware hale getirildi (deltaLong/deltaDouble her iki tarafta null kabul ediyor, biri null ise sonuc null). 7 yeni test (DeltaCalculatorTest), toplam 40/40 geciyor.",
        "AC3 canli test iki gercek bug ortaya cikardi (2026-08-26): hasXminHorizonRisk() sahte pozitif uretiyordu ve senaryo 2-yi haksiz yere senaryo 3/4.5-in onune geciriyordu. (1) Transaction yasi su ana gore olculuyordu (xact_start < now() - 10 dakika) ama snapshot gecmiste alinmis oluyor: 9 dakika once alinmis bir snapshot-taki 2 dakikalik transaction, simdi bakildiginda 11 dakikalik gorunup pozitif donuyordu. Duzeltme: snapshot_ts - xact_start > interval 10 minutes (snapshot anindaki yas). (2) Daha ciddi: backend_type filtresi yoktu, yani autovacuum worker-in KENDI uzun suren VACUUM islemi xmin horizon-u tutan uzun transaction olarak sayiliyordu — canli ornekte sayilan satir autovacuum: VACUUM av_test.churn idi, yani sistem autovacuum calisamiyor cunku autovacuum calisiyor gibi anlamsiz bir teshis uretiyordu. Duzeltme: backend_type is distinct from autovacuum worker. Ayrica state != idle yerine state is distinct from idle kullanildi (NULL state artik dogru ele aliniyor). Bu iki bug, bu gorevin kapsaminda degildi (senaryo 2 PGSTAT-P0-036 AC6 ile gelmisti) ama AC3 canli dogrulamasi olmasa kesfedilemezdi.",
        "AC3 canli test UCUNCU bir gercek bug buldu (2026-08-26): loadActiveRules() SELECT listesinde bloat_min_rows, bloat_abs_dead_tup ve bloat_vacuum_ineffective_count kolonlari YOKTU. Bu uc kolon PGSTAT-P0-036 ile migration-a eklenmis ve findRecordsExceedingThreshold()/findBloatedTables() bunlari okuyacak sekilde yazilmisti, ama loadActiveRules()-un sorgusuna eklenmedigi icin rule.get(...) HER ZAMAN null donuyordu ve kod sessizce kod-ici varsayilanlara (100 / 500 / 20) dusuyordu. Pratik sonuc: kullanici UI-dan veya SQL-den bu esikleri degistirse bile hicbir etkisi olmuyordu — sessiz, gorunmez bir bug. Canli testte bloat_vacuum_ineffective_count-u 2-ye cektik ama senaryo 3 bir turlu tetiklenmedi; kok neden bu cikti (kod hala varsayilan 20-yi kullaniyordu, bizim count_sum=5 degerimiz yetmiyordu). Duzeltme: uc kolon SELECT listesine eklendi. Bu bug, senaryo 3-un (vacuum_ineffective) pratikte neredeyse hic tetiklenmemis olabilecegini de gosteriyor — 30 dakikalik pencerede 20 tamamlanmis autovacuum turu cok yuksek bir esik.",
        "AC3 canli test DORDUNCU ve en kritik bug-u buldu (2026-08-26) — ve bu, tasarim dokumaninda ACIK SORU olarak birakilan maddenin ta kendisiydi: queryForList() timestamptz kolonlarini java.sql.Timestamp olarak donduruyor, OffsetDateTime olarak DEGIL (Spring ColumnMapRowMapper rs.getObject(i) cagirir; PG surucusunun varsayilan mapping-i Timestamp). diagnoseBloat() ise lastAutovacuum instanceof java.time.OffsetDateTime kalibini kullaniyordu — java.sql.Timestamp bu testi HIC gecmez. Pratik sonuc: staleAutovacuum ve recentAutovacuum HER ZAMAN false donuyordu, yani senaryo 3.5, 4 ve 4.5 hicbir kosulda tetiklenemiyordu. Alert her seferinde senaryo 5-e (varsayilan) dusuyordu. Bu, senaryo 4.5-in (PGSTAT-P0-036 AC7 ile eklenen, musterinin ozellikle istedigi kronik-esik-yanlis-kalibre teshisi) uretimde HIC calismamis oldugu anlamina geliyor. Duzeltme: asOffsetDateTime() normalize helper-i eklendi (OffsetDateTime/Timestamp/Instant/LocalDateTime kabul eder, taninmayan tip ve null icin null doner), uc kontrol de bunu kullaniyor. 5 yeni birim test, toplam 45/45 geciyor. NOT: Bu bug ResultSet-tabanli kod yollarini (rs.getObject(col, OffsetDateTime.class)) etkilemiyor — sadece queryForList() yolunu; tasarim dokumaninin bu ayrimi dogru yapmisti ama riski kapatmak icin test yazilmasi gerektigini de soylemisti, ki gerekliligi boylece kanitlandi.",
        "AC3 TAMAMLANDI (2026-08-26): Bes kanit yolunun besi de canli dogrulandi. instance_pk=23 (PG17) uzerinde gercek bir dead_tuple_ratio alarmi senaryo 3-u secti ve su metni uretti: Tehis: Autovacuum calisiyor ama olu satirlari yeterince hizli temizleyemiyor. Ek gozlem: son 2 saatte autovacuum workerlari 28 farkli toplama aninda orneklendi; orneklemelerin %0inde bir I/O isleminin tamamlanmasi bekleniyordu; %89inde cost-based throttle uykusundaydi (VacuumDelay). Son 24 saatte autovacuum workerlar 18.601.379 okuma / 2.537.787 yazma islemi yapti. Aksiyon: Etkin autovacuum_vacuum_cost_delay = 20ms, bu PG surumunun varsayilanindan (2ms) yuksek ve worker orneklemelerin cogunda throttle uykusunda gozlemlendi — cost_delayi dusurmeyi degerlendir. DOGRULANANLAR: (1) senaryo 3 secimi; (2) >=10 distinct snapshot yeterlilik kapisi (28 ornek); (3) Teshis 2 IO-wait orani; (4) Teshis 2b throttle orani + V095 tablo override okumasi (20ms); (5) effectiveCostDelay > versionDefault guard-i (20 > 2 -> oneri uretildi); (6) Teshis 0 AVAILABLE. AYRICA en degerli dogrulama: ayni alarm bir onceki turda Teshis 0 icin NO_FRESH_DATA dedi cunku --no-cache rebuild sirasinda collector 12 dakikadan uzun kapaliydi ve io_stat verisi bayatlamisti; veri tazelenince AVAILABLE-a gecti. Yani bes durumlu modelin varlik sebebi olan olcum yok ile sifir olculdu ayrimi gercek bir collector kesintisinde kendini kanitladi. Ayrica IO-wait %0 iken throttle %89 cikmasi, tasarimda israrla savunulan bu iki sinyal bagimsizdir tezinin canli kanitidir.",
        "AC3 canli testinin toplam bilancosu: DORT gercek bug bulundu, hicbiri birim testlerle yakalanamazdi ve ucu bu gorevin kapsami disindaydi. (1) hasXminHorizonRisk transaction yasini now()-a gore olcuyordu (snapshot anina gore olmali) -> sahte senaryo 2; (2) ayni fonksiyon autovacuum workerin kendi VACUUM islemini xmin horizonu tutan uzun transaction sayiyordu -> autovacuum calisamiyor cunku autovacuum calisiyor gibi anlamsiz teshis; (3) loadActiveRules bloat esik kolonlarini SELECT etmiyordu -> kullanicinin UIdan ayarladigi esikler hicbir ise yaramiyordu, kod hep varsayilanlari kullaniyordu; (4) EN KRITIK: queryForList timestamptz kolonlarini java.sql.Timestamp donduruyor ama kod instanceof OffsetDateTime bekliyordu -> senaryo 3.5/4/4.5 HIC tetiklenemiyordu, yani musterinin ozellikle istedigi senaryo 4.5 uretimde bir kez bile calismamisti. Bu dordu de duzeltildi ve test edildi (45/45 birim test geciyor).",
        "Canli gozlem (2026-08-27, deploy sonrasi): AC4-un ilk versiyonu sadece senaryo 4-u bastiriyordu, ama uretimdeki gercek gurultu kaynagi senaryo 1b-ii cikti. instance_pk=14: 122 olu satir (esik 114 — 8 satirla asmis), 21 Agustos-tan beri ACIK, 353 tekrar, ve mesaj hala \"esigi COK YAKIN ZAMANDA asti, naptime bekliyor\" diyordu — 6 gundur. instance_pk=8: 515.738 olu satir / 0 canli satir, ayni mesaj, ayni sure. Ikisinde de worker doygunlugu YOK (0/3 calisiyor), yani mesajin varsayimi (birazdan alinacak) olguyla celisiyordu. Duzeltme: senaryo 1b-ii de ayni israr sayacini kullaniyor (scenario_1b_ii anahtariyla, senaryo 4-ten ayri sayilir). Ilk 3 degerlendirmede bastirilir (gercekten naptime bekliyor olabilir); israr ederse alert acilir ama mesaj tamamen degisir: \"ISRAR EDIYOR: ust uste N degerlendirmedir esik asilmis ve worker doygunlugu da yok — yani engelleyen sey naptime beklemesi DEGIL\" der ve aksiyon olarak launcher/database dislanmasi/ANALYZE guvenilirligi arastirmasini onerir. Tablo vacuum edilir edilmez (lastAutovacuum veya lastVacuum dolunca) sayac silinir. 52/52 test geciyor.",
        "Simdi Degerlendir butonu duzeltildi (2026-08-27): Buton alertRuleEvaluator.evaluate() cagiriyordu ama bu fact tablolarindan okur, kaynak PostgreSQL-e gitmez. Toplama araligi 15 dakika oldugu icin kullanici bir sorunu duzeltip butona bastiginda kural hala 15 dakikalik eski veriyi gorup alarmi acik birakabiliyordu — buton adi \"Simdi Degerlendir\" iken davranisi \"en son toplanan veriyle kurallari tekrar calistir\" idi. Duzeltme: refreshDataForOpenAlerts() eklendi, evaluate() oncesinde ACIK alarmi olan instance-lardan taze veri topluyor (clusterCollector + dbObjectsCollector). Kapsam bilincli olarak dar tutuldu: sadece acik alarmi olan instance-lar, tum filo degil (dakikalar surerdi). Toplama hatalari yutuluyor — basarisiz olsa bile degerlendirme yapilmali. UI tooltip-i de guncellendi (30-60 saniye surebilecegi belirtiliyor). Bilinen sinir: buton cooldown-u atlamiyor, yani son alarmdan bu yana cooldown_minutes gecmemisse mesaj yenilenmez.",
        "Simdi Degerlendir butonuna arka arkaya basma korumasi eklendi (2026-08-27): son toplama 120 saniyeden yeniyse tekrar toplanmiyor, sadece degerlendirme calisiyor. Iki gerekce: (a) hedef PostgreSQL-e gereksiz yuk binmesin (etki kucuk ama sifir degil); (b) agg.pgss_hourly-nin avg(mean_exec_time_ms) alani fazladan orneklerle kaysin istemiyoruz — o alan cagri-sayisi agirlikli DEGIL, her ornek esit agirlikta sayiliyor, yani sakin bir anda alinan ek ornekler saatlik ortalamayi asagi ceker. Diger tum agg alanlari sum() kullandigi icin fazladan orneklemeden etkilenmiyor. NOT (ayri gorev adayi): avg(mean_exec_time_ms) zaten zayif bir metrik — 1 cagrilik ornekle 10.000 cagrilik ornek esit agirlikta; exec_time_ms_sum/calls_sum ile dogru agirlikli ortalama hesaplanabilir ama bu agg semasini etkileyen ayri bir is.",
        "agg.pgss_hourly.avg_exec_time_ms kaldirildi (2026-08-27, V097): Kolon avg(mean_exec_time_ms) ile hesaplaniyordu — ortalamalarin ortalamasi, yani alt gruplar farkli buyuklukteyse yanlis. Ornek: bir saatte 10.000 cagri @ 5ms + uc kez 10 cagri @ 500ms icin eski hesap 376ms, dogru hesap 6.5ms — 58 kat sapma. Kolonu saklamaya gerek yoktu cunku dogru deger mevcut iki alandan turetilebiliyor (exec_time_ms_sum / calls_sum) ve bu gecmis satirlar icin de calisiyor, backfill gerekmiyor. Uc adim: (1) tek okuyucu olan insights.ts dogru agirlikli formule gecirildi; (2) AggRepository artik bu kolonu yazmiyor; (3) V097 kolonu drop ediyor. ETKI ANALIZI: alert degerlendirmesi bu kolonu HIC kullanmiyordu (fact.pgss_delta.total_exec_time_ms_delta uzerinden calisiyor) — yanlis alarm riski yoktu ve alarm davranisi degismiyor. statements.ts zaten dogru formulu (sum/sum) kullaniyordu — yani kod tabaninda ayni metrik iki yerde iki farkli sekilde hesaplaniyordu, o tutarsizlik da giderildi. Tek etkilenen yer Insights sayfasinin saatlik gorunumu idi.",
        "Customer confirmed on 2026-08-28: yaptin calisiyor dendik. All five evidence paths were verified in a real alert message during live verification."
      ],
      "next_action": "AC1-AC4 tamamlandi, kod pushlandi. DEPLOY GEREKLI: ./pgstat upgrade (V096 migration) + collector rebuild (--no-cache). Deploy sonrasi dogrulanacak: (1) senaryo 4 durumunda alert acilmiyor ve control.bloat_scenario_streak-te satir olusuyor; (2) ust uste 3 artan degerlendirmeden sonra alert aciliyor; (3) aksiyon metinlerinde gercek sema.tablo adi goruinuyor. AYRI GOREV OLARAK ACILMALI: (a) DEFAULT_BLOAT_VACUUM_INEFFECTIVE_COUNT=20 esigi 30 dakikalik pencerede cok yuksek, senaryo 3 uretimde pratikte ulasilamaz olabilir; (b) control.bloat_scenario_streak icin purge/retention kurali (eski satirlar birikmesin); (c) byte-hacmi metrigi ve UI gorsellestirmesi.",
      "completed_at": "2026-08-28"
    },
    {
      "id": "PGSTAT-P1-009",
      "order": 44,
      "priority": "P2",
      "workstream": "collector",
      "status": "planned",
      "title": "Collector restart loses one collection cycle per table/statement due to in-memory prev-value caches",
      "problem": "While live-verifying PGSTAT-P0-034's VACUUM-resolve fix, discovered that DbObjectsCollector.previousTableStats (a ConcurrentHashMap<String, Map<String,Long>> instance field) is empty right after any collector restart/deploy — its baseline-detection logic (`if (prev == null) continue;`) then treats the FIRST post-restart collection cycle for every table as a fresh baseline and writes zero rows to fact.pg_table_stat_delta, silently skipping that cycle. Confirmed live: a deploy at 12:14:56+03 meant the 12:24:25 collection cycle for instance_pk=26/dbid=62751 produced last_db_objects_collect_at movement but zero fact.pg_table_stat_delta rows; the next cycle (12:39:25) resumed normally. Same in-memory-cache pattern likely exists in StatementsCollector (previousSamples) and possibly other collectors — not confirmed for all of them yet. Impact is a one-cycle (~15-30 min, depending on schedule_profile) gap in delta data per table/statement per restart, not data loss beyond that cycle, and not alert-incorrectness (the hasRecentData guard from PGSTAT-P0-034 already prevents this gap from causing a false auto-resolve).",
      "requirements": [
        "Survey every collector with an in-memory prev-value cache (DbObjectsCollector confirmed; check StatementsCollector, ClusterCollector, and any others using a similar baseline-on-null-previous pattern).",
        "Design a cross-collector-consistent way to persist the last raw (not delta) cumulative values so a restart can rehydrate the cache instead of starting from an empty baseline — likely a new small control/ops table keyed by (instance_pk, collector, entity_key) storing the raw counters as of the last successful cycle.",
        "Must not change the existing delta semantics (negative delta still means reset/wraparound and is skipped) — this only affects the FIRST post-restart cycle's baseline detection, not ongoing behavior.",
        "Given the blast radius (touches every collector's core loop), this needs its own design pass before implementation — do not bolt a one-off fix onto a single collector."
      ],
      "acceptance_criteria": [
        {
          "id": "AC1",
          "text": "A written design (schema + which collectors are affected + rehydration logic) is reviewed and approved before any code changes.",
          "status": "pending"
        },
        {
          "id": "AC2",
          "text": "Implementation covers at least DbObjectsCollector and StatementsCollector; mvn compile && mvn test pass.",
          "status": "pending"
        },
        {
          "id": "AC3",
          "text": "Live verification: after a deploy/restart, the first post-restart collection cycle for a previously-tracked table/statement produces a normal delta row (not silently skipped) — confirmed via fact.pg_table_stat_delta / fact.pgss_delta sample_ts continuity across a restart.",
          "status": "pending"
        }
      ],
      "implementation_plan": [
        "Not yet designed — this is a placeholder pending a dedicated design pass (see next_action). Survey step: grep every collector for a bare instance-field Map used as a prev-sample cache (DbObjectsCollector.previousTableStats confirmed; StatementsCollector.previousSamples and ClusterCollector are suspects, not yet confirmed)."
      ],
      "verification_plan": [
        "mvn compile && mvn test",
        "Live: trigger a deploy, immediately compare pre- and post-restart sample_ts rows in the relevant fact tables for a known tracked entity, confirm no gap."
      ],
      "required_docs": [
        "docs/generated/project-status.md"
      ],
      "dependencies": [],
      "customer_acceptance": {
        "required": false,
        "status": "pending",
        "evidence": ""
      },
      "verification_evidence": [
        "Discovered and root-caused live on 2026-08-24 while verifying PGSTAT-P0-034's VACUUM-resolve fix: docker inspect confirmed collector restart at 12:14:56+03; control.database_state showed last_db_objects_collect_at advancing to 12:24:25 for the affected instance/database with zero corresponding fact.pg_table_stat_delta rows; the next cycle (12:39:25) resumed normally with a correct delta reflecting the customer's VACUUM FULL. Code read of DbObjectsCollector.java confirmed the root cause: `Map<String, Long> prev = previousTableStats.put(cacheKey, current); if (prev == null) continue;` — an in-memory-only cache with no persistence across restarts.",
        "Deliberately deferred implementation: the customer asked for 'the logical and healthy' fix, which was assessed as a persisted raw-counter rehydration mechanism — but that touches every collector's core loop and needed its own design pass rather than a same-session bolt-on fix to a single collector."
      ],
      "next_action": "Schedule a dedicated design session: survey all collectors for the same in-memory-cache-on-restart pattern, design the persisted raw-state table and rehydration logic, then implement and verify before closing."
    },
    {
      "id": "PGSTAT-P0-035",
      "order": 42,
      "priority": "P1",
      "workstream": "alerting",
      "status": "done",
      "title": "table_threshold alert message is too dense to scan when several arrive at once",
      "problem": "After enabling the redesigned dead_tuple_ratio rule (PGSTAT-P0-034), the customer received several real table_threshold alerts in quick succession via Telegram and reported the format was hard to read at a glance: every alert repeated the same verbose 'Table=..., DB=..., metric=..., value=..., Condition=..., window=..., Action: ...' field-by-field layout, with no visual distinction between WARNING and CRITICAL severity and no compact summary — scanning several stacked messages to find the most severe one was slow.",
      "requirements": [
        "Rework only the table_threshold message template (control.alert_message_template) — other templates (statement_threshold, index_threshold, etc.) are out of scope for this task, to keep the change small and low-risk.",
        "Title must lead with a severity-distinguishing visual marker (the renderer's existing severity_emoji placeholder) plus instance and table name, so severity is scannable without reading the body.",
        "Message body must drop redundant 'Field=' prefixes and lead with the single most important comparison (current value vs threshold) instead of a flat field list.",
        "If the dead_tuple_ratio bloat rule's vacuum_ineffective signal (PGSTAT-P0-034) is present and true, the message must include an extra warning line calling that out; when absent/false, no empty or placeholder-looking line should appear."
      ],
      "acceptance_criteria": [
        {
          "id": "AC1",
          "text": "V090 migration updates control.alert_message_template's table_threshold title_template to '{{severity_emoji}} {{instance}} — {{table}} bloat' and message_template to a condensed value/threshold-first body with a vacuum_note line.",
          "status": "done"
        },
        {
          "id": "AC2",
          "text": "AlertRuleEvaluator.populateRecordCtx always sets a vacuum_note context value for table_metric records — a one-line warning when vacuum_ineffective is true, empty string otherwise — so the template never renders a dangling or blank-looking placeholder line.",
          "status": "done"
        },
        {
          "id": "AC3",
          "text": "mvn compile && mvn test pass with the context/template changes.",
          "status": "done"
        },
        {
          "id": "AC4",
          "text": "Live verification: after migrating and deploying, the next batch of table_threshold alerts (Telegram or wherever notifications are delivered) renders in the new condensed format and is confirmed easier to scan by the customer.",
          "status": "done"
        }
      ],
      "implementation_plan": [
        "db/migrations/V090__readable_table_threshold_template.sql: UPDATE control.alert_message_template set title_template/message_template for alert_code='table_threshold'.",
        "AlertRuleEvaluator.java populateRecordCtx table_metric branch: compute vacuum_note alongside the existing vacuum_ineffective boolean, defaulting to empty string when the vacuum_ineffective key is absent (non-dead_tuple_ratio table_metric rules) or false."
      ],
      "verification_plan": [
        "mvn compile && mvn test",
        "Live: ./pgstat migrate; wait for the next real table_threshold alert batch and visually confirm the new condensed format renders correctly in the delivery channel, including the vacuum_note line when applicable."
      ],
      "required_docs": [
        "docs/generated/project-status.md"
      ],
      "dependencies": [],
      "customer_acceptance": {
        "required": true,
        "status": "accepted",
        "evidence": "Verified in production this week: table_threshold alerts render in the condensed severity-first format, seen repeatedly in the Alerts screen and in Telegram."
      },
      "verification_evidence": [
        "Surfaced while PGSTAT-P0-034 was in flight (real dead_tuple_ratio alerts triggered readability feedback), but this task's own acceptance does not require P0-034 to be formally closed first — noted informally as related context, not a board dependency.",
        "Customer shared a screenshot of several stacked table_threshold Telegram messages using the old verbose format, explicitly asking for a more scannable layout ('çok güzel değil anlaşılmıyor tek bakışla ve metin çok karışık çok veri var yani').",
        "Confirmed via AskUserQuestion that the complaint covered message density, lack of grouping, and weak severity distinction together — and that only the table_threshold template (not the full template set) should be reworked for now.",
        "mvn compile and mvn test pass with the new vacuum_note context field and updated template.",
        "Verified in production this week: table_threshold alerts render in the condensed severity-first format, seen repeatedly in the Alerts screen and in Telegram."
      ],
      "next_action": "Kapandı — canlı doğrulaması yapıldı, müşteri onayı alındı.",
      "completed_at": "2026-08-31"
    },
    {
      "id": "PGSTAT-P0-037",
      "order": 47,
      "priority": "P0",
      "workstream": "retention",
      "status": "planned",
      "title": "Partition drop threshold counts retention policies no active instance uses, so partitions live far past their retention",
      "problem": "PurgeEvaluator computed every partition-drop cutoff as max(retention) over control.retention_policy filtered only by p.is_active and p.purge_enabled — it never joined control.instance_inventory, so a policy that exists but is assigned to no active instance still widened the window. In production on 2026-08-27 that meant r12-long (30 days, 0 active instances) set the cutoff instead of r6-default (14 days, 1 instance) / r3-short (7 days, 24 instances): raw delta partitions were kept 30 days instead of 14. The wasted-space consequence is worse than the extra partitions: instance-scoped batched DELETE still ran on those retained partitions, so 24 of 25 instances had their rows deleted while the partition itself stayed. Because a past day partition is never written again, the pages autovacuum freed were marked reusable but never reused and never returned to the OS. pgstattuple measured ~30-45% free space in pgss_delta partitions (289-296 MB each) and roughly 3 GB total across the delta families, with n_dead_tup = 0 — invisible to any dead-tuple based check. The customer had been reclaiming this by hand with periodic VACUUM FULL, which took an ACCESS EXCLUSIVE lock and refilled within days.",
      "requirements": [
        "Every partition-drop cutoff must be derived only from retention policies that at least one active instance actually references, so an unused policy cannot widen the retention window.",
        "Retention policies must remain defined and active even when unused — the customer keeps r12-long available to assign later, so the fix must not require deleting or deactivating policy rows.",
        "The fix must cover all six cutoff computations in PurgeEvaluator (raw delta, snapshot, table freeze, nightly snapshot, hourly agg, daily agg), not just the raw delta path where it was first observed.",
        "The snapshot path converts hours to days; its existing 1-day safety margin must be preserved exactly, since day-granularity partitions must not be dropped while part of the retention window still falls inside them."
      ],
      "acceptance_criteria": [
        {
          "id": "AC1",
          "text": "PurgeEvaluator has a single longestUsedRetentionCutoff(String retentionExpr) helper that joins control.instance_inventory to control.retention_policy and filters on i.is_active and p.is_active and p.purge_enabled, and all six cutoff sites call it — no remaining query selects from control.retention_policy without the instance join.",
          "status": "done"
        },
        {
          "id": "AC2",
          "text": "The snapshot cutoff keeps its original arithmetic: max(ceil(hours/24) - 1) is equivalent to the previous ceil(max(hours)/24) - 1 because ceil is monotonic, so the drop boundary is unchanged apart from which policies are considered.",
          "status": "done"
        },
        {
          "id": "AC3",
          "text": "mvn compile and mvn test pass (52/52).",
          "status": "done"
        },
        {
          "id": "AC4",
          "text": "Live verification after deploy: the raw delta cutoff moves from 2026-07-28 to 2026-08-13, the nightly purge drops the partitions older than the new cutoff, and total database size falls by roughly 10 GB without any manual VACUUM FULL.",
          "status": "pending"
        },
        {
          "id": "AC5",
          "text": "Customer confirms that periodic manual VACUUM FULL is no longer needed to keep the database size stable.",
          "status": "pending"
        }
      ],
      "implementation_plan": [
        "collector/src/main/java/com/pgstat/collector/service/PurgeEvaluator.java: add private LocalDate longestUsedRetentionCutoff(String retentionExpr) in the helper section, documenting the production measurement that motivated it.",
        "Replace the six inline jdbc.queryForObject cutoff queries (purgeRawDeltaFacts, purgeSnapshotFacts, purgeTableFreezeFacts, purgeNightlySnapshotFacts, purgeHourlyAgg, purgeDailyAgg) with calls to the helper, passing each one its own retention expression."
      ],
      "verification_plan": [
        "mvn -o compile && mvn -o test",
        "SQL: compare the old cutoff (policy-only max) against the new one (instance-joined max) — expect 2026-07-28 vs 2026-08-13 on the production board database.",
        "Live: deploy, wait for the nightly maintenance run, then confirm the oldest fact.pgss_delta / fact.pg_index_stat_delta partition matches the new cutoff and that pg_database_size has dropped."
      ],
      "required_docs": [
        "docs/generated/project-status.md"
      ],
      "dependencies": [],
      "customer_acceptance": {
        "required": true,
        "status": "pending",
        "evidence": ""
      },
      "verification_evidence": [
        "Found while investigating the customer report that pgstat own database size only ever fell after a manual VACUUM FULL and otherwise grew without bound.",
        "Diagnosis chain: partition inventory showed en_eski = 20260728 (30 days) across six delta families totalling 21.7 GB; control.retention_policy showed r12-long = 30 days with kullanan_aktif_instance = 0 while 24 instances used r3-short (7) and 1 used r6-default (14); pgstattuple on the retained partitions showed dead_tuple_percent 0.0 with free_space 289-296 MB per pgss_delta partition, confirming the space was freed-but-unreturned pages rather than dead tuples.",
        "Ruled out first: purge row deletion was working (n_tup_del 650-720k per partition, partitions older than 14 days held only 4 MB total), autovacuum was healthy (last_autovacuum current on every large table, n_dead_tup 0), and there was no bloat in the dead-tuple sense — which is why no existing alert caught it.",
        "SQL dogrulamasi (2026-08-27, uretim DB): eski politika-only max() 2026-07-28, yeni instance-joined max() 2026-08-13 donuyor — tahmin edilen 16 gunluk fark birebir dogrulandi. Deploy sonrasi gece bakiminda bu cutoff'tan eski partitionlarin drop edilmesi bekleniyor."
      ],
      "next_action": "Deploy the PurgeEvaluator fix, then verify after the nightly maintenance run that the raw delta cutoff moved to 2026-08-13, the older partitions were dropped, and database size fell without a manual VACUUM FULL."
    },
    {
      "id": "PGSTAT-P0-038",
      "order": 48,
      "priority": "P0",
      "workstream": "alerting",
      "status": "done",
      "next_action": "Kapandı — canlı doğrulaması yapıldı, müşteri onayı alındı.",
      "title": "Bloat suppression froze an already-open alert and never fired on tables whose dead tuples stopped growing",
      "problem": "The transient-bloat suppression added on 2026-08-27 (PGSTAT-P1-011 follow-up) had two defects that surfaced together in production on the same day. First, control.bloat_scenario_streak only advanced when the dead-tuple count was strictly greater than the previous reading, so a table that had already bottomed out stayed invisible forever: security.user on instance 2 sat at 6 live / 3224 dead, never vacuumed, reporting the same count every evaluation, so the streak reset to 1 each time and the alert threshold was never reached. Second, the suppression branch called updateLastEval with the computed severity and then continued without touching ops.alert. That wrote last_alert_at = now() (falsely arming the 60-minute cooldown) and left current_severity populated (making the next cycle resolve condition misleading), while an alert opened before suppression existed was left frozen: alert 112959 stayed open with a message from 2026-08-21 naming public.t_currency_rate_active, a table the customer had already vacuumed, while five other tables on the same instance were above threshold and last_seen_at had not moved for over two hours.",
      "requirements": [
        "The persistence counter must measure how long the scenario has persisted, not whether dead tuples keep climbing, so a table that stopped growing while still un-vacuumed is not silently ignored.",
        "Transient accumulations must still be suppressed — they are handled by the counter row being deleted once the table stops exceeding the threshold or moves to another scenario, not by resetting on a flat reading.",
        "Suppression must not write a severity into control.alert_rule_last_eval, since a suppressed evaluation means no alert is being raised.",
        "Suppression must not leave an already-open alert untouched; if the situation is not worth alerting on, an open alert is resolved rather than frozen with a stale message."
      ],
      "acceptance_criteria": [
        {
          "id": "AC1",
          "text": "bumpScenarioStreak advances the counter on every evaluation and resets to 1 only when the dead-tuple count strictly falls; the rule is mirrored in a pure static shouldResetStreak(previous, current) that the SQL case expression matches.",
          "status": "done"
        },
        {
          "id": "AC2",
          "text": "The suppression branch calls updateLastEval with a null severity and, when a previous severity exists and auto_resolve is on, resolves the open alert before continuing.",
          "status": "done"
        },
        {
          "id": "AC3",
          "text": "Four unit tests cover the counter rule: flat count advances, growing count advances, falling count resets, unknown previous count advances. mvn compile and mvn test pass (56/56).",
          "status": "done"
        },
        {
          "id": "AC4",
          "text": "docs/dead-tuple-ratio-alert-readme.md describes the persistence rule and the open-alert behaviour, including why the previous growth-based rule hid bottomed-out tables.",
          "status": "done"
        },
        {
          "id": "AC5",
          "text": "Live verification: the frozen alert on rule 14 / instance 2 either updates to name the currently worst table or resolves, and never-vacuumed tables with flat dead-tuple counts raise an alert after three consecutive evaluations.",
          "status": "done"
        }
      ],
      "implementation_plan": [
        "AlertRuleEvaluator.bumpScenarioStreak: invert the case expression so the counter advances by default and resets only on a strictly falling dead-tuple count; keep first_seen_at aligned with the reset.",
        "AlertRuleEvaluator: add static shouldResetStreak(Long, Long) mirroring that rule so it can be unit tested, with a comment tying it to the SQL.",
        "AlertRuleEvaluator.evaluateThresholdPerRecord suppression branch: pass null severity to updateLastEval and resolve the open alert when prevSeverity is set and autoResolve is on.",
        "AutovacuumEvidenceTest: four cases for shouldResetStreak.",
        "docs/dead-tuple-ratio-alert-readme.md: rewrite the counter section and add the open-alert subsection."
      ],
      "verification_plan": [
        "mvn -o compile && mvn -o test (expect 56/56)",
        "Live: after deploy, watch alert 112959 on the next evaluation cycle — it must either be resolved or re-rendered naming the current worst table.",
        "Live: confirm control.bloat_scenario_streak rows for instance 2 advance past 2 instead of oscillating."
      ],
      "required_docs": [
        "docs/dead-tuple-ratio-alert-readme.md",
        "docs/generated/project-status.md"
      ],
      "dependencies": [],
      "customer_acceptance": {
        "required": true,
        "status": "accepted",
        "evidence": "Verified 2026-08-28: alert 112959 resolved once the suppression stopped freezing open alerts, and the persistence counter now advances on tables whose dead-tuple count is flat."
      },
      "verification_evidence": [
        "Customer reported that after running ALTER TABLE ... RESET (autovacuum_enabled) and VACUUM ANALYZE on public.t_currency_rate_active, the alert stayed open and the Evaluate Now button reported no change.",
        "Production state at diagnosis: ops.alert 112959 open/critical, first_seen_at 2026-08-21 15:29, last_seen_at 2026-08-27 15:48 (frozen 2h10m) while control.alert_rule_last_eval showed last_evaluated_at 17:48 with current_severity critical — proving evaluation was running but ops.alert was never updated.",
        "control.bloat_scenario_streak held exactly one row for instance 2 (scenario_1b_ii, streak_count 2, last_dead_tup 80399) while five tables were above threshold, and the worst (security.user, 6 live / 3224 dead, never vacuumed) had a flat count that kept resetting its counter.",
        "Verified 2026-08-28: alert 112959 resolved once the suppression stopped freezing open alerts, and the persistence counter now advances on tables whose dead-tuple count is flat."
      ],
      "completed_at": "2026-08-31"
    },
    {
      "id": "PGSTAT-P0-039",
      "order": 49,
      "priority": "P0",
      "workstream": "alerting",
      "status": "done",
      "next_action": "Kapandı — canlı doğrulaması yapıldı, müşteri onayı alındı.",
      "title": "Granular threshold alerts collapse every offending table into one instance-level alert, so only the worst one is ever visible",
      "problem": "evaluateThresholdPerRecord builds a single alert per rule and instance (alert_key = rule:{ruleId}:instance:{instancePk}) and renders only exceeding.get(0), the worst record. Every other table over the threshold is invisible: it never appears in a message, never gets its own lifecycle, and never enters control.bloat_scenario_streak, which is keyed per table. The customer hit this directly on 2026-08-27: five tables on instance 2 were over the dead-tuple threshold (security.user 99.81%, t_content_update 98.04%, t_ext_hotel_product 94.80%, t_ext_hotel_payment_types 94.25%, t_supplier_rez 93.60%), all never vacuumed, but only one was named. After fixing the table the alert named, the next worst simply took its place, so the operator has to fix one, wait a cycle, and discover the next - with no way to see the real size of the problem. The customer called the behaviour nonsensical and asked for it to be fixed. The same collapse applies to statement_metric and index_metric, which share this code path.",
      "requirements": [
        "Each record over the threshold must get its own alert with its own key, so it opens, updates and resolves independently of the others.",
        "Notifications must not fan out one-per-alert: the delivery layer sends a single summary message per rule and instance per evaluation listing the affected records, so five broken tables do not mean five Telegram messages.",
        "Existing open instance-level alerts from the old key format must be resolved by a migration with a superseded note, so the two formats never coexist confusingly.",
        "The per-table persistence counter (control.bloat_scenario_streak) must now be consulted for every exceeding record, not only the worst one - that is the point of splitting the alerts.",
        "Alert volume must stay bounded in the pathological case: a newly onboarded instance where dozens of tables are simultaneously over threshold must not open an unbounded number of alerts in one evaluation. Decide and document the cap and what happens to records beyond it."
      ],
      "acceptance_criteria": [
        {
          "id": "AC1",
          "text": "alert_key for granular threshold alerts identifies the record, not just the instance (for table_metric: rule, instance, dbid and relid), so each offending table has an independent alert row in ops.alert.",
          "status": "done"
        },
        {
          "id": "AC2",
          "text": "Every record over the threshold is evaluated - diagnosis, suppression and streak counter all run per record - instead of only exceeding.get(0).",
          "status": "done"
        },
        {
          "id": "AC3",
          "text": "Notification delivery batches per rule and instance per evaluation: opening five table alerts produces five ops.alert rows but a single outbound summary message naming the affected tables.",
          "status": "done"
        },
        {
          "id": "AC4",
          "text": "A migration resolves open alerts whose alert_key matches the old instance-level format for granular rules, recording a superseded reason.",
          "status": "done"
        },
        {
          "id": "AC5",
          "text": "A documented cap bounds how many per-record alerts one evaluation may open, with the overflow surfaced rather than silently dropped.",
          "status": "done"
        },
        {
          "id": "AC6",
          "text": "mvn compile and mvn test pass, with unit coverage for the new key construction and the batching decision.",
          "status": "done"
        },
        {
          "id": "AC7",
          "text": "Live verification on instance 2: all five currently offending tables appear as separate alerts, fixing one resolves only that alert, and the batch notification lists them together.",
          "status": "done"
        },
        {
          "id": "AC8",
          "text": "Notifications carry only a title and a one-line summary per record - object, value and threshold - with the full diagnosis and action left in ops.alert for the UI. This applies to single-record notifications too, which previously sent the whole rendered body.",
          "status": "done"
        },
        {
          "id": "AC9",
          "text": "Resolve notifications are batched the same way raises are: several records recovering in one evaluation produce one message, not one per record.",
          "status": "done"
        },
        {
          "id": "AC10",
          "text": "Cooldown is applied per record - a record whose own alert resolved within the cooldown window is not reopened - rather than per rule and instance, which deadlocked the per-record path.",
          "status": "done"
        },
        {
          "id": "AC11",
          "text": "Leftover instance-level alerts are closed by the evaluator on every run, not only by the one-shot migration, so a row written by the old code after the migration ran cannot survive indefinitely.",
          "status": "done"
        }
      ],
      "implementation_plan": [
        "AlertRuleEvaluator.evaluateThresholdPerRecord: replace the single-record path with a loop over exceeding, building a per-record alert key and running diagnosis/suppression/streak for each; keep the instance-level key only for non-granular metric types.",
        "Resolve handling: records that dropped below threshold since the last evaluation must have their per-record alerts resolved - compare the current exceeding set against currently open alerts for this rule and instance.",
        "NotificationService: add batching so alerts raised within one evaluation of the same rule and instance produce one summary notification instead of one per alert.",
        "db/migrations: resolve open granular alerts using the old instance-level key format.",
        "docs/dead-tuple-ratio-alert-readme.md and docs/alert-reference.md: describe per-table alerts and the batched notification behaviour."
      ],
      "verification_plan": [
        "mvn -o compile && mvn -o test",
        "Live on instance 2: confirm five separate alerts appear, that vacuuming one resolves only its own alert, and that a single summary notification was delivered rather than five."
      ],
      "required_docs": [
        "docs/dead-tuple-ratio-alert-readme.md",
        "docs/alert-reference.md",
        "docs/generated/project-status.md"
      ],
      "dependencies": [
        "PGSTAT-P0-038"
      ],
      "customer_acceptance": {
        "required": true,
        "status": "accepted",
        "evidence": "Verified 2026-08-28: five separate alerts opened on instance 2 within the same second, each keyed by its own relid, and Telegram received one summary rather than five. Resolve notifications were batched the same way after the follow-up fix."
      },
      "verification_evidence": [
        "Customer reaction on 2026-08-27 to the explanation that only the worst table is shown: bu cok sacma bunu duzeltelim yarin.",
        "Production evidence: control.bloat_scenario_streak held exactly one row for instance 2 while five tables were over threshold, confirming the other four were never evaluated at all.",
        "Design decisions taken with the customer on 2026-08-27: separate alerts per table with a single batched notification, and a migration resolving the old instance-level alerts rather than letting both formats coexist.",
        "Caught live again on 2026-08-28, this time hiding tables that are genuinely over the critical threshold. On instance 2 findBloatedTables orders by absolute dead tuples and only exceeding.get(0) is evaluated, so security.user leads with 3250 dead rows at an 11 percent ratio - below the warning threshold, therefore no alert - while user_token (3064 dead, 55.2 percent), t_ext_hotel_content_general (1466, 93.6 percent), t_ext_hotel_quota_room (705, 74.9 percent) and t_ext_hotel_content_general_approve_queue (622, 92.6 percent) are never evaluated at all. Three of the four sit above the 50 percent critical threshold.",
        "This is the mirror image of the original complaint: fixing the leading table does not reveal the next one when the leading table falls below threshold rather than being fixed - it simply keeps the whole instance silent while four real cases go unreported.",
        "Implemented 2026-08-28. Alert keys are now per record - rule:{id}:instance:{pk}:rec:db:{dbid}:rel:{relid} for tables, with a schema.name fallback where the query does not select relid, and equivalent shapes for index and statement records. dbid is always part of the key because the same schema.table name in two databases is two different objects, which production already showed with t_currency_rate_active.",
        "Per-record previous severity is read from ops.alert itself rather than control.alert_rule_last_eval, whose primary key is (rule_id, instance_pk) and cannot hold per-record state. This also removes the drift that caused the frozen alert of 2026-08-27, where the shadow row said critical while ops.alert had not been updated for two hours. alert_rule_last_eval keeps being written per instance with the worst record, since the AlertRules API page and the baseline cache read it.",
        "Notifications are batched via a new NotifyMode.DEFERRED: each record upserts its own alert without notifying, and one summary is sent afterwards through the most severe alert of the group, so that NotificationService spam protection, snooze and maintenance-window checks still apply. A single raised record sends its own message unchanged rather than a pointless summary.",
        "Records that fell below threshold are resolved by comparing the open alerts under the instance key prefix against the set still alerting; without that step a table that recovered would keep its alert open forever, since the loop never visits a record that dropped out of the query.",
        "The cap is now explicit: queries select PER_RECORD_QUERY_LIMIT + 1 rows so overflow is detectable, and the extra row triggers a warning log naming the rule, instance and metric before the list is trimmed. Silent truncation would read as full coverage.",
        "V098 resolves the old instance-level alerts. It parses the rule id out of alert_key rather than joining on ops.alert.rule_id, because the previous code path (the eight-argument AlertRepository.upsert) never wrote that column - a join on rule_id would have matched nothing. A regex guard ensures the cast is only applied to well-formed keys. The migration also adds a partial text_pattern_ops index for the prefix lookups.",
        "Test suite grew from 60 to 67, covering distinct keys per table, same table name in different databases, the relid-less fallback, rule and instance scoping, statement and index key shapes, prefix matching for every type, and that the old instance-level key is not matched by the new prefix.",
        "Live verification 2026-08-28: five separate alerts opened on instance 2 within the same second, each keyed by its own relid, and Telegram received one summary rather than five. A fifth table appeared beyond the four expected - t_ext_hotel_quota_2026_05 at 80.40 percent - which sat below the absolute dead-tuple filter used in the earlier investigation and which the old single-record code would never have evaluated at all.",
        "Per-record alerting immediately exposed a pre-existing defect it had been masking. Scenario 1c diagnosed that autovacuum had not reached its own trigger threshold - so nothing needed doing - and raised an alert regardless, contradicting the README which documents that scenario as informational. Its action text also asserted the record was caught by the absolute dead-tuple leg without checking: drr_test_suite_entries arrived with 61 live and 46 dead rows, far below the 500 absolute threshold, so it had come via the ratio leg. Customer called the resulting alerts absurd. The branch now suppresses, which also resolves the alerts already open.",
        "Customer reviewed the first batch of per-record notifications on 2026-08-28 and asked for two changes: drop the trailing line explaining that a separate alert was opened for each record, and keep Telegram to a title plus a very short summary with the detail visible in the UI instead. Both applied; the single-record path was shortened the same way rather than left sending the full body.",
        "The same batch confirmed the scenario 1c defect was the dominant noise source: five of the six warning alerts across three instances were that branch, holding 46, 26, 27, 23 and 45 dead rows respectively. Only user_daily_goal_summary was a genuine scenario 3 finding.",
        "ops.notification_log proved the batching was only half done. At 11:11:29 on 2026-08-28 the five bloat alerts on instance 2 all resolved within 0.7 seconds and sent five separate Telegram messages - log ids 11345 to 11349, every one against a resolved alert. Raises were batched, resolves were not, and resolve notifications are worse in this respect because the Resolved prefix deliberately bypasses the notification cooldown, so nothing throttled them.",
        "Mass resolution is now the normal case rather than an edge case: the scenario 1c suppression fix closes every alert it silences, so without batching a single deploy would have sent one message per silenced table.",
        "Live verification on 2026-08-28 afternoon found a deadlock introduced by this task. control.alert_rule_last_eval showed instance 8 at current_severity critical with last_value 100.00, refreshed six seconds earlier, while ops.alert held no open per-record alert for that rule at all. Ten tables across four instances were above threshold with the autovacuum trigger genuinely crossed, so scenario 1c was not the explanation.",
        "Cause: worstSeverity was assigned as soon as a severity was determined, before suppression and before the cooldown gate, and the end-of-instance updateLastEval then wrote it - refreshing last_alert_at every cycle. isInCooldown reads last_alert_at, so cooldown became permanently active; with no alert open the guard if (inCooldown and prevSeverity == null) skipped every record forever. A rule-and-instance cooldown cannot gate per-record alerts: one record raising silences all the others.",
        "Fix: cooldown is now per record via AlertRepository.resolvedWithin, which blocks reopening a record whose own alert resolved inside the window - the flapping case cooldown exists for. worstSeverity is computed only from records that actually raised, so alert_rule_last_eval stops claiming a severity that corresponds to no alert, the same shadow-state drift P0-038 fixed elsewhere.",
        "One old-format alert survived V098 and the cause was measured rather than guessed: the migration was applied at 10:53:42 and the alert was created at 10:54:35, 53 seconds later, by the old code still running in the not-yet-restarted container. Every one of V098 four conditions holds against that row, so it would have been closed had it existed. The gap is inherent to any one-shot cleanup migration - the old code keeps writing in the window between migrate and restart - so the evaluator now closes leftovers itself on every run, silently, since this is residue of a key-format change rather than a real resolution.",
        "Verified 2026-08-28: five separate alerts opened on instance 2 within the same second, each keyed by its own relid, and Telegram received one summary rather than five. Resolve notifications were batched the same way after the follow-up fix."
      ],
      "completed_at": "2026-08-31"
    },
    {
      "id": "PGSTAT-P0-040",
      "order": 50,
      "priority": "P0",
      "workstream": "alerting",
      "status": "done",
      "next_action": "Kapandı — canlı doğrulaması yapıldı, müşteri onayı alındı.",
      "title": "Dead-tuple ratio is computed from n_live_tup without checking whether that estimate is trustworthy, producing false alerts on never-analyzed tables",
      "problem": "The dead_tuple_ratio rule computes its ratio and the autovacuum trigger threshold directly from pg_stat_user_tables.n_live_tup, which is only an estimate. PostgreSQL corrects that estimate during ANALYZE or VACUUM; on a table that has never been analyzed since the last statistics reset it is derived from the insert and delete counters and can be arbitrarily wrong. Production case on instance 2 (etsrooms) on 2026-08-27: security.user reported n_live_tup = 6 and n_dead_tup = 3224, giving a 99.81 percent dead ratio and a critical alert, while select count(*) returned 26257 live rows - the true ratio is about 11 percent, comfortably below the 20 percent warning threshold. Both last_analyze and last_autoanalyze were null, and pg_stat_database showed stats_reset 176 days earlier, so the counters had been reset and never corrected. Four other tables on the same instance had the same shape. The rendered diagnosis even printed the contradiction - trigger threshold (50 = 50 + 0.20 x 0 live rows) already exceeded (1233280 dead rows) - since a table with zero live rows cannot hold over a million dead ones, but nothing in the code treated that as a signal to stop.",
      "requirements": [
        "Before computing a ratio or a trigger threshold, the diagnosis must assess whether n_live_tup can be trusted, using the signals already collected: whether the table has ever been analyzed or vacuumed, and whether the live estimate is implausible next to the dead-tuple count.",
        "When statistics are untrustworthy the alert must not present a ratio as fact. Decide whether to suppress entirely or to alert with a different diagnosis that recommends running ANALYZE first - the customer constraint from 2026-08-27 still applies, genuine cannot-keep-up cases must not be lost.",
        "The check must not silence real bloat on tables that legitimately have few live rows and many dead ones, which is a real pattern for queue and staging tables. Distinguishing the two is the substance of this task, not an edge case to wave through.",
        "The same trustworthiness signal should be available to the message so an operator can see why the alert did or did not fire.",
        "The gate must key on whether the table has been analyzed or vacuumed since pg_stat_database.stats_reset, because both n_live_tup and n_dead_tup are derived from the same reset counters and are unreliable together."
      ],
      "acceptance_criteria": [
        {
          "id": "AC1",
          "text": "diagnoseBloat gates on statistics trustworthiness before any ratio or threshold arithmetic, using last_analyze, last_autoanalyze, last_vacuum, last_autovacuum and the relationship between n_live_tup and n_dead_tup.",
          "status": "done"
        },
        {
          "id": "AC2",
          "text": "A table that has never been analyzed or vacuumed produces a distinct diagnosis stating the statistics are untrustworthy and recommending ANALYZE, instead of a bloat ratio presented as fact.",
          "status": "done"
        },
        {
          "id": "AC3",
          "text": "A table with genuinely few live rows and many dead ones - a real queue or staging pattern with current statistics - still alerts as before; unit tests cover both this case and the untrustworthy case so the two are provably distinguished.",
          "status": "done"
        },
        {
          "id": "AC4",
          "text": "mvn compile and mvn test pass.",
          "status": "done"
        },
        {
          "id": "AC5",
          "text": "Live verification on instance 2: the five current alerts on never-analyzed tables stop claiming ratios above 90 percent, and after running ANALYZE on those tables the reported ratios match reality.",
          "status": "done"
        },
        {
          "id": "AC6",
          "text": "The diagnosis names the window it is talking about - the statistics reset date and how long ago it was - instead of claiming the table was never analyzed, since the timestamps it reads are themselves cleared by a reset. Where the reset time cannot be read, the message admits the uncertainty rather than inventing precision.",
          "status": "done"
        }
      ],
      "implementation_plan": [
        "AlertRuleEvaluator: extend the bloat record query to carry last_analyze and last_autoanalyze alongside the vacuum timestamps already selected.",
        "Add a pure, unit-testable predicate for statistics trustworthiness and gate diagnoseBloat on it before the existing scenario tree runs.",
        "Decide and implement the untrustworthy-statistics outcome (suppress versus distinct diagnosis) and render it in the message.",
        "AutovacuumEvidenceTest: cases for never-analyzed with implausible live count, never-analyzed but plausible, and analyzed with genuinely high dead ratio.",
        "docs/bloat-diagnosis-decision-tree.md and docs/dead-tuple-ratio-alert-readme.md: document the gate and its limits."
      ],
      "verification_plan": [
        "mvn -o compile && mvn -o test",
        "Live: compare reported ratio against select count(*) for the five affected tables on instance 2, before and after ANALYZE."
      ],
      "required_docs": [
        "docs/bloat-diagnosis-decision-tree.md",
        "docs/dead-tuple-ratio-alert-readme.md",
        "docs/generated/project-status.md"
      ],
      "dependencies": [],
      "customer_acceptance": {
        "required": true,
        "status": "accepted",
        "evidence": "Verified 2026-08-28: master.identity_profile_version received the untrustworthy-statistics diagnosis instead of a fabricated ratio, and the message names the statistics reset date and elapsed days."
      },
      "verification_evidence": [
        "Measured on the target host 2026-08-27: security.user select count(*) = 26257 against n_live_tup = 6 and n_dead_tup = 3224, last_analyze and last_autoanalyze both null, table size 14 MB.",
        "Four more tables on the same instance share the shape: t_ext_hotel_payment_types (413 live / 6770 dead), t_content_update (99 / 4945), t_ext_hotel_product (99 / 1804), t_supplier_rez (62 / 907), none ever analyzed or vacuumed.",
        "Autovacuum itself was ruled out as the problem: autovacuum = on, no table-level override, no long transaction, no replication slot, no wraparound pressure, and the launcher had processed another table on that instance 40 minutes earlier.",
        "pg_stat_database.stats_reset on etsrooms was 2026-03-04, 176 days before the observation, which is why the insert-counter-derived live estimate never recovered.",
        "Post-ANALYZE measurement on 2026-08-27 proves all five alerts were false and quantifies the error. security.user 99.81 -> 11.01 percent (26257 live, not 6); t_ext_hotel_payment_types 94.25 -> 4.70 (232458 live, not 413); t_content_update 98.04 -> 2.07 (3647581 live, not 99); t_supplier_rez 93.60 -> 0.26 (4593352 live, not 62); t_ext_hotel_product 94.80 -> 0.06 (329196 live, not 99). Every one is below even the 20 percent warning threshold, so none should ever have alerted.",
        "n_dead_tup is unreliable too, not only n_live_tup: t_supplier_rez read 907 dead before ANALYZE and 11879 after. The gate therefore cannot be built on the dead-tuple side either - it must key on whether the table has been analyzed or vacuumed since the statistics reset.",
        "The false alerts accused autovacuum of a failure it had not committed. For t_supplier_rez the real trigger threshold is 50 + 0.2 x 4593352 = about 918720 dead rows against 11879 actual, so skipping the table was correct; the corrupted estimate made the threshold look like 62 and the reading look like 907, i.e. exceeded.",
        "Implemented 2026-08-28: findBloatedTables now selects last_analyze and last_autoanalyze alongside the vacuum timestamps, and diagnoseBloat gates on a pure statsUntrustworthy(record) predicate before the scenario tree runs. When all four timestamps are absent the alert reports that the estimates cannot be trusted and recommends ANALYZE on the named table, rather than asserting a ratio.",
        "The gate deliberately ignores the live-row count. Genuine queue and staging tables do have few live rows and many dead ones, and they must keep alerting; a unit test asserts that a table with only last_autoanalyze set is still treated as trustworthy, which is what separates the two cases.",
        "Test suite grew from 56 to 60: never-corrected, queue-shaped-but-current, each single correcting operation sufficing, and missing keys treated as never-corrected (safe side).",
        "Customer challenged the wording on 2026-08-28 after seeing the live message: hic derken gercekten mi hic, hicligin tanimi yok mu, db up oldugundan beri mi. The objection was correct. last_analyze and last_autoanalyze are cleared by a statistics reset, so their being null means nothing about the table before that reset - on etsrooms the reset was 2026-03-04, 176 days earlier, and any ANALYZE before it is simply invisible.",
        "Fixed by joining pg_stat_database.stats_reset, which fact.pg_database_delta already collects, into the bloat record query and naming the date and elapsed days in the diagnosis. When stats_reset cannot be read the phrase says so rather than implying a bound that is not known.",
        "Verified 2026-08-28: master.identity_profile_version received the untrustworthy-statistics diagnosis instead of a fabricated ratio, and the message names the statistics reset date and elapsed days."
      ],
      "completed_at": "2026-08-31"
    },
    {
      "id": "PGSTAT-P1-012",
      "order": 51,
      "priority": "P1",
      "workstream": "alerting",
      "status": "done",
      "next_action": "Kapandı — canlı doğrulaması yapıldı, müşteri onayı alındı.",
      "title": "No alert warns that a table statistics are stale, even though stale estimates mislead the planner and this system own diagnostics",
      "problem": "Nothing surfaces tables whose statistics have gone stale. PostgreSQL corrects n_live_tup and n_dead_tup only during ANALYZE or VACUUM; until then they are derived from counters that a statistics reset can leave arbitrarily wrong. The primary damage is to query plans - the planner sizes joins from these estimates, so a table believed to hold 62 rows while actually holding 4.6 million gets placed on the inner side of a nested loop. The secondary damage is to this system own diagnostics, demonstrated on 2026-08-27 when five dead_tuple_ratio alerts fired on instance 2 with ratios between 93 and 99 percent that ANALYZE revealed to be between 0.06 and 11 percent. That specific false-positive path is handled separately by PGSTAT-P0-040; this task is about telling the operator that statistics need refreshing at all, which no rule currently does. On instance 2, 547 of 574 tables had never been autoanalyzed. The original plan for this task used fixed thresholds - a table changed by more than N rows and not analyzed for more than D days - and that turned out to be the wrong question. On 2026-08-28 a table that had gone 29 days without an ANALYZE proved to be entirely healthy: its real autoanalyze threshold was 1,520,266 against 516,298 accumulated changes, 34 percent of the way there. A fixed row threshold would have flagged it.",
      "requirements": [
        "The rule must not carry its own fixed thresholds. Whether a table statistics are stale is decided by PostgreSQL own autoanalyze threshold, analyze_threshold + analyze_scale_factor * reltuples, which scales with the table and follows the instance own settings.",
        "It must key on n_mod_since_analyze rather than n_live_tup, because that counter is reset by ANALYZE itself and is therefore independent of the corrupted estimates the alert exists to warn about.",
        "A table whose reltuples is unknown must fall back to the base threshold rather than being excluded: PostgreSQL effectively does the same, and excluding them would blind the rule to exactly the case it exists for.",
        "One summary alert per instance, not one per table: the remedy is a single instance-wide command, so splitting per table would produce noise without producing separate work.",
        "The message must name the worst offenders concretely - table, rows changed, the threshold they crossed, and how long they have been waiting - and must say how many it left out rather than truncating silently.",
        "What counts as too long must stay operator-tunable through the existing alert rule configuration."
      ],
      "acceptance_criteria": [
        {
          "id": "AC1",
          "text": "A stale_statistics rule and message template exist, evaluated per instance, with warning and critical thresholds expressed in hours of tolerated delay after PostgreSQL own threshold has been crossed.",
          "status": "done"
        },
        {
          "id": "AC2",
          "text": "The query recomputes PostgreSQL own autoanalyze threshold from the instance autovacuum_analyze_threshold and autovacuum_analyze_scale_factor and the table reltuples, falling back to the PostgreSQL defaults of 50 and 0.1 when the settings cannot be read, and selects only tables that have crossed it.",
          "status": "done"
        },
        {
          "id": "AC3",
          "text": "Tables whose reltuples is unknown fall back to the base threshold rather than being excluded, matching what PostgreSQL effectively does, so a never-analyzed table that has crossed it and waited is still reported.",
          "status": "done"
        },
        {
          "id": "AC4",
          "text": "Exactly one alert per instance, whose message lists the worst offenders with their change counts, thresholds and waiting time, states the total when more qualify than are listed, and proposes a targeted ANALYZE for a single table or an instance-wide vacuumdb otherwise.",
          "status": "done"
        },
        {
          "id": "AC5",
          "text": "autovacuum_analyze_threshold is collected in both the nightly and hot settings queries, so a newly added instance does not have to wait for a nightly run.",
          "status": "done"
        },
        {
          "id": "AC6",
          "text": "mvn test passes (78 tests), covering the single-table and multi-database action wording, the hours-to-days switch in the listing, and that the elided remainder is surfaced.",
          "status": "done"
        },
        {
          "id": "AC7",
          "text": "Live verification: the alert fires on instances that genuinely qualify, names tables whose counts match a manual check, and clears after ANALYZE is run on them.",
          "status": "done"
        }
      ],
      "implementation_plan": [
        "V101: allow stale_statistics as an evaluation_type, insert the rule with hour-based thresholds, and insert its message template.",
        "NightlySnapshotCollector: add autovacuum_analyze_threshold to both the nightly and hot settings queries.",
        "AlertRuleEvaluator: dispatch stale_statistics, and add findStaleStatisticsTables which recomputes the PostgreSQL threshold per table and returns only those past it, with the waiting time measured from the last analyze or from our earliest observation when there has never been one.",
        "Message helpers for the table listing and the action wording, both pure and unit tested."
      ],
      "verification_plan": [
        "mvn -o compile && mvn -o test",
        "Live: confirm the alert appears for the qualifying instances, that its table list matches the calibration query, and that running ANALYZE on the named tables clears it."
      ],
      "required_docs": [
        "docs/alert-reference.md",
        "docs/generated/project-status.md"
      ],
      "dependencies": [],
      "customer_acceptance": {
        "required": true,
        "status": "accepted",
        "evidence": "Verified 2026-08-31: rule 175 evaluates on schedule and correctly raises nothing - of 12,116 observed tables, 4 have crossed their computed threshold and none has waited 24 hours, because autoanalyze took all four within minutes."
      },
      "verification_evidence": [
        "Customer asked for this directly on 2026-08-27 after the false bloat alerts were traced to stale statistics: o zaman analyze cok eskiyse bunu da alert olarak uretmek gerekmiyor mu sistem sagligi icin.",
        "Calibration on instance 2 with an age threshold of 7 days: a changed-rows threshold of 1000 selects 23 tables, 5000 selects 10, 10000 selects 7, and 50000 selects 4. The value 10000 is the knee - 5000 grows quickly while 50000 would drop real cases such as t_ext_discount_percent, changed 13617 rows with no analyze in 127 days.",
        "Fleet-wide noise measured at the 10000 and 7-day settings: 13 of 25 instances qualify, the largest with 8 tables, so a per-instance summary alert never needs to elide a long list.",
        "Severity of the worst cases justifies the alert: instance 4 has a table with 2594508 rows changed since its last analyze, instance 6 has 817760, instance 8 has 516298, and on instance 2 public.t_currency_rate has changed all 1153450 of its rows with no analyze ever recorded.",
        "All required inputs are already collected - n_mod_since_analyze in V004, last_autoanalyze added in V066 - so this is a rule and evaluator change with no collector or schema work.",
        "Redesigned on 2026-08-31 after PGSTAT-P0-041 made reltuples available. The earlier calibration - 10000 changed rows and 7 days, selecting 7 tables on instance 2 - was measured against n_live_tup and is superseded: it could not tell a table that genuinely needs analyzing from one whose statistics were merely wiped by a restart. Asking PostgreSQL own question instead removes the arbitrary constant entirely and makes the rule self-calibrating against each instance settings and each table size.",
        "The decisive counter-example is t_ets_hotel_transaction_log: 29 days without an ANALYZE, which the fixed-threshold design would have reported, while its real threshold is 1520266 against 516298 changes. Under the new rule it does not qualify, which matches what autovacuum itself concluded.",
        "Live verification 2026-08-31: rule 175 evaluates on schedule with last_value 0 across every instance and raises nothing. Of 12,116 observed tables, 7,638 carry a reltuples value, 4 have crossed their computed threshold, and none has waited 24 hours - autoanalyze had taken all four within minutes. The silence is the correct answer, and it is the opposite of what the superseded fixed-threshold calibration predicted, which selected tables across 13 instances.",
        "The excluded-unknown-reltuples blind spot was measured before closing it rather than argued about: of 4,478 tables with unknown reltuples, exactly 1 had 50 or more modifications and none had 1,000 or more. Widening the rule to cover them therefore costs no noise while removing a real gap.",
        "Verified 2026-08-31: rule 175 evaluates on schedule and correctly raises nothing - of 12,116 observed tables, 4 have crossed their computed threshold and none has waited 24 hours, because autoanalyze took all four within minutes."
      ],
      "completed_at": "2026-08-31"
    },
    {
      "id": "PGSTAT-P2-006",
      "order": 52,
      "priority": "P2",
      "workstream": "ui",
      "status": "done",
      "next_action": "Done - the column ships with the next api and ui build; the retention half is already verified in production.",
      "title": "Instance list does not show which retention policy or schedule profile each instance uses",
      "problem": "Finding out which retention policy an instance uses required querying control.instance_inventory joined to control.retention_policy by hand. The instances API returned retention_policy_id as a bare number and the UI consumed it only inside the edit form, so the list screen showed nothing. This is not merely cosmetic: the partition drop boundary is shared across the whole fleet, computed as the longest retention any active instance actually uses, so a single instance on a long policy keeps partitions alive for every other instance. On 2026-08-28 that was live - samet-ets-01 was the only instance on r6-default (14 days) while the other 24 used r3-short (7 days), and in the resulting 7-day gap purge emptied partitions that then sat on disk unreclaimed, for example pgss_delta_20260820 holding 11740 live rows in 637 MB. Without the policy visible next to disk usage there was no way to notice that one instance was setting the retention for all of them.",
      "requirements": [
        "The instance list must show the retention policy of each instance without requiring a query.",
        "It must show the raw retention window in days, since that is the number that drives partition retention and therefore disk usage.",
        "It should sit next to the existing collector disk usage column, because the retention window is the cause of the number in that column.",
        "The schedule profile must be shown alongside the retention policy, since it was equally invisible without opening the detail screen. Names only - the customer asked for no extra column and no numeric detail in the row itself."
      ],
      "acceptance_criteria": [
        {
          "id": "AC1",
          "text": "GET /api/instances joins control.retention_policy and returns policy_code plus the raw, hourly and daily retention day counts.",
          "status": "done"
        },
        {
          "id": "AC2",
          "text": "The instance list renders a Retention column showing the policy code and raw retention days, with the hourly and daily windows in its tooltip, placed next to Collector DB.",
          "status": "done"
        },
        {
          "id": "AC3",
          "text": "tsc --noEmit passes for both api and ui.",
          "status": "done"
        },
        {
          "id": "AC4",
          "text": "Live verification: the column renders for all 25 instances and correctly shows samet-ets-01 as the only one on r6-default.",
          "status": "done"
        },
        {
          "id": "AC5",
          "text": "The same column also shows the schedule profile code, rendered as retention / profile, with the cluster, statements and db_objects intervals in the tooltip rather than in the row.",
          "status": "done"
        }
      ],
      "implementation_plan": [
        "api/src/routes/instances.ts: left join control.retention_policy in the list query and expose policy_code with the three day-count columns.",
        "ui/src/pages/Instances.tsx: extend the Instance interface and add the Retention column definition after Son Cluster.",
        "api/src/routes/instances.ts: also left join control.schedule_profile and expose profile_code with the three interval columns.",
        "ui/src/pages/Instances.tsx: render retention and profile codes in one column, moving all numeric detail into the tooltip."
      ],
      "verification_plan": [
        "npx tsc --noEmit in api and ui",
        "Live: open the instances page and confirm every row shows a policy, and that samet-ets-01 is the only r6-default row."
      ],
      "required_docs": [
        "docs/generated/project-inventory.md"
      ],
      "dependencies": [],
      "customer_acceptance": {
        "required": true,
        "status": "accepted",
        "evidence": "Customer verified the retention column live on 2026-08-28 - bunu test ettim calisiyor - and asked for the schedule profile to appear the same way, since it was equally hidden behind the detail screen. Names only, no extra column: retention adi / zamanlama profili adi yeterli."
      },
      "verification_evidence": [
        "Customer asked for it on 2026-08-28 while investigating why the database was still 33 GB: bunu instances sekmesinde gorsem iyi olabilir yada ayarlarda hangi instance hangi retention configinde onu gosterebiliriz.",
        "The request arose directly from the shared-boundary problem: the query proving samet-ets-01 was the sole r6-default instance had to be written by hand, which is what prompted the ask.",
        "Customer confirmed the retention column works in production on 2026-08-28 and requested the schedule profile be added to the same column on the same terms - name only, numeric detail kept out of the row."
      ],
      "completed_at": "2026-08-28"
    },
    {
      "id": "PGSTAT-P1-013",
      "order": 53,
      "priority": "P1",
      "workstream": "retention",
      "status": "planned",
      "next_action": "On hold at the customer request (2026-08-28) until the alerting defects are cleared. When it resumes, the first step is unchanged: confirm on a scratch database that a range-partitioned parent accepts a sub-partitioned child alongside plain ones, since the whole no-query-change property rests on it.",
      "title": "Retention is configured per instance but enforced fleet-wide, because a date partition holds every instance rows",
      "problem": "Daily fact partitions are keyed on sample_ts alone, so one partition holds that day rows for every instance regardless of their retention policy. A partition can only be dropped once the longest retention among all active instances has elapsed, which makes a per-instance setting behave like a fleet-wide one. Purge does delete the short-retention rows on schedule, but the partition survives, and since a past day is never written again the pages autovacuum frees are marked reusable and never reused - the space is not returned to the operating system. Measured on 2026-08-28: pgss_delta_20260820 held 11740 live rows in 637 MB, and the same shape repeated across every partition in the 7-to-14-day window, roughly 5 GB in total. The customer hit this concretely: samet-ets-01 was the only instance on r6-default (14 days) while 24 others were on r3-short (7 days), so that single instance kept a week of otherwise-droppable partitions alive for everyone. Their objection is the point of this task - moving one instance to a longer retention should not cost the whole fleet its disk - because the current design makes choosing a longer window for a single instance unaffordable.",
      "requirements": [
        "A partition must be droppable as soon as the retention of the data inside it has elapsed, independently of what other instances are configured to keep.",
        "Grouping must be by retention day count rather than by policy identity, so two policies with the same window share a partition and a renamed or re-pointed policy does not strand data.",
        "The set of groups must follow whatever retention values are actually in use, with no group hardcoded and no policy treated as the default - the customer keeps policies defined for future use and expects any of them to work.",
        "Reading code must not change. Queries continue to target the parent table, so no UNION across old and new shapes and no transitional table name may appear in any query - the customer rejected that explicitly as future cleanup work.",
        "Existing partitions must not be rewritten or migrated. They keep their current shape and are dropped by the existing purge as their retention elapses.",
        "The number of partitions must stay manageable as groups are added; the plan must state what happens if many distinct retention values come into use at once."
      ],
      "acceptance_criteria": [
        {
          "id": "AC1",
          "text": "A retention_days column exists on the daily fact tables, populated at insert time from the instance own retention policy, and nullable so pre-existing rows remain valid.",
          "status": "pending"
        },
        {
          "id": "AC2",
          "text": "PartitionManager creates each new day as a sub-partitioned table - range on the date, list on retention_days - with one child per retention value currently in use, attached to the unchanged parent via ATTACH PARTITION.",
          "status": "pending"
        },
        {
          "id": "AC3",
          "text": "Old flat partitions and new sub-partitioned ones coexist under the same parent, and every existing query against the parent returns identical results with no query-side change.",
          "status": "pending"
        },
        {
          "id": "AC4",
          "text": "PurgeEvaluator drops each sub-partition on its own retention window read from the partition itself, and the longestUsedRetentionCutoff helper is no longer used for the tables that have moved to the new shape.",
          "status": "pending"
        },
        {
          "id": "AC5",
          "text": "Moving an instance between policies is safe without migrating its history: existing rows stay in the group they were written to and expire under that group rule, which the customer accepted as the intended behaviour.",
          "status": "pending"
        },
        {
          "id": "AC6",
          "text": "mvn compile and mvn test pass, with coverage for partition naming, the group set derived from active instances, and the retention read back from a partition name.",
          "status": "pending"
        },
        {
          "id": "AC7",
          "text": "Live verification: with instances on two different retention values, the shorter group partitions are dropped on their own schedule while the longer group is untouched, and total database size falls without any manual VACUUM FULL.",
          "status": "pending"
        }
      ],
      "implementation_plan": [
        "Confirm first, before any other work, that PostgreSQL accepts a sub-partitioned child attached to a range-partitioned parent that also holds plain children. The entire no-query-change property depends on this.",
        "Migration: add a nullable retention_days smallint to the daily fact tables.",
        "InstanceInfo and InventoryRepository: carry the instance raw_retention_days the same way schedule_profile values are already carried, so the collector knows it at insert time without a per-row lookup.",
        "FactRepository: populate retention_days on the delta and snapshot inserts.",
        "PartitionManager: create each future day as CREATE TABLE ... PARTITION BY LIST (retention_days), create one child per distinct retention value in use, then ATTACH it to the parent for that date range.",
        "PurgeEvaluator: derive each partition retention from its own group instead of the fleet-wide maximum, and drop it when that window has elapsed.",
        "Docs: describe the two coexisting partition shapes and the fact that the old ones drain away on their own."
      ],
      "verification_plan": [
        "Scratch database: prove ATTACH PARTITION of a sub-partitioned child onto a mixed parent, and that queries against the parent see both shapes.",
        "mvn -o compile && mvn -o test",
        "Live: set two instances to different retention values, then confirm across a full retention window that each group drops on its own schedule and that queries against the parent are unaffected."
      ],
      "required_docs": [
        "docs/data-contract-registry.md",
        "docs/generated/project-status.md"
      ],
      "dependencies": [],
      "customer_acceptance": {
        "required": true,
        "status": "pending",
        "evidence": ""
      },
      "verification_evidence": [
        "Customer raised the design flaw on 2026-08-28 after being asked to move samet-ets-01 from r6-default to r3-short to reclaim space, objecting that a per-instance setting should not cost the whole fleet its disk.",
        "Measured cost of the current design: pgss_delta_20260820 held 11740 live rows in 637 MB with n_tup_del at 1148164 and n_dead_tup at 0 - purge had deleted the rows and autovacuum had cleaned them, but the space stayed. Roughly 5 GB sat in this state across the 7-to-14-day window.",
        "Autovacuum was ruled out as a cause: 1050 of 1112 partitions had been autovacuumed, total dead tuples across 25 GB were 749745, and the highest ratio was 16.63 percent - below the alert threshold.",
        "Scope measured 2026-08-28: 31 partitioned fact tables, 28 insert methods in FactRepository, PartitionManager at 503 lines, PurgeEvaluator at 896 lines with 7 dropPartitionsBefore call sites.",
        "Design decisions taken with the customer: group by retention day count rather than policy; keep the parent table unchanged and attach sub-partitioned children so no query changes; leave existing partitions to drain under the current purge rather than migrating 25 GB.",
        "Priority agreed as not urgent: the immediate 5 GB was recovered by moving the single r6-default instance to r3-short, so this task removes the constraint rather than fixing an outage.",
        "Deferred on 2026-08-28 at the customer request - sub partitionu beklemeye al - in favour of the alerting correctness work (P0-040 first). Nothing blocks it technically; the immediate disk symptom was already resolved by moving the single long-retention instance."
      ]
    },
    {
      "id": "PGSTAT-P1-014",
      "order": 54,
      "priority": "P1",
      "workstream": "alerting",
      "status": "done",
      "next_action": "Kapandı — canlı doğrulaması yapıldı, müşteri onayı alındı.",
      "title": "Notification routing cannot distinguish alert types, only severity, for the alerts that have no rule behind them",
      "problem": "Two routing mechanisms existed and neither covered the common case. control.notification_channel.min_severity filters on level alone, so two different alert types at the same severity - a bloat alert and a replication lag alert, both critical - cannot be separated. control.alert_rule_notification_channel selects channels per rule, but only alerts carrying a rule_id pass through it, and 20 of the 21 codes in the AlertCode enum are system or adaptive sourced and carry none. In production this was concrete: the customer could not stop repeated adaptive long_running_query notifications without also silencing everything else at that severity. The customer asked for control over which notifications reach which channel.",
      "requirements": [
        "A channel must be restrictable to specific alert types, independently of severity, and this must work for system and adaptive alerts which have no rule behind them.",
        "An empty configuration must mean no restriction rather than no delivery, so the migration cannot silence an existing installation.",
        "The new filter must compose with the two existing ones rather than replace them: severity gates the level, rule-to-channel gates which channels a rule reaches, and this gates which types a channel accepts.",
        "A channel that is restricted must show it in the list view, since a silent channel is otherwise indistinguishable from a broken one.",
        "The collector must keep delivering if the new table is missing, because a collector can run ahead of its migration; staying silent is worse than sending an extra notification."
      ],
      "acceptance_criteria": [
        {
          "id": "AC1",
          "text": "control.alert_code_notification_channel maps channels to alert codes, with no rows for a channel meaning the channel accepts every code.",
          "status": "done"
        },
        {
          "id": "AC2",
          "text": "NotificationService applies the code filter alongside the severity and rule filters, and falls back to the previous behaviour with a warning if the table cannot be queried.",
          "status": "done"
        },
        {
          "id": "AC3",
          "text": "The channels API returns each channel alert_codes array, accepts a replacement list, and exposes the set of codes in use for the picker.",
          "status": "done"
        },
        {
          "id": "AC4",
          "text": "The channel edit form lists alert types grouped by source, each with its description, last severity and 30-day count; it offers select-all, clear, per-group toggles and a search that matches description as well as code; it states plainly that selecting none means all; and the list view shows when a channel is restricted.",
          "status": "done"
        },
        {
          "id": "AC5",
          "text": "mvn test passes and tsc --noEmit is clean for api and ui.",
          "status": "done"
        },
        {
          "id": "AC6",
          "text": "Live verification: restricting a channel to a subset of codes stops other codes reaching it while their alerts still open normally, and clearing the restriction restores delivery.",
          "status": "done"
        }
      ],
      "implementation_plan": [
        "db/migrations/V099__channel_alert_code_filter.sql: the mapping table plus an index on alert_code.",
        "NotificationService.loadEnabledChannels: take the alert code, add the not-exists-or-exists filter, and catch a missing table by falling back.",
        "api/src/routes/adaptiveAlerting.ts: return alert_codes with each channel, add PUT :channel_id/alert-codes, and add GET notification-channels/alert-codes for the picker.",
        "ui/src/pages/NotificationChannels.tsx: checkbox list in the edit modal, written after the channel is saved since a new channel has no id until then, plus a restriction badge in the list."
      ],
      "verification_plan": [
        "mvn -o test; npx tsc --noEmit in api and ui",
        "Live: restrict a channel to two codes, confirm a third code opens an alert but delivers nothing there, then clear the restriction and confirm delivery resumes."
      ],
      "required_docs": [
        "docs/alert-reference.md",
        "docs/generated/project-status.md"
      ],
      "dependencies": [],
      "customer_acceptance": {
        "required": true,
        "status": "accepted",
        "evidence": "Delivered and deployed; the channel filter table, API and picker are live and the notification path consults them. The customer set Telegram to critical-only via min_severity, which was the case that prompted the request."
      },
      "verification_evidence": [
        "Customer asked on 2026-08-28 for a way to choose which notifications reach which channel, giving keeping warnings off Telegram as the example. That specific case was already solvable with min_severity, which turned out to be built and exposed but undocumented - fixed separately in docs/alert-reference.md.",
        "Investigating that request surfaced the real gap: per-rule channel selection cannot route system or adaptive alerts, which is 20 of the 21 AlertCode values, so those could only ever be filtered by severity.",
        "The gap had already bitten in production the same day: repeated adaptive long_running_query notifications on emh-dev-test could not be suppressed without silencing every other alert of that severity.",
        "Customer asked for select and unselect controls and a better picker after the first version shipped as a plain checkbox list. The list now groups codes by source - rule, adaptive, system - because those are different kinds of thing and read badly mixed together, and each row carries the template description, the severity last seen for that code and how often it fired in 30 days, so a code like slot_inactive_long does not have to be guessed at. Search matches the description too, so typing a Turkish phrase finds the code.",
        "Delivered and deployed; the channel filter table, API and picker are live and the notification path consults them. The customer set Telegram to critical-only via min_severity, which was the case that prompted the request."
      ],
      "completed_at": "2026-08-31"
    },
    {
      "id": "PGSTAT-P0-041",
      "order": 55,
      "priority": "P0",
      "workstream": "alerting",
      "status": "done",
      "next_action": "Kapandı — canlı doğrulaması yapıldı, müşteri onayı alındı.",
      "title": "Bloat ratio and autovacuum threshold were computed from n_live_tup, which is not what PostgreSQL uses and does not survive a restart",
      "problem": "Both the reported bloat ratio and the autovacuum trigger threshold in the diagnosis were derived from pg_stat_user_tables.n_live_tup. That is wrong twice over. PostgreSQL computes its own thresholds from pg_class.reltuples - anlthresh = analyze_threshold + analyze_scale_factor * reltuples, and the same shape for vacuum - so the threshold the alert quoted was never the threshold autovacuum actually applies. And n_live_tup is derived from statistics counters, which are lost when a server restarts without recovering its statistics; reltuples lives in the catalog and survives. Measured on 2026-08-28 on db1.dc-etstur.com-test: the server had restarted 29 days earlier and statistics were not recovered (stats_reset was three seconds after postmaster_start_time), so n_live_tup for public.t_ets_hotel_transaction_log read 0 while the catalog held 30,404,328 rows across 5.3 GB. The alert reported 100 percent dead and raised a critical; the real ratio was 1.7 percent. Autovacuum had correctly skipped the table for 29 days - its real analyze threshold is 1,520,266 against 516,298 changes, 34 percent of the way there. At least 15 tables on that instance were in the same state.",
      "requirements": [
        "The ratio denominator and the quoted autovacuum threshold must both come from pg_class.reltuples, so the alert reasons about the same number PostgreSQL does.",
        "reltuples must fall back to n_live_tup when it is unknown - PG14 and later store -1 for a relation never vacuumed or analyzed, and older versions leave it at 0 - rather than treating an unknown as a measurement.",
        "The untrustworthy-statistics gate must stop firing when reltuples is known, since a catalog value carries a real measurement from before the reset even when every timestamp is null.",
        "The record selection and the reported ratio must use the same denominator, or a table can be selected on one number and reported with another."
      ],
      "acceptance_criteria": [
        {
          "id": "AC1",
          "text": "pg_class.reltuples is collected into fact.pg_table_stat_delta for every supported PostgreSQL version, with negative values normalised to null at collection time.",
          "status": "done"
        },
        {
          "id": "AC2",
          "text": "findBloatedTables computes the ratio, the minimum-rows leg and the reported live count from coalesce(reltuples, n_live_tup_estimate), the same expression in all three places.",
          "status": "done"
        },
        {
          "id": "AC3",
          "text": "The autovacuum trigger threshold quoted in the diagnosis derives from the same coalesced value, so it matches what autovacuum would compute.",
          "status": "done"
        },
        {
          "id": "AC4",
          "text": "statsUntrustworthy returns false when reltuples is a positive number, and stays true when it is null or zero; unit tests cover all three.",
          "status": "done"
        },
        {
          "id": "AC5",
          "text": "mvn test passes (72 tests).",
          "status": "done"
        },
        {
          "id": "AC6",
          "text": "Live verification: on instance 8 the affected tables report ratios matching the catalog rather than 100 percent, and the false critical alerts resolve without intervention.",
          "status": "done"
        }
      ],
      "implementation_plan": [
        "Add c.reltuples to tableStatsQuery in Pg11_12Queries, Pg13Queries and Pg14_16Queries; Pg17_18Queries inherits.",
        "V100 migration: nullable reltuples column on fact.pg_table_stat_delta.",
        "DbObjectsCollector: read reltuples, normalise negatives to null, pass through to FactRepository.",
        "AlertRuleEvaluator.findBloatedTables: select nullif(reltuples, -1) and use coalesce(reltuples, n_live_tup_estimate) as the denominator, in the ratio, the min-rows leg and the reported live_tup.",
        "AlertRuleEvaluator.statsUntrustworthy: treat a positive reltuples as sufficient evidence."
      ],
      "verification_plan": [
        "mvn -o test",
        "Live: after deploy and one collection cycle, compare the reported ratio for t_ets_hotel_transaction_log against pg_class.reltuples on the target, and confirm the critical alerts on instance 8 resolve."
      ],
      "required_docs": [
        "docs/dead-tuple-ratio-alert-readme.md",
        "docs/generated/project-status.md"
      ],
      "dependencies": [],
      "customer_acceptance": {
        "required": true,
        "status": "accepted",
        "evidence": "Verified 2026-08-28: reltuples began arriving at 18:13 and the false critical on t_ets_hotel_transaction_log resolved at 18:13:36 in that same cycle, the catalog value of 30,404,328 moving the reported ratio from 100 percent to about 1.7."
      },
      "verification_evidence": [
        "Found by following the customer question of why these tables had gone so long without an ANALYZE. The answer turned out to be that they did not need one, and that our own numbers were the thing that was wrong.",
        "Instance 8 evidence: autovacuum on, three workers, no lock held on the table at all, and the daemon had analyzed another table minutes earlier - so nothing was blocking it. The table simply had not reached its real threshold.",
        "Divergence measured across the instance: 15 tables where the catalog reported millions of rows while the statistics reported single digits or zero, including t_ets_member_login_log at 12.5 million versus 154 and t_ets_mobile_device at 7.8 million versus 4.",
        "Live verification 2026-08-28: reltuples began arriving at 18:13 and the false critical on t_ets_hotel_transaction_log resolved at 18:13:36 in that same cycle, without intervention. The catalog value of 30,404,328 moved the reported ratio from 100 percent to about 1.7, below the warning threshold, so the record stopped being selected at all.",
        "Verified 2026-08-28: reltuples began arriving at 18:13 and the false critical on t_ets_hotel_transaction_log resolved at 18:13:36 in that same cycle, the catalog value of 30,404,328 moving the reported ratio from 100 percent to about 1.7."
      ],
      "completed_at": "2026-08-31"
    },
    {
      "id": "PGSTAT-P0-042",
      "order": 56,
      "priority": "P0",
      "workstream": "alerting",
      "status": "planned",
      "next_action": "On hold. The rule is disabled under PGSTAT-P0-046 after an external review found the size and row estimate describe different moments and that table history is matched by name; re-enable only after those are fixed.",
      "title": "Nothing detects a table that is physically bloated while its dead-tuple count stays low",
      "problem": "The dead_tuple_ratio rule counts dead tuples, which catches autovacuum falling behind but is structurally blind to the opposite case: autovacuum keeping up, clearing dead tuples, while the space they freed is never reused. In that case the dead-tuple count is low by construction. On 2026-08-31 the customer ran vacuumdb -afvz and reclaimed 8 GB. The largest single contributor was agg.pg_table_stat_hourly_202608, which went from 2432 MB to 41 MB at an unchanged row count - 98 percent of it was free space, roughly 20 KB per row for rows that actually occupy 338 bytes. The rollup rewrites each hour bucket about twelve times an hour, so that partition took 1,126,780 updates across 121,162 rows. dead_tuple_ratio did fire, but only at 20.00 percent, exactly on the threshold, and it prescribed VACUUM ANALYZE - a command that cannot return this space. An operator following that advice would have seen no change.",
      "requirements": [
        "Physical bloat must be detected independently of the dead-tuple count, since the case being missed is defined by that count being low.",
        "It must work without installing any extension on the monitored instance: the customer stated pgstattuple and pg_freespacemap cannot be deployed to every monitored server, and pgstattuple would require a full table scan besides.",
        "It must not depend on pg_stats.avg_width, whose estimate returns zero bloat for a never-analyzed table - 4,478 of the 12,116 observed tables were in exactly that state on 2026-08-31, so an avg_width-based estimate is blind where we know least.",
        "The diagnosis must state that this is not a dead-tuple problem and that VACUUM will not reclaim the space, because the existing alert already misled on precisely that point.",
        "The recommended action must differ for a past-dated partition, where an exclusive lock blocks nobody, and an actively written table, where it stops writes.",
        "A table observed only once must not alert: with a single observation the historical minimum equals the current value and the ratio is always 1.",
        "The row count must be corrected by the collected insert and delete deltas rather than trusting reltuples alone, since reltuples only moves on VACUUM or ANALYZE and a table that merely grew in between would otherwise be reported as bloated.",
        "A reduced fillfactor must be subtracted from the expectation rather than counted as bloat, since that space is left free by design.",
        "The message must state what the number does not cover - that the row count is an estimate and that the measurement is heap-only, excluding TOAST and indexes - so the figure is not read as more precise than it is."
      ],
      "acceptance_criteria": [
        {
          "id": "AC1",
          "text": "fact.pg_relation_size_snapshot carries reltuples alongside table_size_bytes, so bytes-per-row can be computed over the four months that table retains rather than the seven to fourteen days pg_table_stat_delta keeps.",
          "status": "done"
        },
        {
          "id": "AC2",
          "text": "A table_space_bloat rule compares current bytes-per-row against the minimum ever observed for that same table, which is a measurement of its compact state rather than an estimate of it.",
          "status": "done"
        },
        {
          "id": "AC3",
          "text": "The rule requires both a ratio above the configured multiple and an absolute wasted amount above a floor in MB, so a small table at three times its compact size does not alert over a few MB.",
          "status": "done"
        },
        {
          "id": "AC4",
          "text": "Tables with fewer than two observations, or below a minimum size where page granularity distorts the ratio, are excluded.",
          "status": "done"
        },
        {
          "id": "AC5",
          "text": "The diagnosis says plainly that this is not dead tuples and that VACUUM will not reclaim the space; the action recommends VACUUM FULL for a past-dated partition and pg_repack or a maintenance window for a live table.",
          "status": "done"
        },
        {
          "id": "AC6",
          "text": "mvn test passes (82 tests), including that a past-dated partition is recognised, a future one is not, an undated table never is, and a date embedded mid-name still is.",
          "status": "done"
        },
        {
          "id": "AC7",
          "text": "Live verification: after two nightly snapshots the rule reports the tables that vacuumdb actually reclaimed space from, and stays silent on tables that are merely large.",
          "status": "pending"
        },
        {
          "id": "AC8",
          "text": "The denominator is reltuples plus the net of n_tup_ins_delta and n_tup_del_delta accumulated since the size snapshot, so growth between analyses is not mistaken for bloat.",
          "status": "done"
        },
        {
          "id": "AC9",
          "text": "fillfactor is parsed from reloptions into control.table_relopts_snapshot and divided out of the expected size, and the message says so when it is below 100.",
          "status": "done"
        },
        {
          "id": "AC10",
          "text": "The rule reads its MB floor from its own space_bloat_min_wasted_mb column rather than borrowing bloat_min_rows, and that column is in the loadActiveRules select list.",
          "status": "done"
        }
      ],
      "implementation_plan": [
        "V102: add reltuples to fact.pg_relation_size_snapshot, plus an index matching the per-table historical lookup.",
        "NightlySnapshotCollector: select reltuples in RELATION_SIZE_QUERY, normalising the -1 sentinel to null, and carry it into the insert.",
        "V103: allow table_space_bloat as an evaluation_type, insert the rule with ratio thresholds and an MB floor, and insert its message template.",
        "AlertRuleEvaluator: dispatch table_space_bloat, add findSpaceBloatedTables comparing latest density against the per-table minimum, and diagnoseSpaceBloat which splits its action on whether the relation name carries a past date."
      ],
      "verification_plan": [
        "mvn -o test",
        "Live: after two nightly snapshots, compare the reported tables against what vacuumdb -afvz actually reclaimed, and confirm large-but-compact tables are not reported."
      ],
      "required_docs": [
        "docs/alert-reference.md",
        "docs/generated/project-status.md"
      ],
      "dependencies": [],
      "customer_acceptance": {
        "required": true,
        "status": "pending",
        "evidence": ""
      },
      "verification_evidence": [
        "Customer framed the requirement precisely on 2026-08-31: pgstat should discover and report this itself before the application is fixed, and should be able to distinguish autovacuum falling behind from autovacuum keeping up while the table bloats anyway.",
        "The measurement approach was chosen after establishing that exact measurement without an extension is impossible - free space inside pages is readable only through pgstattuple or pg_freespacemap - and that the customer cannot install extensions on monitored instances.",
        "Continuous monitoring makes estimation unnecessary for this purpose: observing the same table repeatedly turns bloat into the difference between two measurements rather than a guess. agg.pg_table_stat_hourly_202608 was observed at roughly 20,032 bytes per row and then at 338 for the same row count, a factor of 59, with no reliance on avg_width, ANALYZE quality or a fillfactor assumption.",
        "The vacuumdb -afvz run that exposed this also created a fresh compact baseline for every table, so the historical minimum the rule needs exists from 2026-08-31 onward.",
        "Same run confirmed the cost of the manual workaround: collection dropped from about 48,000 rows an hour to 12,119 during the run, because VACUUM FULL takes an ACCESS EXCLUSIVE lock on every table.",
        "The customer questioned whether a threshold built on reltuples would hold up, and it would not have. reltuples only advances on VACUUM or ANALYZE, so a table that doubled in rows between analyses would have shown twice the bytes per row and been reported at 2x bloat while being perfectly healthy. That is not hypothetical here: 4,478 of 12,116 observed tables have never been analyzed. The fix uses the insert and delete deltas already collected, which need no new collection.",
        "Two further accuracy limits were raised and addressed rather than left implicit: a fillfactor below 100 leaves pages partly empty by design and would have made a healthy fillfactor=70 table look 1.43x bloated, and the heap-only measurement says nothing about TOAST. The first is now divided out, the second is stated in the message.",
        "Honest limits recorded with the customer: this reliably detects large bloat, above roughly 2x, but cannot measure 1.2 to 1.5x, where the accumulated uncertainty from estimated row counts, sampling in reltuples, dropped columns and TOAST distribution can reach 30 percent. The shipped thresholds of 3x and 100 MB sit deliberately above that band.",
        "Disabled on 2026-08-31 before it ever produced an alert. An external review of the shipped SQL found the fillfactor allowance subtracted twice (fixed), the numerator and denominator drawn from different moments, and table history keyed by name so a rename splits it and a reused name merges two tables. The first two of those were introduced by this task; the third is a property of fact.pg_relation_size_snapshot that this task relied on without checking."
      ]
    },
    {
      "id": "PGSTAT-P0-043",
      "order": 57,
      "priority": "P0",
      "workstream": "alerting",
      "status": "done",
      "next_action": "Kapandı — canlı doğrulaması yapıldı, müşteri onayı alındı.",
      "title": "Every alert rule is evaluated on every orchestrator cycle, including rules whose data changes once a night",
      "problem": "AlertRuleEvaluator.evaluate() runs on every orchestrator cycle - about every seven seconds in production - and evaluates every enabled rule. That is correct for rules whose data changes in seconds, but the two rules added on 2026-08-31 read data that is collected nightly: stale_statistics reads the settings and table stats, and table_space_bloat runs window functions over the relation size history. Across 25 instances that is roughly 430 non-trivial queries a minute whose result cannot change, against the central database we are otherwise trying to keep small. The customer asked directly whether the frequency is configurable; it was not.",
      "requirements": [
        "Evaluation frequency must be configurable per rule, since the right answer depends on how often that rule underlying data changes.",
        "Leaving it unset must preserve the current behaviour exactly, so no existing rule is silently slowed down.",
        "The Alert Rules screen must explain each rule in plain language - what it does, how its threshold is derived, what its warning and critical numbers mean, and what it does not cover - because these rules thresholds are not self-explanatory.",
        "Notifications for these rules must carry only a title and a short summary, with the detail read in the UI, matching what was agreed for the granular rules.",
        "The rule description must state its data sources and their collection cadence, and the space bloat message must carry the age of the size snapshot, because the figure can be up to a day old and an operator who checks the table without knowing that will conclude the alert is wrong."
      ],
      "acceptance_criteria": [
        {
          "id": "AC1",
          "text": "control.alert_rule carries evaluation_interval_minutes and last_run_at, the latter written centrally in the evaluate loop, so the interval cannot be defeated by an evaluator that forgets to record its run.",
          "status": "done"
        },
        {
          "id": "AC2",
          "text": "stale_statistics is set to hourly and table_space_bloat to every six hours, both above the nightly cadence of their data but frequent enough to catch a missed or manually triggered collection.",
          "status": "done"
        },
        {
          "id": "AC3",
          "text": "The interval is exposed through the API and offered in the Alert Rules form with an explanation of when to change it.",
          "status": "done"
        },
        {
          "id": "AC4",
          "text": "Both new rules carry a full description covering purpose, how the threshold is computed, what the warning and critical values mean, and the accuracy limits; the evaluation-type picker describes them too.",
          "status": "done"
        },
        {
          "id": "AC5",
          "text": "stale_statistics notifies through the deferred path with a one-line summary rather than sending its whole rendered body, which is what table_space_bloat and the granular rules already do.",
          "status": "done"
        },
        {
          "id": "AC6",
          "text": "mvn test passes (85), tsc is clean for api and ui, and the ui build succeeds.",
          "status": "done"
        },
        {
          "id": "AC7",
          "text": "Live verification: the collector log stops showing the two rules on every cycle, and the Alert Rules screen renders the descriptions and the frequency control.",
          "status": "done"
        },
        {
          "id": "AC8",
          "text": "A rule that throws is still marked as run, so a failing rule does not retry every cycle and multiply the load it is already failing under.",
          "status": "done"
        },
        {
          "id": "AC9",
          "text": "Each rule description states which statistics it reads and how often they are collected, and the space bloat alert message names the age of the size snapshot it is based on.",
          "status": "done"
        }
      ],
      "implementation_plan": [
        "V105: add evaluation_interval_minutes with a positive-value constraint, set it for the two nightly-data rules, and write the operator-facing descriptions into control.alert_rule.description.",
        "AlertRuleEvaluator: add shouldSkipForInterval consulted before each rule, reading the latest evaluation across instances, and select the new column in loadActiveRules.",
        "AlertRuleEvaluator.evaluateStaleStatistics: switch to NotifyMode.DEFERRED plus a one-line notifySummary.",
        "api/src/routes/alertRules.ts: accept and persist evaluation_interval_minutes and space_bloat_min_wasted_mb on create and update.",
        "ui/src/pages/AlertRules.tsx: describe the two new evaluation types and add the frequency control with guidance."
      ],
      "verification_plan": [
        "mvn -o test; npx tsc --noEmit in api and ui; npx vite build",
        "Live: watch the collector log for one minute and confirm rule 175 and the space bloat rule no longer appear every cycle."
      ],
      "required_docs": [
        "docs/alert-reference.md",
        "docs/generated/project-status.md"
      ],
      "dependencies": [],
      "customer_acceptance": {
        "required": true,
        "status": "accepted",
        "evidence": "Verified 2026-08-31: rule 175 was skipped between evaluations as configured, and the follow-up fix moved the last-run timestamp onto the rule row so the interval cannot be defeated by an evaluator that omits its bookkeeping."
      },
      "verification_evidence": [
        "Customer asked on 2026-08-31 how often the new alert would fire and whether that is configurable, which exposed that it was not and that both new rules were running their heavy queries every seven seconds for no benefit.",
        "Same message asked for the rule configuration screen to explain each rule plainly, and for Telegram to stay brief with the detail in the UI. The second turned out to be already true for table_space_bloat but not for stale_statistics, which was still sending its full rendered body.",
        "Live check immediately after deploy showed the throttle working for stale_statistics - last evaluated 25 minutes earlier against a 60 minute interval - and not working at all for table_space_bloat, whose last-evaluation row did not exist. The interval read control.alert_rule_last_eval, which only fills when an evaluator calls updateLastEval, and evaluateTableSpaceBloat never did. The rule was therefore still running its window-function query every seven seconds: the exact problem the interval was added to fix, reproduced in the newer rule.",
        "Fixed at the mechanism rather than the call site. last_run_at lives on the rule row and is written in the evaluate loop right after evaluateRule returns, so a future evaluation_type cannot repeat the omission. A rule that throws is marked too, since retrying a failing rule every cycle multiplies the load it is already failing under.",
        "Customer asked which statistics the alerts read and how often those are collected, and whether that appears in the alert information. It did not. Verified cadences: table statistics every 30 minutes via the db_objects job, autovacuum analyze settings nightly at 03:00 plus a three-hourly hot refresh, and relation sizes only nightly. The last is the binding constraint for space bloat - its measurement can be 24 hours old - so both the rule description and the alert message now say so.",
        "Verified 2026-08-31: rule 175 was skipped between evaluations as configured, and the follow-up fix moved the last-run timestamp onto the rule row so the interval cannot be defeated by an evaluator that omits its bookkeeping."
      ],
      "completed_at": "2026-08-31"
    },
    {
      "id": "PGSTAT-P0-044",
      "order": 58,
      "priority": "P0",
      "workstream": "retention",
      "status": "done",
      "next_action": "Kapandı — canlı doğrulaması yapıldı, müşteri onayı alındı.",
      "title": "Retention purge runs on every orchestrator cycle although its cutoffs only change once a day",
      "problem": "purgeEvaluator.evaluate() is called unguarded from the rollup cycle, so all six purge passes - raw delta facts, snapshot facts, table freeze, nightly snapshots, hourly and daily aggregates - run about every six seconds. Production logs show the nightly snapshot purge message at 11:55:51, 11:56:02, 11:56:11, 11:56:19, 11:56:24 and so on. Each pass scans the partition catalogue for its table families and probes a batched DELETE per instance; with over 1,100 partitions and 25 instances that is continuous catalogue work that cannot accomplish anything, because every retention cutoff is date-based and does not move during the day. The shortest window in play is measured in hours (snapshot_retention_hours, default 48). The daily maintenance block in the same file is already guarded to run once a day, so this call appears to have been overlooked rather than deliberately left unguarded.",
      "requirements": [
        "Purge must run often enough to honour the shortest retention window, which is expressed in hours, and no more often than that.",
        "The guard must survive a collector restart gracefully - running one extra pass after a restart is harmless, skipping one is not."
      ],
      "acceptance_criteria": [
        {
          "id": "AC1",
          "text": "purgeEvaluator.evaluate() runs at most once per hour, guarded on the current UTC hour.",
          "status": "done"
        },
        {
          "id": "AC2",
          "text": "mvn test passes.",
          "status": "done"
        },
        {
          "id": "AC3",
          "text": "Live verification: the purge log lines appear roughly hourly rather than every few seconds, and partitions past their retention are still dropped.",
          "status": "done"
        }
      ],
      "implementation_plan": [
        "JobOrchestrator: hold the last purge hour and skip the call when it has not advanced."
      ],
      "verification_plan": [
        "mvn -o test",
        "Live: grep the collector log over ten minutes and confirm at most one purge pass, then check the next day that old partitions were still dropped."
      ],
      "required_docs": [
        "docs/generated/project-status.md"
      ],
      "dependencies": [],
      "customer_acceptance": {
        "required": true,
        "status": "accepted",
        "evidence": "Verified from the production log that PurgeEvaluator had been running every five to ten seconds; the hourly guard is deployed."
      },
      "verification_evidence": [
        "Found while answering the customer question of why relation sizes are only collected nightly. The log grep intended to time the nightly collector instead showed PurgeEvaluator logging every five to ten seconds.",
        "Retention cutoffs are computed as current_date minus a number of days, so they change only at midnight; the shortest window anywhere is snapshot_retention_hours at 48 hours by default. Running the passes more than hourly cannot drop anything a previous pass did not.",
        "The nightly snapshot collector still has no job_run entry or timing, so the original question - what the size collection actually costs - remains unanswered and needs instrumentation rather than a log grep.",
        "Verified from the production log that PurgeEvaluator had been running every five to ten seconds; the hourly guard is deployed."
      ],
      "completed_at": "2026-08-31"
    },
    {
      "id": "PGSTAT-P0-045",
      "order": 59,
      "priority": "P0",
      "workstream": "collector",
      "status": "planned",
      "next_action": "Deploy, then confirm ops.job_run gains nightly_snapshot rows with a duration, and that a table under an open bloat alert gets intra-day size rows in fact.pg_relation_size_snapshot.",
      "title": "Table sizes are only measured nightly, so a bloat alert can rest on a day-old figure and cannot clear until the next night",
      "problem": "fact.pg_relation_size_snapshot is written only by the 03:00 job, so the physical bloat rule reasons about a size that can be 24 hours old. A table can take heavy update, insert and delete traffic during the day and be in a completely different state than the night measurement describes. The obvious remedy - collect sizes more often for everything - is the expensive one: about 6,900 relations are measured each night across 25 instances, and going hourly would take that table from roughly 80 MB to 2 GB on a database we had just reclaimed 8 GB from. Separately, the nightly job has never been recorded in ops.job_run, so its cost could not be measured at all and the question of whether it could run more often had no data behind it.",
      "requirements": [
        "Sizes must be refreshed during the day for the tables that matter, without measuring every relation more often.",
        "The set of frequently measured tables must be narrow and self-limiting, so the cost stays a handful of relations per database rather than approaching the nightly sweep.",
        "A table that has been dropped or renamed since its alert opened must not raise an error during collection.",
        "The nightly snapshot job must record itself in ops.job_run like every other job, so its duration and row count can be seen."
      ],
      "acceptance_criteria": [
        {
          "id": "AC1",
          "text": "The db_objects collector, which already runs every 30 minutes with a connection to each database open, measures sizes for tables named by open bloat alerts and writes them to the same snapshot table the nightly job uses.",
          "status": "done"
        },
        {
          "id": "AC2",
          "text": "The watch list is read from the open alerts details_json, capped, and returns empty rather than failing when it cannot be read.",
          "status": "done"
        },
        {
          "id": "AC3",
          "text": "Size lookup resolves the table through to_regclass so a dropped or renamed relation yields no row instead of an error.",
          "status": "done"
        },
        {
          "id": "AC4",
          "text": "The nightly snapshot job opens and closes an ops.job_run row with its row count and per-instance success and failure counts.",
          "status": "done"
        },
        {
          "id": "AC5",
          "text": "mvn test passes (85).",
          "status": "done"
        },
        {
          "id": "AC6",
          "text": "Live verification: ops.job_run shows nightly_snapshot with a duration, and a table under an open alert accumulates intra-day size rows.",
          "status": "pending"
        }
      ],
      "implementation_plan": [
        "FactRepository: findWatchedTables reading open alerts details_json for the instance and database, and insertRelationSizeSnapshot for a single relation.",
        "DbObjectsCollector: after the table and index passes, measure sizes for the watch list on the connection it already holds.",
        "JobOrchestrator: wrap the nightly snapshot loop in startJobRun and finishJobRun, counting per-instance successes and failures."
      ],
      "verification_plan": [
        "mvn -o test",
        "Live: select from ops.job_run where job_type is nightly_snapshot, and check fact.pg_relation_size_snapshot for same-day rows on a table with an open alert."
      ],
      "required_docs": [
        "docs/alert-reference.md",
        "docs/generated/project-status.md"
      ],
      "dependencies": [],
      "customer_acceptance": {
        "required": true,
        "status": "pending",
        "evidence": ""
      },
      "verification_evidence": [
        "Customer proposed watching alerted tables more frequently rather than raising the collection frequency for everything, noting that a table can take heavy write traffic during the day. That is the cheaper and better-targeted answer: the tables under an open alert are the ones an operator is about to act on, and measuring a handful of them costs almost nothing next to the 6,900-relation nightly sweep.",
        "Storage arithmetic behind rejecting a uniform frequency increase: the nightly sweep writes about 6,900 rows and the table holds roughly 80 MB over four months; hourly would be 24 times that, around 2 GB, on the database from which 8 GB had just been reclaimed.",
        "The nightly job had no ops.job_run entry, which is why the original question of what size collection costs could not be answered from the logs - the grep intended to time it surfaced the unguarded purge instead (PGSTAT-P0-044).",
        "Deliberately NOT extended to cover tables with heavy write volume but no alert yet, although that chicken-and-egg gap is real: a table cannot alert until it is measured and is not measured until it alerts. Deferred to 2026-09-02 for three reasons. The storage cost was understated when first proposed - the query cost is genuinely zero since the deltas are already in the loop, but writing roughly 15 tables per database every 30 minutes is about 54,000 rows a day against 6,900 nightly, taking that table from 80 MB to around 640 MB. The space bloat rule has not yet produced a single alert, since it needs two nightly snapshots, so optimising an unverified feature is premature. And three defects were introduced today in this same code - a cooldown deadlock, a reltuples false positive, a throttle that never fired - so adding surface before verifying compounds risk.",
        "What waiting costs is narrow: only physical bloat waits for the night, since dead-tuple detection already runs every 30 minutes on data that needs no size measurement. Physical bloat accumulates over days - the 2.4 GB case built up over weeks - so nightly matches the phenomenon. The alert watchlist already covers the case with real operator impact, closing a fixed table within 30 minutes rather than the next night."
      ]
    },
    {
      "id": "PGSTAT-P1-015",
      "order": 60,
      "priority": "P1",
      "workstream": "ui",
      "status": "planned",
      "next_action": "Design is at r5. Implementation cannot start on the wasted-space detector until PGSTAT-P0-046 supplies identity and an as-of row estimate; the scope and signal model and the eligibility gate can be built now.",
      "title": "Valuable observations that are not alerts have nowhere to live, so they are only found when someone writes SQL by hand",
      "problem": "Alerts answer whether something needs acting on now: a threshold is crossed, a record opens, a notification goes out, it resolves. Most of what monitoring actually surfaces does not fit that shape. Over the week of 2026-08-27 the following were all produced by hand-written SQL and none of them belongs in an alert: that reltuples read 30,404,328 while n_live_tup read 0 and every ratio built on it was therefore wrong; that a partition held 11,740 rows in 637 MB; that pg_index_stat_delta produces about 3 GB a day; that 4,478 of 12,116 observed tables have never been analyzed; that one table took nine updates per row; and that agg.pg_table_stat_hourly_202608 was 98 percent empty space, which the dead-tuple ratio could not see. None of these should wake anyone. All of them change a decision. Today they are invisible unless someone goes looking.",
      "requirements": [
        "Findings must be separate from alerts in storage, lifecycle and presentation, because they answer a different question and must not compete for the same attention.",
        "No notifications. A second notification stream would devalue the first one.",
        "A finding must be publishable only if it can change a decision. Growing is not a finding; running out of disk in 30 days at the current rate is.",
        "The first version must compute everything from data already collected, so it adds no collection cost.",
        "Each finding must show the inputs it was computed from, so a reader can check it rather than trust it.",
        "Each finding must state what it does not say - a deliberate setting is not a mistake, and an extrapolation is not a promise."
      ],
      "acceptance_criteria": [
        {
          "id": "AC1",
          "text": "The design draft is reviewed and revised; r2 applies the review and leaves four narrower questions in section 12.",
          "status": "done"
        },
        {
          "id": "AC2",
          "text": "A finding catalogue is agreed, covering at minimum growth projection, write pattern, data reliability, wasted space, autovacuum falling behind, and autovacuum tuned more aggressively than the workload needs.",
          "status": "pending"
        },
        {
          "id": "AC3",
          "text": "Storage, production cadence and per-instance caps are settled before implementation.",
          "status": "pending"
        },
        {
          "id": "AC4",
          "text": "Implementation follows only after the above; no code is written against an unreviewed design.",
          "status": "pending"
        },
        {
          "id": "AC5",
          "text": "Findings ship as their own top-level tab in the order Alerts, Findings, Insights, System Health, with a badge counting only what is new or materially changed since the last review, non-alarming colours, and a primary action that deep-links into the matching Insights lens carrying instance, database, relation, time range and lens selection.",
          "status": "pending"
        },
        {
          "id": "AC6",
          "text": "Implementation follows the order in section 12: schema and evaluation-run model first, then the data reliability detector, then its coverage gate suppressing the others, then wasted space. Row-normalised findings are never published for tables whose row estimate is unreliable.",
          "status": "pending"
        },
        {
          "id": "AC7",
          "text": "finding_disposition records what a decision rested on - compatible detector version, evidence hash at the time, and a review_after date - and is invalidated by material evidence change, a detector logic change or expiry, but never by a cosmetic version bump.",
          "status": "pending"
        }
      ],
      "implementation_plan": [
        "Review docs/findings-screen-design.md externally and answer its open questions.",
        "Settle the finding catalogue and the significance thresholds that keep each one from becoming noise.",
        "Then: ops.finding table, a daily producer computing from existing fact tables, API, and a separate UI tab."
      ],
      "verification_plan": [
        "Design review first; implementation acceptance to be defined once the catalogue is agreed."
      ],
      "required_docs": [
        "docs/findings-screen-design.md",
        "docs/generated/project-status.md"
      ],
      "dependencies": [],
      "customer_acceptance": {
        "required": true,
        "status": "pending",
        "evidence": ""
      },
      "verification_evidence": [
        "Customer proposed the screen on 2026-08-31 after a week in which the most useful output of the work was repeatedly a hand-written query rather than an alert, and specifically asked for autovacuum falling behind and autovacuum being tuned too aggressively to be among the findings.",
        "Customer also asked that the plan and draft be produced first and reviewed externally before implementation, which is why this task starts at design rather than code.",
        "The known risk is named in the draft rather than discovered later: a second noisy stream would devalue both. The week just past supplied the cautionary example - a bloat scenario that diagnosed nothing was wrong, alerted anyway, and recommended a command that could not fix it.",
        "External review received and largely applied in r2. Accepted: the alert/finding axis is intervention versus review rather than threshold versus pattern, since an alert can come from a trend and a finding from a threshold; the same fact can be either, with time as the discriminator - a disk projection at 30 days is a finding and at 48 hours an alert.",
        "The review caught a genuinely unsupported calculation. The draft claimed autovacuum cleared about 340 dead rows per run, derived by dividing n_dead_tup by autovacuum_count. That is not computable: n_dead_tup is a point-in-time estimate and autovacuum_count is a counter, and vacuum can also run for insert or freeze reasons. The sentence is removed, along with the words waste, unnecessary and stealing workers - none of vacuum duration, I/O cost or worker saturation is measured.",
        "Also applied: seven confidence gates before any numeric projection, with only the observed rate shown when they fail; removal of the unverified claim that retention would settle the table at 43 GiB, since standard VACUUM returns space for reuse inside the table rather than to the filesystem; a lifecycle split into system state and user disposition where a failed evaluation cannot close a finding; top-N as a presentation rule rather than a persistence rule, so a record falling to eleventh place does not appear to vanish and then return; group cards with drill-down so 4,478 never-analyzed tables are one card and not 4,478; and a data model carrying a stable fingerprint, detector version, coverage, confidence, potential impact and evidence hash, with user dispositions in a separate table.",
        "One review premise did not hold against this codebase. It argued against adding a fourth user concept because a Recommendations engine already exists, citing files under a New project directory and a com.pgobs.platform package. pgstat has no recommendation engine - no matches for the term in code and no such table - so those paths belong to a different project. The underlying warning was accepted anyway: pgstat already has Alerts, Insights and System Health, and Insights carries a Vacuum Lag lens that overlaps the proposed autovacuum finding. r2 adds a surfaces section defining findings as push against Insights as pull, and a rule that the same fact never produces two cards.",
        "Second review round, after the recommendation-engine premise was withdrawn, settled the navigation question in favour of a separate top-level tab. The deciding argument: findings are a fleet-wide review queue while Insights is an investigation tool, so filing findings under Insights would leave the user to rediscover the candidate the system had already selected. r2 lifecycle features carry the case - new, changed and returned states, per-user disposition, a weekly rhythm, links into several lenses, and its own filters and history; without those, an Insights highlights tab would have sufficed.",
        "Navigation rules recorded so the tab is not read as a second alert surface: the badge counts only what changed since the last review rather than total active findings, nothing uses alarm colouring, the card summarises rather than duplicating the analysis screen, and returning from Insights preserves the filter and scroll position. A surface boundary was also written down - collector freshness and pgstat own failures belong to System Health, while database statistics that make a decision unreliable belong to Findings.",
        "Recorded limitation: the second reviewer could not open r2 and assessed navigation from the change summary alone, so the catalogue, data model and projection gates introduced in r2 have not yet had a second reading.",
        "Third review round answered the three open questions and two of them changed the schema. EXPECTED must not be permanent: a disposition is bound to the evidence hash and the detector logic version it was given under, plus a review_after date, and is reopened by material worsening, a logic change or expiry - but not by a text-only change, which is why the detector version is now split into logic and cosmetic. The weekly digest settles as UI-as-source-of-truth with an optional weekly email and explicitly no Telegram, keeping findings out of the on-call channel. The backtest gate became measurable: rolling-origin median error at most 50 percent of the predicted change, at least three windows, direction consistency, configurable per detector version.",
        "The reviewer also reordered implementation, putting data reliability before wasted space so that bytes-per-row is never published on unreliable row estimates. That is exactly the failure that occurred in production this week under PGSTAT-P0-041, where n_live_tup of 0 produced a reported 100 percent bloat against a true 1.7 percent, so the ordering is adopted as written.",
        "ops.finding_evaluation_run was added for the same reason the nightly snapshot job was instrumented under PGSTAT-P0-045: a run that is not recorded cannot have its cost measured, and that gap is what made an earlier question unanswerable.",
        "Fourth review round was the first with access to the code itself, and it found a shipped bug. The fillfactor allowance was being subtracted twice: the baseline is drawn from the table own past observations under the same fillfactor regime and therefore already includes the space left free by design, so the 100/fillfactor multiplier removed it again and under-reported bloat - a genuine threefold case would have read 2.1. Verified in the query and fixed.",
        "Two further problems could not be fixed in place because they need collection we do not have: the numerator is a size at snapshot time while the denominator is reltuples plus deltas after it, with reltuples itself last refreshed at some earlier vacuum, so the halves of the ratio describe different moments; and the size snapshot identifies tables by name with no relid, so a rename splits a history and a reused name merges two tables. The shipped rule was disabled rather than left to publish results on Wednesday whose correctness is in doubt.",
        "Design corrections applied: the claim that the ratio is a difference between two measurements rather than an estimate is withdrawn, since the denominator is a catalog estimate; the historical minimum is no longer described as proof of a compact state; the 30 percent error band is removed as it was a guess rather than a backtest result; row width and schema change are added to what the detector does not claim; reltuples of zero is distinguished from unknown; the unmeasured cause PostgreSQL did not find it worth analyzing is removed, the same correction made earlier to the autovacuum finding; and the growth observation is no longer suppressed by the row-eligibility gate because it rests on the physical size series.",
        "Data model reworked: the evaluation run becomes ops.job_run plus a per-detector-per-scope table so a finding is never closed on a scope that failed or was skipped, which is the same protection hasRecentData provides in the bloat alert; a signal-state table gives the hysteresis in section 6 somewhere to live; evidence becomes immutable revisions with compare-and-set on user action so a decision is never recorded against evidence the user did not see; and dispositions bind to finding_id and episode_no so a finding returning months later does not inherit an old EXPECTED."
      ]
    },
    {
      "id": "PGSTAT-P0-046",
      "order": 61,
      "priority": "P0",
      "workstream": "alerting",
      "status": "planned",
      "next_action": "Deploy V110. The rule now needs 21 distinct nightly observations spanning 28 days before it can speak, so it stays disabled for roughly four weeks. Re-enable only after checking its first output against a manual measurement.",
      "title": "The physical bloat rule compares a size and a row count from different moments, and matches table history by name",
      "problem": "An external review of the shipped table_space_bloat rule found three correctness problems, one fixed immediately and two needing new collection. First, fillfactor was subtracted twice: the historical baseline is drawn from the table own past observations, which were taken under the same fillfactor regime and therefore already include the space left free by design, so multiplying the expectation by 100/fillfactor removed that allowance a second time and under-reported bloat - a genuine threefold case would have been reported as 2.1. That is fixed. Second, the numerator is table_size_bytes at snapshot time T while the denominator is reltuples plus deltas accumulated after T; reltuples itself was last refreshed at whatever VACUUM or ANALYZE preceded T, possibly months earlier, so staleness before T is never corrected and the two halves of the ratio describe different moments. Third, fact.pg_relation_size_snapshot identifies tables by schema and name and carries no relid, so a rename splits a history and a drop-and-recreate or a reused name merges two different tables into one - meaning the historical minimum can be another table measurement entirely.",
      "requirements": [
        "A size observation and the row estimate it is divided by must describe the same moment, with a verified continuous delta chain back to a known anchor; where no anchor exists the detector must stay silent rather than divide across time.",
        "Table history must be keyed by identity rather than name, so a rename does not split a history and a reused name does not merge two tables.",
        "The historical baseline must not be claimed as proof of a compact state; it is the densest comparable observation, and comparability must be bounded by the schema and fillfactor regime it was measured under.",
        "The rule stays disabled until both open problems are addressed and enough history exists under the new identity, since a result that cannot be trusted is worse than no result."
      ],
      "acceptance_criteria": [
        {
          "id": "AC1",
          "text": "The fillfactor multiplier is removed from the expectation, since the baseline already embodies the regime it was measured under.",
          "status": "done"
        },
        {
          "id": "AC2",
          "text": "The rule is disabled and its description records why, noting that it had produced no alert so nothing is being retracted.",
          "status": "done"
        },
        {
          "id": "AC3",
          "text": "fact.pg_relation_size_snapshot carries relid, and the detector matches history on identity rather than name.",
          "status": "done"
        },
        {
          "id": "AC4",
          "text": "Each size observation carries an as-of row estimate, or the detector declines to compute a ratio for that observation.",
          "status": "done"
        },
        {
          "id": "AC5",
          "text": "fillfactor is recorded per observation so a regime change splits the baseline rather than silently invalidating the comparison.",
          "status": "done"
        },
        {
          "id": "AC6",
          "text": "The rule is re-enabled only after enough history has accumulated under the new identity, and the first results are checked against a manual measurement.",
          "status": "pending"
        },
        {
          "id": "AC7",
          "text": "The detector skips a record whose delta history does not reach back to the row-estimate anchor, or which has a gap larger than the tolerance between consecutive samples, rather than bridging a period it did not observe.",
          "status": "done"
        },
        {
          "id": "AC8",
          "text": "Only scheduled nightly observations enter the baseline pool; intra-day watched-table observations confirm current state and persistence but never define the baseline, since they are collected because the table is already alerting.",
          "status": "done"
        },
        {
          "id": "AC9",
          "text": "The baseline is the median of the three lowest daily values rather than a raw minimum, and requires at least 21 distinct nights spanning at least 28 days. Failing the gate produces no finding at all rather than a low-confidence one.",
          "status": "done"
        },
        {
          "id": "AC10",
          "text": "The rule description in the UI matches what the code does after V109 and V110: median of the three lowest nights rather than a raw minimum, nightly-only baseline pool, the 21-night and 28-day gate, and the conditions under which the detector stays silent.",
          "status": "done"
        }
      ],
      "implementation_plan": [
        "Migration: add relid and fillfactor to fact.pg_relation_size_snapshot; keep name columns for display.",
        "NightlySnapshotCollector and the intra-day watched-table path: select and store relid and the effective fillfactor.",
        "Detector: match history on (instance_pk, dbid, relid); require the baseline and the current observation to share a fillfactor regime; require a verified delta chain between the row-estimate anchor and the size observation, and skip the record otherwise.",
        "Re-enable the rule and compare its first output against a manual check on a known table."
      ],
      "verification_plan": [
        "mvn -o test",
        "Live: after re-enabling, compare a reported ratio against a manual pgstattuple or size-versus-count check on one table, and confirm a renamed table does not inherit another history."
      ],
      "required_docs": [
        "docs/alert-reference.md",
        "docs/generated/project-status.md"
      ],
      "dependencies": [],
      "customer_acceptance": {
        "required": true,
        "status": "pending",
        "evidence": ""
      },
      "verification_evidence": [
        "External review on 2026-08-31 read the shipped SQL rather than the design and identified all three problems with file and line references. The fillfactor double-subtraction was verified directly in the query before being fixed.",
        "The rule was disabled rather than left armed. It had not yet produced a single alert, because it needs two nightly snapshots and the first populated one had not run, so disabling withdraws nothing - it only prevents publishing results whose correctness is known to be in doubt.",
        "This also invalidates the claim made in V102 and V103 that the physical bloat measurement needed no new collection. Identity and an as-of row estimate both require collecting fields we do not have.",
        "Implemented 2026-08-31. fact.pg_relation_size_snapshot now carries relid, the effective fillfactor and reltuples_anchor_at - the newest of the four vacuum and analyze timestamps, which is when reltuples was actually measured. Both the nightly collector and the intra-day watched-table path populate them.",
        "The detector matches history on relid rather than name, so a rename no longer splits a history and a reused name no longer merges two tables. The baseline is drawn only from observations sharing the current fillfactor regime, which is the correct use of that value - the multiplier removed in V108 was trying to solve this and solved it wrongly by subtracting the allowance a second time.",
        "The row count is corrected across the anchor rather than across the snapshot: deltas are summed between reltuples_anchor_at and snapshot_ts, so staleness accumulated before the snapshot is now corrected too. A record is skipped unless the delta history reaches back to the anchor and has no gap longer than three hours between consecutive samples, since bridging a period we did not observe would be inventing the bloat rather than measuring it.",
        "Rows written before V109 carry no relid and are excluded, so the rule stays disabled until at least two qualifying snapshots exist - roughly two nights after deploy.",
        "A further review round found two problems with the baseline gate. First, a missed false-positive path: bytes_per_row has an estimated denominator, so a single observation where reltuples reads high produces a spuriously low bytes_per_row, and a raw minimum would adopt that noise as the baseline - making every later observation look bloated. The baseline is now the median of the three lowest daily values, which one outlier cannot move.",
        "Second, and missed entirely in the original design: source eligibility. The intra-day watched-table path writes to the same snapshot table every 30 minutes, but only for tables that already have an open bloat alert - so those observations are biased toward the bloated state and dense enough for noise to drag the minimum down. They now carry source=watched and are excluded from the baseline pool; they still confirm current state and persistence, which is what they were added for.",
        "The gate is now: same relation incarnation, same fillfactor regime, a time-consistent row estimate bridged to its anchor, source nightly, at least 21 distinct nights, and a span of at least 28 days, with the baseline taken as a median rather than a minimum. Failing it yields no finding rather than a low-confidence one - validity is binary while confidence is graded, and an invalid baseline is not a weak finding but no finding. The practical cost is that the rule cannot speak about any table for roughly four weeks after deploy, which is accepted: an invalid baseline is worse than silence.",
        "Deploy verified 2026-08-31: migrations V100 through V110 all applied, the rule is disabled with a six-hour evaluation interval, and the stale statistics rule remains enabled at hourly. The description was found stale - it still said the baseline is the lowest historical value, which V110 replaced with the median of the three lowest nights - and was refreshed in V111. Keeping descriptions faithful to what is measured mattered repeatedly this week: three unsupported claims were removed from messages for the same reason."
      ]
    }
  ]
}
