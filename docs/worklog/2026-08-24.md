# 2026-08-24

## Yapılanlar

- **PGSTAT-P0-034 canlı izleme sonrası — 4 ek bulgu, PGSTAT-P0-036 olarak
  işlendi**: 21 Ağustos'ta deploy edilen `hasRecentData` düzeltmesi sonrası
  kullanıcı 11 alert'in doğru şekilde yeniden açıldığını doğruladı
  ("evet hatalar geldi"). Telegram akışını canlı izlerken 4 yeni bulgu/istek
  ortaya çıktı:

  1. **Tekrar bildirim çok sık**: Aynı 11 alert her ~1 saatte (cooldown
     süresi) tekrar WARNING olarak bildiriliyordu. Bu bir bug değil —
     `cooldown_minutes` zaten UI'dan ayarlanabiliyor (AlertRules sayfası,
     "4. adım: Davranış" → "Tekrar Alert Cooldown" dropdown, seçenekler:
     yok/5/10/15/30/60/120/360 dakika). Kullanıcıya bu kontrolün yerini
     gösterdik, `rule_id=14`'ün cooldown'ını 360dk'ya (6 saat) çıkarması
     önerildi.

  2. **Çözüldü bildirimi hangi tabloyu söylemiyor**: `resolveAlert()`
     yardımcı metodu sadece `instance - rule_name` üretiyordu, `evaluateThresholdPerRecord`'daki
     `severity == null` dalında elimizde `top` (en yüksek değerli kayıt)
     zaten vardı ama geçirilmiyordu. Düzeltme: `resolveAlert()`'e opsiyonel
     `detail` parametresi eklendi, yeni `recordLabel(record, metricType)`
     yardımcı metodu tablo/index/sorgu etiketini üretiyor. `exceeding.isEmpty()`
     dalı (hangi kaydın düzeldiği belirsiz olabilir — bir veya birden fazla
     kayıt olabilir) jenerik etiketi koruyor.

  3. **Aksiyon önerisi hep aynı, nedene göre değişmeli**: Kullanıcı "tam
     teşhis" istedi, kanıta dayalı olmasını (internetten araştırılmış,
     "yanlış olmasın") talep etti. Arka planda bir araştırma agent'i
     PostgreSQL resmi dokümantasyonu, Citus/pganalyze mühendislik blogları
     ve check_postgres kaynak kodunu tarayıp 4 senaryo + kaynak URL'leri
     getirdi (autovacuum hiç çalışmamış / çalışıyor ama yetersiz / çalışıyor
     ama xmin horizon engelliyor / trend analizi). Bu, ayrı bir tasarım
     kararı gerektirdiği için (karar ağacı + ek context alanları) şimdilik
     board'da AC6 olarak "pending" bırakıldı, implementasyonu ayrı bir
     geçişte yapılacak.

  4. **Kanal seçimi ("isteyen telegramda isteyen uida isteyen ikisinde de
     görsün")**: Yeni bir özellik — her kural için hangi bildirim
     kanallarına (Telegram/email/vb.) gideceği seçilebilsin, UI her zaman
     gösterilsin. `control.alert_rule_notification_channel` (many-to-many
     join tablosu, V091) eklendi. `NotificationService.loadEnabledChannels()`
     artık `alertId`'den `rule_id`'yi okuyup, o kuralın seçili kanalı varsa
     sadece onlara filtreliyor; hiç seçim yoksa eski davranış (tüm aktif
     kanallara git) korunuyor — geriye dönük uyumlu. `AlertRules.tsx`'e
     "Bildirim Kanalları" checklist'i eklendi (4. adım, Davranış bölümü).

  - Ayrıca **ayrı bir bug** bulundu ve düzeltildi: `bloat_min_rows`/
    `bloat_abs_dead_tup`/`bloat_vacuum_ineffective_count` alanları (V089'da
    şemaya ve UI formuna eklenmişti) `api/src/routes/alertRules.ts`'in
    POST/PUT route'larının dinamik kolon listesinde HİÇ yoktu — kullanıcı
    UI'dan özel değer girse bile sessizce atılıyordu, her zaman kod
    tarafındaki default'lar kullanılıyordu. Şimdi düzeltildi.

  `mvn compile`/`mvn test` (collector), `npx tsc --noEmit` (api ve ui,
  ikisi de) temiz geçti.

- **Canlı doğrulama — VACUUM sonrası resolve, ama resolve mesajı hâlâ
  tablo adı içermiyordu**: Kullanıcı `sam-ets` (instance_pk=26)
  üzerinde `public.t_currency_rate_active`'e `VACUUM FULL ANALYZE`
  çalıştırdı, resolve'un gelmesini izledik. İlk toplama döngüsü
  (deploy'dan sonraki ilk döngü, 12:24:25) hiç satır yazmadı — kök
  neden: `DbObjectsCollector.previousTableStats` in-memory cache'i
  restart'ta boşalıyor, `if (prev == null) continue;` mantığı bu
  yüzden ilk döngüyü "baseline" sayıp atlıyor (veri kaybı değil, bir
  döngü — ~15dk — gecikme). Bu davranış `PGSTAT-P1-009` olarak board'a
  ayrı bir görev olarak işlendi (kapsamı büyük — muhtemelen
  `StatementsCollector` gibi başka collector'ları da etkiliyor, tüm
  collector'ları kapsayan bir tasarım oturumu gerekiyor, bugünkü işin
  kapsamı dışında tutuldu).
  Bir sonraki döngüde (12:39:25) VACUUM etkisi doğru şekilde göründü
  (`dead_tup=0`) ve tüm alertler resolve oldu — ama Telegram'daki
  "Çözüldü" mesajları sadece "instance - Dead Tuple Oranı" gösterdi,
  hangi tablo/şema düzeldiğini söylemiyordu. Kök neden: bu resolve'lar
  `exceeding.isEmpty()` dalından geçiyordu (o dal kasıtlı olarak jenerik
  etiket kullanıyordu, çünkü "hangi kayıt(lar) düzeldi" belirsiz
  olabilir — birden fazla tablo aynı anda düzelmiş olabilir).
  Kullanıcı net bir talep verdi: "hangi tablo resolve olduysa resolve
  mesajında o şema ve tablo adı olmalı" (tek tablo da olsa, birden
  fazla da olsa). Düzeltme: yeni `previousRecordsLabel(alertKey,
  metricType)` yardımcı metodu eklendi — `ops.alert.details_json`'daki
  (önceki açık alert anında `buildPerRecordsJson` ile yazılan `records`
  dizisi) kayıtları PostgreSQL'in kendi `jsonb_array_elements` fonksiyonu
  ile okuyup (Java'da JSON parse kütüphanesi olmadığı için SQL'e
  bırakıldı) şema+tablo/index/queryid etiketlerini üretiyor, birden
  fazla kayıt varsa hepsini virgülle ayırıp listeliyor (max 5).
  `exceeding.isEmpty()` dalındaki `resolveAlert()` çağrısına bu etiket
  geçirildi. `mvn compile`/`mvn test` temiz geçti.

- **Canlı doğrulama devam — hangi database'de bilgisi eksikti**: Deploy
  sonrası ilk resolve testinde `emakin-pg-test-190.51`'in
  `public.wJobLog` gösterdiği, diğerlerinin (sam-ets vb., daha eski
  deploy'dan) hâlâ jenerik kaldığı görüldü — bu, önceki maddedeki
  düzeltmenin henüz deploy edilmemiş olmasından kaynaklanıyordu, deploy
  edilince düzeldi. Ardından kullanıcı önemli bir eksik belirtti: ne
  `recordLabel()` ne `previousRecordsLabel()` **hangi database**'de
  olduğunu söylemiyordu — aynı `şema.tablo` adı bir instance'ta birden
  fazla database'de tekrarlanabilir (nitekim `t_currency_rate_active`
  hem `public` hem `engine` şemasında, farklı database'lerde
  görülmüştü). Düzeltme: her iki yardımcı metoda da `DB=<datname>`
  öneki eklendi (kayıtlarda `datname` alanı zaten mevcuttu,
  `findBloatedTables()`'ın `dbr.datname` seçimi sayesinde).
  `mvn compile`/`mvn test` temiz geçti. Kullanıcı test için
  `management.currencies` (instance_pk=19) tablosuna
  `VACUUM (FULL, ANALYZE, VERBOSE)` çalıştırdı, sonucu bekleniyor.

- **PGSTAT-P0-036 AC6 (aksiyon önerisi karar ağacı) uygulandı**:
  Kullanıcı, önceki oturumda başlayıp yarım kalan işin ("agentlardan
  haber bekliyorduk, iş yarım kaldı") tamamlanmasını istedi — hem
  "Teşhis:" hem nedene göre değişen "Aksiyon:" satırı, kanıta dayalı
  ve doğru olacak, ayrıca referans olabilecek bir doküman da yazılacak
  ("40 yıllık profesyonel gibi düşünerek yapalım").
  Önce bir araştırma agent'ı ile mevcut veri kaynaklarını (fact
  tabloları + collector kodunun gerçekten doldurduğu alanlar)
  doğruladık — `last_autovacuum`, `last_vacuum`, `autovacuum_count_delta`,
  `backend_xmin` (pg_activity_snapshot), slot `xmin_int`/`catalog_xmin_int`
  hepsi zaten toplanıyor, ek collector değişikliği gerekmedi.
  `docs/bloat-diagnosis-decision-tree.md` yazıldı — 5 senaryolu karar
  ağacı (hiç vacuum edilmemiş / xmin horizon engeli / autovacuum
  yetersiz / yeni oluşan trend / varsayılan), her senaryonun veri
  kanıtı, kaynak URL'leri (postgresql.org, Citus, pganalyze,
  check_postgres) ve bilinçli basitleştirmeler (xmin-horizon kontrolü
  instance-genel, tablo-özel değil) açıkça not edildi.
  Kod: `findBloatedTables()` sorgusuna `last_autovacuum`, `last_vacuum`,
  `autovacuum_count_sum` ve `lag() over (...)` ile hesaplanan
  `prev_dead_tup` (trend) eklendi. Yeni `hasXminHorizonRisk(instancePk)`
  — `fact.pg_activity_snapshot`'ta uzun süren transaction, ya da
  `fact.pg_replication_slot_snapshot`'ta pasif slot var mı kontrol
  ediyor. Yeni `diagnoseBloat(record, instancePk)` — 5 senaryoyu
  öncelik sırasına göre değerlendirip `[teşhis, aksiyon]` metin çifti
  döndürüyor. `evaluateThresholdPerRecord`'da sadece `dead_tuple_ratio`
  kuralları için bu çift hesaplanıp `ctx`'e ekleniyor (diğer
  `table_metric` kuralları için jenerik fallback set ediliyor, şablon
  kırılmıyor). `V092__bloat_diagnosis_action_template.sql`, `table_threshold`
  şablonundaki sabit "Aksiyon:" satırını `{{diagnosis}}` (tam satır,
  bloat-dışı kurallarda boş) + `{{bloat_action}}` (her zaman dolu) ile
  değiştirdi. `mvn compile`/`mvn test` temiz geçti. Kolon adı hatası
  (`fact.pg_replication_slot_snapshot`'ta `snapshot_ts` yerine doğrusu
  `sample_ts`) derleme sırasında fark edilip düzeltildi.

- **AC6 senaryo 1 revizyonu — "kontrol et" yerine kesin sayılarla teşhis**:
  Deploy sonrası kullanıcı ilk gerçek "hiç vacuum edilmemiş" mesajını
  gördü ve haklı olarak eleştirdi: "bizde tüm veriler var, sen bunları
  zaten görebilirsin, neden olabilir kontrol et gibi teşhis öneriyorsun?"
  Kontrol edildi: `fact.pg_settings_snapshot` (gece toplanıyor, V039)
  zaten `autovacuum`, `autovacuum_vacuum_scale_factor`,
  `autovacuum_vacuum_threshold` tutuyor (doğrulandı: `instance_pk=1`
  için `scale_factor=0.2`, `threshold=50`). Yeni
  `fetchAutovacuumSettings(instancePk)` yardımcı metodu eklendi, senaryo
  1 dört alt-duruma bölündü, hepsi gerçek sayılarla:
  (1a) global `autovacuum=off` — "kontrol et" değil, doğrudan "kapalı"
  deniyor; (1b) `autovacuum=on` ve gerçek tetikleme eşiği (`threshold +
  scale_factor × live_tup`) gerçek `dead_tup` sayısınca çoktan aşılmış
  — normalde tetiklenmiş olmalıydı, gerçekten toplanmayan TEK şeyi
  (`pg_class.reloptions`'taki tablo-özel `autovacuum_enabled` override'ı)
  açıkça belirtip kullanıcıya kontrol edeceği tam SQL'i veriyor (sınırı
  gizlemek yerine); (1c) eşik gerçekten henüz aşılmamış (gerçek bir
  açıklama, "henüz sıra gelmedi"); (1d) `pg_settings_snapshot` henüz
  toplanmamış (yeni instance). `mvn compile`/`mvn test` temiz geçti.
  Doküman (`docs/bloat-diagnosis-decision-tree.md`) bu 4 alt-durumla
  ve "tablo-özel override toplanmıyor" sınırlamasıyla güncellendi.

- **1b'deki "olabilir" de kabul edilmedi — pg_class.reloptions gerçekten
  toplanmaya başladı**: Kullanıcı "bunu da kontrol edebilirsin, neden
  autovacuum tetiklenmemiş onu tespit et" dedi — sınırlamayı belgelemek
  yetmedi, gerçekten toplamamız istendi.
  `V093__table_relopts_snapshot.sql`: `control.table_relopts_snapshot`
  (instance_pk, dbid, relid anahtarlı, her toplama döngüsünde upsert
  edilen, delta olmayan bir tablo — mevcut 33 parametreli
  `insertTableStatDelta` metoduna eklenmedi, riski artırmamak için ayrı
  tutuldu). `Pg11_12Queries`/`Pg13Queries`/`Pg14_16Queries`'in
  `tableStatsQuery()`'lerine `left join pg_class c on c.oid = s.relid`
  ve `array_to_string(c.reloptions, ',')` eklendi. `DbObjectsCollector`
  bu ham string'i okuyup yeni `FactRepository.upsertTableRelOptions()`'a
  geçiriyor, o da `autovacuum_enabled=` değerini parse edip upsert
  ediyor. Senaryo 1b artık iki kesin alt-duruma ayrıldı: 1b-i (override
  KESİN var — `ALTER TABLE ... RESET (autovacuum_enabled)` aksiyonu)
  ve 1b-ii (override KESİN yok — `autovacuum_max_workers` doygunluğu
  gibi kalan olası nedenlere işaret ediyor). `mvn compile`/`mvn test`
  temiz geçti. Not: bu tablonun override durumu, deploy sonrası o
  tablonun BİR SONRAKİ `db_objects` toplama döngüsünde bilinir hale
  gelecek (`table_relopts_snapshot` başta boş).

- **Uçtan uca canlı doğrulama — senaryo 1b-i gerçek verilerle çalıştı**:
  Deploy sonrası `control.table_relopts_snapshot` bir toplama döngüsü
  içinde doldu (1339 satır) — `public.t_currency_rate_active`'in
  (`instance_pk=1` ve `26`) gerçekten `autovacuum_enabled=false`
  override'ına sahip olduğu **kesin olarak** doğrulandı (`reloptions_raw
  = 'autovacuum_enabled=false'`).
  Doğrulama sırasında ilgisiz bir test kalıntısı bulundu: `rule_id=14`'ün
  `evaluation_window_minutes`'ı önceki testten `1` (dakika) olarak
  kalmıştı — bu, ~15dk'lık toplama aralığıyla birleşince pencerenin sık
  sık boş dönmesine yol açıyordu (bug değil, unutulmuş test ayarı).
  Kullanıcı `30`'a düzeltti. Ardından kullanıcı mesajın hemen
  yenilenmesini görmek için `cooldown_minutes`'ı geçici olarak `5`'e
  çekti (normal davraniş: aynı severity'de mesaj/`last_alert_at`
  cooldown süresince yenilenmiyor, spam'i önlemek için — `last_evaluated_at`
  güncellenir ama mesaj değişmez).
  Bu ayarlar düzeltildikten sonra `rule:14:instance:1` için gerçek
  alert tam olarak tasarlandığı gibi geldi:
  ```
  🔴 test-etsrooms-166-52 — public.t_currency_rate_active bloat
  Dead tuple oranı: %99.94 (eşik: > %50), pencere=30m
  DB=etsrooms
  Teşhis: Bu tablo hiç vacuum edilmemiş. Autovacuum genel olarak açık
  (autovacuum=on) ve tetikleme eşiği (402 = 50 + 0.20 × 1764 canlı
  satır) çoktan aşılmış (2900542 ölü satır) — ama bu TABLOYA ÖZEL
  autovacuum_enabled=false override'ı var (pg_class.reloptions), bu
  yüzden hiç çalışmadı.
  Aksiyon: ALTER TABLE ... RESET (autovacuum_enabled); ile bu tabloya
  özel override'ı kaldır, ardından manuel VACUUM ANALYZE çalıştır.
  ```
  Senaryo 1b-i, gerçek sayılarla ve hiçbir "olabilir" ifadesi olmadan
  uçtan uca doğrulandı. Diğer 4 senaryo (1a/1c/1d/2/3/4/5) aynı veri
  yollarını kullanıyor, henüz canlı tetiklenerek doğrulanmadı ama
  mantık aynı desende.

- **1b-ii'nin "olası nedenler" ifadesi de kabul edilmedi**: Kullanıcı
  gerçek fleet-genelinde gelen bir alert batch'ini (11 tablo, farklı
  senaryolar) izledikten sonra `etsground-dev-144.53`/`t_order` ve
  `db1.dc-etstur.com-test-161.213`/`t_ets_hotel_transaction_log`
  mesajlarındaki "olası nedenler: autovacuum_max_workers doygunluğu...
  veya çok yakın zamanda eşiği aştı" ifadesinin hâlâ net bir teşhis
  olmadığını belirtti — 1a/1b-i'de reddedilen aynı "git kontrol et"
  deseni.
  Yeni `fetchAutovacuumWorkerStatus(instancePk)` eklendi —
  `fact.pg_activity_snapshot`'tan (`backend_type='autovacuum worker'`)
  şu an çalışan worker sayısını, `fact.pg_settings_snapshot`'tan
  `autovacuum_max_workers`'ı okuyor. 1b-ii artık 3 kesin alt-duruma
  ayrıldı: 1b-ii-a (çalışan worker sayısı >= max — doygunluk KESİN
  var, "X/Y worker çalışıyor, TÜM WORKER'LAR DOLU" deniyor), 1b-ii-b
  (çalışan < max — doygunluk KESİN yok, "eşiği çok yakın zamanda aştı,
  autovacuum_naptime döngüsünü henüz beklemiyor" deniyor), 1b-ii-c
  (worker verisi henüz toplanmamış — gerçek bir bilinmezlik, öyle
  belirtiliyor). `mvn compile`/`mvn test` temiz geçti.

## Board hareketleri

- PGSTAT-P0-034: `customer_acceptance_note` eklendi (hasRecentData
  düzeltmesi canlı doğrulandı, 4 yeni bulgu P0-036'ya devredildi).
- PGSTAT-P0-036: yeni -> in_progress (bloat alert follow-up'ları: API
  kolon eksikliği, resolve detay bilgisi, kanal seçimi — AC1-AC5 done,
  AC6 (aksiyon önerisi karar ağacı) pending). `max_in_progress` 6 -> 7
  (P0-002, P1-008, P0-032, P0-033, P0-034, P0-035, P0-036 aynı anda
  açık — hiçbiri kapatılabilir durumda değil).
- PGSTAT-P0-036: AC6 done (karar ağacı uygulandı, `verification_evidence`/
  `next_action` güncellendi, `docs/bloat-diagnosis-decision-tree.md`
  referans dokümanı eklendi).

## Kararlar

- `cooldown_minutes` gibi kullanıcı-ayarlanabilir davranışlar için önce
  UI'da zaten var olan bir kontrol olup olmadığı kontrol edilmeli —
  kod değişikliği yapmadan önce.
- Aksiyon önerisi gibi "doğru olması kritik" metinler için kanıta dayalı
  (kaynak gösterilen) bir araştırma adımı atlanmamalı — kullanıcı özellikle
  "yanlış olmasın" talep etti, bu tür içerik tahminle yazılmamalı.
- Kural bazlı bildirim kanalı seçimi many-to-many bir ilişki olarak
  modellenmeli (basit enum değil) — kullanıcı "hangi spesifik kanal"
  seçebilmek istedi, "UI-only vs tümü" gibi iki seçenekli bir model
  yeterli olmazdı.

## Yarına kalanlar

- PGSTAT-P0-036 AC6: aksiyon önerisi karar ağacını tasarlayıp kodlamak
  (autovacuum never-triggered / running-but-ineffective / xmin-horizon-
  blocked / trend bazlı senaryolar, mevcut context alanlarına ek olarak
  `last_autovacuum`/trend yönü gibi yeni alanlar gerekebilir).
- PGSTAT-P0-036: deploy + canlı doğrulama (bloat eşik kalıcılığı, kanal
  filtreleme, spesifik tablo adı içeren resolve bildirimi).
- PGSTAT-P0-032: `pg_locks` araştırması hâlâ bekliyor (job_run purge
  yavaşlığı, önceki oturumdan beri askıda).
- PGSTAT-P0-028: kullanıcı kararı bekliyor (samet-ets-01 WAL yetki
  boşluğu, acil değil).
