# 2026-08-21

## Yapılanlar

- **PGSTAT-P0-033 — TextEnricher gereksiz temp file üretiyor**: Kullanıcı,
  izlenen iki kaynak instance'ta `pgstats_collector` kullanıcısından
  dakikada bir tekrarlanan temp file WARN log satırları bildirdi (9.5MB
  ve 3.1MB sabit boyutlarda). `work_mem`'i 2MB'den 4MB'ye çıkarması
  sorunu küçülttü ama çözmedi — bu, work_mem boyutlandırmasının asıl
  çözüm olmadığını gösterdi.
  Hedef instance'ta `pg_stat_statements`'a bakılarak kesin suçlu sorgular
  bulundu: `TextEnricher.enrich()`'in çalıştırdığı
  `pg_stat_statements(true)` tabanlı iki sorgu (biri ~10MB/çağrı, biri
  ~3MB/çağrı, her ikisi de 11 çağrıda).
  Kök neden: `Pg11_12Queries.pgssTextQuery()` (kalıtım zinciriyle tüm PG
  sürümlerine uygulanıyor — hiçbir alt sınıf override etmiyor)
  `pg_stat_statements(true)`'ı **hiçbir WHERE koşulu olmadan** çağırıyordu
  — bu, o instance'ta izlenen **tüm** aktif sorguların (query text dahil,
  view'in en büyük parçası) sunucu tarafında materialize edilmesi demek,
  sadece `TextEnricher`'ın o batch'te gerçekten ihtiyaç duyduğu birkaç
  `queryid` değil. İzlenen instance çok sayıda/uzun sorgu takip ediyorsa
  bu `work_mem`'i aşıp temp file'a dökülüyor — her enrichment döngüsünde.
  (Not: kullanıcı araştırma sırasında bu instance'ı PG12'den PG17'ye
  upgrade etti; düzeltmenin PG sürümünden bağımsız çalıştığı zaten
  kalıtım yapısıyla doğrulandı.)
  Düzeltme: `pgssTextQuery()` artık `where queryid = any(?)` alıyor;
  `TextEnricher.enrich()` `Statement`'tan `PreparedStatement`'a geçip
  bekleyen `queryid` listesini `bigint[]` olarak bind ediyor. Artık
  sunucu sadece istenen satırları döndürüyor, tüm view'i materialize
  etmiyor. `mvn compile`/`mvn test` temiz geçti.

- **PGSTAT-P0-032 devam** (dün açılan gece purge yavaşlığı): index'ler
  eklendi ama sorgu bu gece hâlâ 5-20 dakika sürdü — index eksikliği tek
  başına açıklama değilmiş. Kilit çakışması ihtimaline yöneldik,
  `pg_locks`/`pg_stat_activity` sorgusu bu gece (02:00 UTC civarı) için
  kullanıcıya verildi, sonuç bekleniyor.

- **Genel bulgu (ayrı, henüz kod değişikliği yapılmadı)**: Kullanıcı
  "pgstat kendi database'inde sürekli DELETE çalışıyor ama bakım
  çalışmıyor, autovacuum yetişmiyor, pgstat bunu söylemesi gerekmiyor
  muydu" sorusunu sordu. İnceleme: `ops.alert` (%22.3 dead tuple) ve
  `ops.notification_log` (%16.9) gerçekten bloat'lu; `PurgeEvaluator`'da
  bu iki tablo için (job_run/job_run_instance'da olduğu gibi) hiç VACUUM
  çağrısı yok. Ayrıca `dead_tuple_ratio` alert kuralı sunucuda **tanımlı
  ama kapalı** (`is_enabled=false`, iki kural: klasik eşik + haftalık
  smart), VE kod tarafında `n_live_tup+n_dead_tup > 1000` sabit eşiği
  küçük ama kritik tabloları (592 satırlık `ops.alert` gibi) zaten
  değerlendirme dışı bırakıyor. Kullanıcı "false pozitif üretmeyen,
  gerekirse birden fazla parametreye bakan mantıklı bir alert yapısı"
  istedi; piyasa pratiği (check_postgres, Datadog, pganalyze, Citus)
  araştırıldı — sonuç: tek eşikli oran modeli yaygın değil, oran+mutlak
  sayı+süreklilik/trend kombinasyonu standart. Üç bacaklı bir tasarım
  (oran+min-satır 100'e düşür, mutlak dead-tuple eşiği, "autovacuum
  çalışıyor ama yetişmiyor" ayrı sinyali; disk MB sadece context'e)
  önerildi, kullanıcı onayı bekleniyor — henüz kod yazılmadı.

## Board hareketleri

- PGSTAT-P0-033: yeni -> in_progress (TextEnricher temp file üretimi,
  production bug). `max_in_progress` 3 -> 4.

## Kararlar

- Kaynak instance'a giden herhangi bir sorgu, sadece Java tarafında
  filtrelenecek bir "tüm sonuçları çek, sonra filtrele" deseni
  kullanmamalı — filtre mümkünse SQL'in kendisinde (WHERE/parametre)
  olmalı, özellikle `pg_stat_statements` gibi potansiyel olarak büyük/
  metin-ağırlıklı view'lerde.
- Bloat/vacuum alert kuralı yeniden tasarımı henüz onaylanmadı — tasarım
  kullanıcıya sunuldu, sonraki oturumda eşikler netleşince uygulanacak.

## Yarına kalanlar

- PGSTAT-P0-033: deploy + canlı doğrulama (iki etkilenen instance'ta
  temp file WARN log satırlarının kesildiğini/küçüldüğünü teyit et).
- PGSTAT-P0-032: bu geceki `pg_locks` sonucu bekleniyor — index'ler
  yeterli olmadı, kilit çakışması ihtimali araştırılacak.
- Bloat/vacuum alert kuralı yeniden tasarımı: kullanıcı onayı bekliyor
  (eşikler: 100 satır min, mutlak dead-tuple eşiği, autovacuum-yetersizlik
  sinyali).
- PGSTAT-P0-028: kullanıcı kararı bekliyor (samet-ets-01 WAL yetki
  boşluğu, acil değil).
- PGSTAT-P1-008: pgstatdash'ta modül/ana başlık haritası onayı bekliyor.
- PGSTAT-P0-002: 904 alanın kontrat incelemesi (aktif görev, devam ediyor).
