# 2026-08-28

## Yapılanlar

- **PGSTAT-P0-037 doğrulaması — partition drop çalıştı, ama boyut tahminim
  yanlıştı.** Gece bakımından sonra altı delta ailesinde de `en_eski`
  `20260728` → `20260814`, partition sayısı 45 → 29. 16 partition drop
  edildi, düzeltme beklendiği gibi çalıştı.

  Ama DB 35 GB → **33 GB**, tahmin ettiğim 25 GB değil. Tahmin yanlıştı ve
  sebebi kendi ölçtüğüm veriyi doğru yorumlamamamdı: dün "14 günden eski
  partition'lar toplam 4 MB" diye ölçmüştüm, yani drop edilecek şeyler zaten
  boştu. Buna rağmen ~10 GB kazanç öngördüm; iki ölçüm birbiriyle çelişiyordu
  ve fark etmedim. Düzeltmenin gerçek değeri boyutu bugün düşürmek değil,
  **boşa düşen alanın birikmesini durdurmak**.

- **Müşteri hipotezi test edildi: "partition'larda autovacuum yetişemiyor
  olabilir"** — veri bunu desteklemedi. 1112 partition'ın 1050'si autovacuum
  görmüş, toplam ölü satır 749.745 / 25 GB (binde birkaç), en yüksek oran
  %16.63 (eşik %20). `last_autovacuum` hepsinde bugün 03:0x — gece hepsini
  gezmiş.

- **Asıl bulgu — boşalmış ama küçülmemiş partition'lar**: `pgss_delta_20260820`
  **11.740 canlı satır** tutuyor ama **637 MB** yer kaplıyor. `n_tup_del` =
  1.148.164, `n_dead_tup` = 0 — yani purge bir milyon satır silmiş,
  autovacuum hepsini temizlemiş, ama disk boşalmamış. Aynı desen tüm eski
  partition'larda (`_20260821`: 141K satır / 708 MB, `_20260822`: 234K /
  759 MB).

  Sebep: purge, `r3-short` (7 gün) instance'ların satırlarını siliyor ama
  partition en uzun **kullanılan** retention'a göre (`r6-default`, 14 gün)
  duruyor. Aradaki 7 günde partition boşalıyor, diskte kalıyor, ve geçmiş
  güne bir daha yazılmadığı için o alan asla kullanılmıyor.

  `r6-default` politikasını kullanan **tek** instance var: `samet-ets-01`.
  `r3-short`'a alınırsa cutoff 7 güne düşer, partition'lar boşalmadan drop
  edilir — kaba tahminle 7 partition × ~700 MB ≈ 5 GB, kalıcı olarak.
  Karar müşteride (14 günlük veriye ihtiyacı var mı).

- **UI — instance listesine retention kolonu** (müşteri talebi): hangi
  instance'ın hangi retention politikasında olduğu şu ana kadar sadece SQL'den
  görülebiliyordu; API `retention_policy_id` (sayı) dönüyor, UI onu yalnızca
  düzenleme formunda kullanıyordu. `control.retention_policy` JOIN'i eklendi,
  liste ekranına politika adı + ham veri gün sayısı kolonu kondu (saatlik ve
  günlük özet süreleri tooltip'te). "Collector DB" kolonunun yanına
  yerleştirildi çünkü biri diğerinin sebebi — ve partition drop sınırı tüm
  instance'lar için ortak olduğundan, tek bir instance'ın uzun politikası
  diğerlerinin diskini de meşgul ediyor.

- **Sıradaki büyüme kaynağı (henüz ele alınmadı)**: `pg_index_stat_delta`
  günde 3 GB üretiyor (`_20260826` tek başına 3000 MB). Retention kısaltmak
  bunu çözmez, üretimi azaltmak gerekir. Ayrıca `agg.pg_table_stat_hourly`
  aylık partition olduğu için temmuz ayı (2.4 GB) hâlâ duruyor.

## Kapatılan işler

Müşteri testleri sonrası beş görev kapatıldı (`in_progress` 8 → 4):

- **PGSTAT-P0-034** — dead_tuple_ratio kuralının 3 sinyalli yeniden tasarımı
- **PGSTAT-P1-011** — autovacuum kanıt katmanı (I/O, wait-event, throttle)
- **PGSTAT-P0-032** — `ops.job_run` purge yavaşlığı (V088 index)
- **PGSTAT-P0-033** — `TextEnricher` temp dosya taşması
- **PGSTAT-P1-010** — `agg.pg_table_stat_hourly` UPSERT kaynaklı büyüme (V094)

## Sub-partition tasarımı (PGSTAT-P1-013)

Müşteri, retention'ın per-instance ayarlanıp fleet-wide etki etmesine haklı
olarak itiraz etti: bir instance'ı `r12`'ye almak 24 instance'ın diskini
meşgul ediyor.

**Kök sebep**: günlük partition sadece `sample_ts` ile anahtarlanıyor, yani
bir partition o güne ait **bütün** instance'ların satırlarını tutuyor.
Partition ancak en uzun retention dolduğunda düşebiliyor. Purge kısa
retention'lı satırları zamanında siliyor ama partition duruyor; geçmiş güne
bir daha yazılmadığı için autovacuum'un boşalttığı sayfalar hiç yeniden
kullanılmıyor ve OS'a dönmüyor.

**Seçilen çözüm — retention gün sayısına göre alt-partition:**

```
fact.pgss_delta                    ← parent, DEĞİŞMEZ
├── pgss_delta_20260820            ← eski, düz partition (doğal ölümüne)
└── pgss_delta_20260901            ← yeni, LIST(retention_days) ile bölünmüş
    ├── pgss_delta_20260901_d7
    └── pgss_delta_20260901_d14
```

Kritik özellik: parent tablo değişmediği için **sorgular hiç değişmiyor**.
İlk düşündüğüm "yeni tablo + UNION" yaklaşımını müşteri haklı olarak
reddetti — kodda kalıcı borç bırakır, ileride olmayan tablo adları
SQL'lerde kalırdı. `ATTACH PARTITION` ile aynı parent altında iki farklı
şekil bir arada yaşayabiliyor.

**Politikaya değil gün sayısına göre gruplama**: iki politika aynı gün
sayısındaysa aynı partition'ı paylaşır; yeni politika mevcut bir gün
değerine denk gelirse hiç yeni partition gerekmez; politika adı değişse
bile veri yerinde kalır.

**Geçmiş veri taşınmıyor** (müşteri kararı): retention değiştirilen
instance'ın eski verisi yazıldığı grupta kalır ve o grubun kuralıyla
ölür. Retention değişikliği nadir olduğu için savunulabilir davranış.

**Kapsam** (ölçüldü): 31 partition'lı fact tablosu, `FactRepository`'de 28
insert metodu, `PartitionManager` 503 satır, `PurgeEvaluator` 896 satır /
7 `dropPartitionsBefore` çağrısı. Tahmin: 4-6 saatlik bir oturum + ertesi
gün canlı doğrulama. En belirsiz parça collector'ın insert anında
instance'ın retention'ını bilmesi — `InstanceInfo`'ya `schedule_profile`
değerleriyle aynı desende eklenecek.

**Öncelik P1**: acil değil, çünkü bugünkü 5 GB `samet-ets-01`'i `r3-short`'a
alarak geri kazanıldı. Bu iş kısıtı kaldırıyor, bir arızayı düzeltmiyor.

**İlk adım riski doğrulamak**: PostgreSQL'in, düz partition'ları da olan bir
range-partition'lı parent'a alt-partition'lı bir çocuk kabul ettiği scratch
DB'de kanıtlanacak. Planın "sorgu değişmiyor" özelliği tamamen buna dayanıyor.

## Öncelik kararı

Müşteri sub-partition işini (PGSTAT-P1-013) beklemeye aldı; sıradaki iş
**PGSTAT-P0-040** (bozuk istatistikle yanlış bloat alert'i).

Sıralama önemli: **P0-040, P0-039'dan (tablo bazlı alert) önce yapılmalı.**
Aksi hâlde bozuk istatistikli her tablo ayrı bir yanlış alert üretir ve
gürültü beş katına çıkar.

## PGSTAT-P2-006 kapandı + zamanlama profili eklendi

Müşteri retention kolonunu canlıda doğruladı ("test ettim çalışıyor") ve
zamanlama profilinin de aynı şekilde eklenmesini istedi — o da detay
ekranına girmeden görünmüyordu.

Aynı kolonda `retention / profil` olarak gösteriliyor (`r3-short / default`
gibi). Müşterinin şartı netti: *"ek kolon eklemeye detay vermeye gerek yok,
retention adı / zamanlama profili adı yeterli"* — bu yüzden sayısal
değerler (cluster/statements/db_objects interval'ları, saatlik/günlük özet
süreleri) satırda değil tooltip'te kalıyor.

`api/src/routes/instances.ts`'e `control.schedule_profile` JOIN'i eklendi.
`tsc --noEmit` her iki tarafta temiz.
