# 2026-09-01 — Sessiz bir hata, üç yanlış iddia ve 721 milyon gereksiz güncelleme

## Özet

Gün, dünden devredilen tek bir doğrulamayla başladı ve dört ayrı bulguyla
kapandı. Ortak tema: **ölçmediğimiz şeyler hakkında iddiada bulunmuşuz.**

- Gün içi boyut ölçümü 14 saattir hiçbir şey yazmıyordu — kök neden bulundu,
  düzeltildi, canlıda doğrulandı.
- Fiziksel şişme alarmının mesajı üç yanlış iddia taşıyordu; ikisi testlerle
  kilitlenmişti.
- Kaynak makine yükü ilk kez ölçüldü. Cevap: engel değil.
- Merkezi DB'nin kendi şişmesinin sebebi retention değil, **yazma çoğaltması**
  çıktı. Ölçüldü, düzeltildi.

---

## 1. İzleme yolu neden hiçbir şey yazmıyordu (PGSTAT-P0-045)

Dün deploy edilmişti ve 14 saat boyunca tek bir `source='watched'` satırı
yazmamıştı. Log tamamen sessizdi.

**Elenenler** (hepsi ölçümle, tahminle değil):

| Şüpheli | Nasıl elendi |
|---|---|
| Kod deploy edilmemiş | Aynı commit'in (`22ef7fc`) diğer çıktısı `ops.job_run`'da vardı |
| `findWatchedTables` boş dönüyor | Elle koşturuldu, satır döndü |
| `instance_pk` NULL (V098'deki gibi) | Dolu: 14, 20, 19 |
| İzleme adımına hiç ulaşılmıyor | Index delta'ları aynı zaman damgasında tamamlanmış |
| `relkind='p'` (partition üst tablosu) | `dim.relation_ref`'te üçü de `'r'` |

**Kök neden — JDBC tip hatası.** `sum(pg_relation_size(...))`, `sum(bigint)`
demektir ve PostgreSQL bunu `numeric` döndürür; JDBC `BigDecimal` verir. Kod
`(Long) rs.getObject("index_size_bytes")` ile okuyordu → her tabloda
`ClassCastException`. İstisna `insertRelationSizeSnapshot(...)` argümanları
değerlendirilirken oluştuğu için insert'e hiç girilmiyordu.

**Gece toplayıcısı neden çalışıyordu:** birebir aynı SQL ifadesini kullanıyor
ama `rs.getLong(...)` ile okuyor, o dönüştürüyor. İki yoldan sadece birinin
bozuk olması bundandı — ve gece toplamasının çalışıyor olması kodun sağlam
olduğuna dair yanıltıcı bir kanıt üretti.

**Asıl zamanı yakan:** tek `catch` bloğu **DEBUG** seviyesindeydi. Fleet
genelinde deterministik bir hata tek satır log üretmedi.

Düzeltme (`c57a130`): ifadeye `::bigint`, gece yolunun kullandığı null-güvenli
`getLong` deseni, `catch` → `WARN` + stack trace. Kod tabanının kalanı tarandı:
diğer tüm aggregate ve `lsn_diff` sütunları zaten açık `::bigint` taşıyor, tek
eksik burasıydı.

**Doğrulandı:** 11:00'de 4 `watched` satırı, `index_size_bytes` dolu.

### İzleme listesi de yanlış kurulmuştu (`3ab65dd`)

Liste `details_json`'da tablo adı olan **her** açık alarmı alıyordu. Amacı ise
sadece şişme alarmlı tabloların boyutunu tazelemek. İzlenen üç tablodan biri
16 kB'lık bir lookup tablosuydu — şişme kuralının 8 MB alt sınırının da, gece
toplamasının 1 MB alt sınırının da altında, yani hiçbir ölçümü kullanılamazdı.

`evaluation_type = 'table_space_bloat'` ile daraltıldı. **Doğrulamadan sonra**
yapıldı, bilerek: kural kapalı olduğu için önce daraltılsaydı liste boşalır ve
`ClassCastException` düzeltmesi 29 Eylül'e kadar kanıtlanamazdı.

---

## 2. Kaynak makine yükü — ilk kez ölçüldü

Müşteri önerisi: `pg_stat_statements` zaten kurulu, kendi toplama
sorgularımızın maliyetini oradan okuyalım. Doğru yol oydu; kendi
kullanıcımızı istatistiklerden dışlayan bir filtre yok.

| Ölçüm | Değer |
|---|---|
| Gece boyut sorgusu, en yoğun tek veritabanı | 396 ms |
| Gece, en yoğun instance'ın tüm db'leri | ~510 ms, gecede 1 kez |
| `db_objects` (30 dk'da bir) | 15–107 ms/çağrı |
| Diskten okunan blok | çoğunda 0 |
| `blk_read_time` | 0.0–0.9 ms (`track_io_timing` açık) |

**Sonuç: toplama sıklığının önünde kaynak makine engeli yok.** Tek gerçek
kısıt merkezi DB'deki depolama.

Sabah `ops.job_run`'dan okunan 8.8 saniye, 25 instance'ın **paralel duvar
saati**ydi ve tek bir makinenin yükü hakkında hiçbir şey söylemiyordu. O
rakamla soruya cevap vermek hataydı. `ops.job_run_instance`, `nightly_snapshot`
için satır yazmıyor, o yüzden instance kırılımı oradan da alınamıyor.

---

## 3. Şişme kuralı — sonda testi (PGSTAT-P0-046)

29 Eylül'ü beklemek yerine sentetik veri yüklendi. **Gerçek geçmişle backfill
denendi ve imkânsız çıktı:** `reltuples` collector'a 2026-08-31'de eklenmiş.
Haziran 1.105 satırın 0'ında, Temmuz 14.467'nin 0'ında, Ağustos 215.344'ün
29'unda dolu. Taban `table_size_bytes / reltuples` olduğu için satır sayısı
olmayan gözlem hiçbir işe yaramıyor. Eksik olan `relid` veya `source` değil,
`reltuples` — hiçbir backfill yardım etmiyor.

### Üç sonda — ikisi susmalıydı

| Sonda | Kurgu | Beklenen | Sonuç |
|---|---|---|---|
| `probe_boundary` | tam 3.0x, 30 gece | warning | ✅ warning |
| `probe_growth` | boyut ve satır 3'er kat, bayt/satır sabit | sessiz | ✅ sessiz |
| `probe_young` | 12x şişme ama 10 gece / 9 gün | sessiz | ✅ sessiz |

Kanıtlananlar: `>=` sınırda gerçekten tetikliyor (3.0 sessiz kalmıyor); büyüme
şişme sanılmıyor; **21 gece + 28 gün uygunluk kapısı tutuyor** — 29 Eylül'e
kadar üretimi koruyan tek şey oydu ve bugüne dek varsayımdı.

Sayılar aritmetikle birebir: 286 MB güncel, 95 MB sıkışık, 191 MB israf.

### Sonda üç yanlış iddia buldu (`e6cd5cd`)

Hesap doğruydu ama mesaj artık doğru olmayan üç şey söylüyordu:

| İddia | Gerçek | Ne zaman geçersizleşti |
|---|---|---|
| "tahmin değil, iki gözlem arasındaki fark" | Payda `reltuples`, katalog **tahmini** | Dış incelemede geri çekilmişti |
| "tarihsel minimum" | En düşük **üç günün medyanı** | V110 |
| "bu pay hesaptan düşüldü" (fillfactor) | Taban aynı rejimden, pay zaten içinde | V108 |

V111 kural açıklamasını düzeltmişti ama **alarm metnini düzeltmemişti** —
kullanıcının Telegram'da ve UI'da okuduğu yer tam orasıydı.

Daha kötüsü: **iki unit test bu yanlış ifadeleri kilitliyordu**
(`contains("tahmin değil")`, `contains("düşüldü")`). Geri çekilmiş bir iddiayı
doğrulayan test davranışı korumaz, hatayı korur. İkisi de tersine çevrildi;
`docs/alert-reference.md`'deki aynı iddialar da düzeltildi.

29 Eylül beklenseydi bu üç yanlış gerçek bir alarmda, müşteriye gitmiş hâlde
bulunacaktı.

---

## 4. Merkezi DB neden büyüyor — retention değil, yazma çoğaltması (PGSTAT-P0-047)

Sıradaki iş `control`/`dim` retention'ı sanılıyordu. Ölçüm başka bir şey
söyledi.

```
dim.statement_series   942 MB   1.761.565 canli satir
  n_tup_ins            92.902
  n_tup_upd       721.732.172   -> eklenen satir basina ~7.800 guncelleme
  n_tup_hot_upd             0   -> %0.0

dim.relation_ref        30 MB
  n_tup_ins             2.801
  n_tup_upd        27.529.066
  n_tup_hot_upd             0
```

**Sebep:** her toplama döngüsü, `statement_series_id`'yi geri alabilmek için
her seriyi upsert ediyor. `DO NOTHING` çakışmada satır döndürmediği için
`DO UPDATE` yazmak zorunlu — ve `last_seen_at = now()` her seferinde yanında
gidiyor. `statements_interval` 300 saniye olduğundan satır başına **günde 288
yeniden yazma**, hem de collector'ın hiç okumadığı bir damga için.

**HOT neden %0 — kontrol grubu aynı veritabanında:** `dim.database_ref` 37.840
güncellemenin %50'sini HOT yapıyor. Tek farkı `last_seen_at`'inin **indeksli
olmaması**. İndeksli kolon her güncellemede değişince HOT imkânsız hâle gelir
ve her yazma indekse de gider.

Ölü satır oranı %4.6'da kalıyor — ne autovacuum sorun sayıyor, ne de
`dead_tuple_ratio` alarmı görebiliyor. **Bu, fiziksel şişme kuralının tam
olarak yakalaması gereken vaka;** kural 29 Eylül'de açıldığında ilk gerçek
testi merkezi DB olacak.

`dim.query_text` farklı bir vaka: 6.810 güncelleme, 569 ölü satır. Oradaki 316
MB gerçek sorgu metni ve sorunu retention (PGSTAT-P0-023), şişme değil.

### İki yanlış varsayım düzeltildi

- **"Hiçbirinde purge yok"** — yanlıştı. `ops.job_run_instance` zaten purge
  ediliyor (`PurgeEvaluator:470`), ardından `VACUUM` da yapılıyor.
- **5 dakikalık aralık keyfi sanılmıştı** — gerekçesi hiçbir yerde kayıtlı
  değil, ama karşılığı gerçek: `insights.ts:92`, altı saatten kısa pencereleri
  **beş dakikalık adımlarla** çiziyor. Aralık genişlerse bir sorgunun ne zaman
  bozulduğu bulanıklaşır. **Aralığa dokunulmadı.**

Müşteri "kaynak makineden aynı SQL'i tekrar istemeyelim diye bir şey vardı"
diye hatırlattı. Mekanizma gerçek ve **plana konu değil**:
`findSeriesWithoutQueryText`, `query_text_id is null` ile çalışıyor;
`last_seen_at` orada hiç geçmiyor. Varsayılmadı, koda bakıldı.

### Yapılan (`f28c875`)

İki farklı düzeltme, çünkü iki çağrı yeri farklı:

- **`relation_ref`** `void` döndürüyor → `do update`'e `where` eklendi. Ad
  değiştiyse hemen, değişmediyse damga bayatlayınca yazar.
- **`statement_series`** `returning` ile id alıyor → `where` eklemek koşul
  yanlışken satır döndürmemesine ve `queryForObject`'in patlamasına yol
  açardı. Artık **önce okuyor**, yalnızca yazacak bir şey varsa upsert ediyor.

`query_text`, `database_ref`, `role_ref` dokunulmadı — ölçümde patoloji yok.

**V112** iki `last_seen_at` indeksini kaldırdı, `fillfactor=85` verdi.
İndekslerin başka müşterisi olmadığı varsayılmadı, kod tarandı: collector
damgayı hiç okumuyor; `statements.ts` sıralaması zaten `ilike` ile tam tarama
yapıyor; `insights.ts` join'leri `uq_relation_ref` kullanıyor;
`instances.ts`'teki sıralama `database_ref`'e ait ve o tablonun böyle bir
indeksi zaten yok.

**Kabul edilen bedel:** `last_seen_at` bir saate kadar eski olabilir. Görüldüğü
tek yer arama sonucu sıralaması.

---

## Commit'ler

| | |
|---|---|
| `c57a130` | `index_size_bytes`'ı JDBC'nin gerçekten döndürdüğü tiple oku |
| `3ab65dd` | İzleme listesini şişme kuralının değerlendirebildiği tablolarla sınırla |
| `e6cd5cd` | Şişme mesajının sahip olmadığı kesinliği iddia etmesini durdur |
| `bc7f576` | Sondaların kanıtladıklarını, sessizlikleri dâhil, board'a işle |
| `f28c875` | 1.76M boyut satırını beş dakikada bir yeniden yazmayı bırak |

---

## Yarına devir (2026-09-02)

### 0. Deploy sonrası ilk sinyal (2026-09-01 14:03) — HOT sıfırdan çıktı

Deploy 14:00 civarında tamamlandı. V112 doğrulandı: iki indeks kalkmış, iki
tabloda `{fillfactor=85}` duruyor. Toplama sağlam: son 15 dakikada 20.015
`pgss_delta` satırı, son bir saatte 517 yeni seri — `select`-önce yolu id'leri
doğru döndürüyor, FK ihlali yok.

**Yarınki karşılaştırmanın çıpası bu okuma:**

| Tablo | `n_tup_upd` | `n_tup_hot_upd` |
|---|---|---|
| `statement_series` | 728.300.801 | **152.790** |
| `relation_ref` | 27.720.735 | 0 |
| `database_ref` (kontrol) | 38.236 | 19.120 |

Sabah (deploy öncesi) aynı sayaçlar: 721.732.172 / **0**, 27.529.066 / 0,
37.840 / 18.916.

`statement_series` HOT'u **0'dan 152.790'a** çıktı. İndeks kaldırma ve
`fillfactor` yeni yazılan sayfalarda hemen etkili oldu; repack'i öne alma
gerekçesi ortadan kalktı.

`relation_ref` hâlâ 0, ama beklenen bu: sabahtan beri aldığı 191.669
güncellemenin neredeyse tamamı deploy öncesi. `db_objects` 30 dakikada bir
koştuğu için ilk anlamlı ölçüm yarın.

### 0b. Ölçüm yarına kalmadı — aynı gün doğrulandı (17:21) ve şişme geri alındı

14:03 çıpasından 3 saat 18 dakika sonra:

| | Yeni güncelleme | HOT | **Pencere HOT oranı** |
|---|---|---|---|
| `statement_series` | 247.031 | 242.989 | **%98.4** |
| `relation_ref` | 98.597 | 96.061 | **%97.4** |

`pg_stat_user_tables`'taki kümülatif `hot_yuzde` (%0.1, %0.3) yanıltıcı — geçmişteki
728 milyon HOT olmayan güncelleme tarafından eziliyor. Ölçülmesi gereken pencere
oranı ve o **%98**. Kontrol grubu `database_ref` %50'de kaldı; ikisi de onu geçti.

Yazma hızı: `statement_series` saatte ~74.800 güncelleme. Deploy öncesi ölçülen
hız saatte ~4,7 milyondu → **~63 kat az**.

**Birikmiş şişme geri alındı** (AC6):

| Tablo | Sabah | VACUUM FULL öncesi | Sonra |
|---|---|---|---|
| `statement_series` | 942 MB | 792 MB | **544 MB** |
| `relation_ref` | 30 MB | 24 MB | **13 MB** |

Sabahtan `VACUUM FULL` öncesine kadar olan 150 MB'ı autovacuum kendisi geri aldı —
HOT çalışmaya başlayınca alan yeniden kullanılabilir hâle geldiği için.

544 MB'ın dağılımı: **heap 290 MB** (1.76M satır, ~165 bayt/satır) + **indeksler
255 MB**. "~250 MB'a düşer" tahminim yanlıştı; hesabı heap üzerinden yapıp
`pg_total_relation_size`'ın indeksleri de kapsadığını atlamıştım. `VACUUM FULL`
indeksleri de yeniden yazdığı için ek kazanç yok; 544 MB dürüst taban.

İndeks kırılımı: `uq_statement_series_natural` 177 MB / 745M tarama (yeni
"önce oku" yolu bunu kullanıyor, kalmalı), `..._instance_query_text` 35 MB /
215K, `pkey` 32 MB / 76M, `..._instance_ids` 10 MB / **817 tarama** — neredeyse
kullanılmıyor ama küçük, şimdilik dokunulmadı.

### 1. Yazma çoğaltması düzeldi mi — ASIL KONTROL

```sql
select relname, n_tup_upd, n_tup_hot_upd,
       round(100.0 * n_tup_hot_upd / nullif(n_tup_upd,0), 1) as hot_yuzde,
       pg_size_pretty(pg_total_relation_size(relid)) as boyut
from pg_stat_user_tables
where schemaname = 'dim'
  and relname in ('statement_series','relation_ref','database_ref');
```

**Farkı 14:03 çıpasından al** (yukarıdaki bölüm 0), sabahki değerlerden değil —
sabahki sayaçlar deploy öncesini de içeriyor.

| Tablo | 14:03 `n_tup_upd` | 14:03 `n_tup_hot_upd` |
|---|---|---|
| `statement_series` | 728.300.801 | 152.790 |
| `relation_ref` | 27.720.735 | 0 |
| `database_ref` | 38.236 | 19.120 |

- `statement_series` HOT'u zaten çalışıyor; yarın bakılacak olan **oranın ne
  kadar yükseldiği**. Kontrol grubu `database_ref` %50 yapıyor; ona yaklaşması
  beklenir.
- `relation_ref` HOT'u **sıfırdan çıkmalı**. Çıkmazsa o tabloda mevcut sayfalar
  dolu demektir ve repack ondan başlar.
- `n_tup_upd` artışı: eskiden saatte ~21 milyon (1.76M satır × günde 288 ÷ 24).
  Saatlik kısmayla ~1.8 milyon beklenir.

**Boyut değişmeyecek.** 942 MB olduğu gibi kalır; beklenen değişim yazma
hızında.

### 2. Sırada — PGSTAT-P0-047 kalan adımlar

- **AC6 (3. adım):** birikmiş şişmeyi bir kez geri al. `pg_repack` varsa o
  (kilitsiz), yoksa bakım penceresinde `VACUUM FULL`. Beklenti 942 MB → ~250
  MB. **1. maddedeki ölçüm tutmadan yapılmamalı** — yazma hızı düzelmeden geri
  alınan alan birkaç haftada geri dolar.
- **4. adım = PGSTAT-P0-023:** `dim` retention'ı. FK zinciri sırayı belirliyor:
  `pgss_delta` partition'ları zaten düşüyor → `statement_series` instance kesim
  tarihine göre → referanssız `query_text`. En küçük kazanç, en sona.

### 3. Not edilen, görev açılmamış

`purgeJobRunHistory`, retention'ı `select job_run_retention_days ...
where is_active limit 1` ile okuyor — **`order by` yok**, hangi politikanın
değeri geldiği belirsiz. P0-037'de düzelttiğimiz ailenin gözden kaçmış üyesi.
Etkisi düşük ama belirsizlik gerçek.

`ops.job_run` bugüne dek hiç autovacuum görmemiş (`last_autovacuum` boş),
1M satır / 236 MB.

### 4. Takvim

**~2026-09-29** — `table_space_bloat` kuralı (rule 176) hâlâ **kapalı**.
Gerçek seri 2026-08-31'de başladı; 21 gece + 28 gün yayılım o tarihte dolar.
Açmadan önce ilk gerçek çıktı elle bir ölçümle karşılaştırılacak. Sonda
verileri ve alarmları temizlendi, `is_enabled` `false`'a alındı — doğrulandı.
