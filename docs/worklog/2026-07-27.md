# 2026-07-27

## Yapılanlar

- Kullanıcı geri bildirimi: P0-024'ün açtığı `system_instance_unreachable`
  Telegram alert'i anlaşılır değildi — hangi host etkilendiği hiçbir yerde
  yazmıyordu ("Instance unreachable ... The connection attempt failed.").
  Kısa bir tartışmayla kapsam netleşti: dar (sadece bu mesajı düzelt) yerine
  merkezi çözüm seçildi.
- Kök neden: `AlertService.upsertSystemAlert` ve çağıranları title/message'ı
  elle inşa ediyordu, `instancePk` sadece `details_json`'a (UI-only) gidiyordu.
  Ayrıca `InstanceDetail.tsx`'teki `BootstrapBanner` bileşeni sadece bootstrap
  aşaması alert kodlarını tanıyordu, `system_instance_unreachable`'ı hiç
  yakalamıyordu — UI'da da sessiz kalıyordu.
- Düzeltme (PGSTAT-P0-024'e AC3-AC5 olarak eklendi, ayrı görev açılmadı):
  `AlertService.withInstanceLabel()` eklendi — `instancePk` verilen her
  `upsertSystemAlert` çağrısında title'a otomatik olarak
  `— <display_name> (<host>:<port>)` ekleniyor (tek merkezi nokta, gelecekteki
  tüm instance-scoped sistem alertlerini de kapsıyor). `BootstrapBanner`'ın
  `bootstrapCodes` listesine `system_instance_unreachable` eklendi.
  Dashboard/Instances listesinin zaten host + durum + hata sayısı gösterdiği
  doğrulandı, orada değişiklik gerekmedi.
- `mvn compile`, `mvn test`, `ui tsc --noEmit` hepsi temiz geçti.
- İlk düzeltme push edilirken lokal kalite kapısı (`eslint .`) UI'daki
  önceden birikmiş ~130-420 lint hatası yüzünden push'u reddetti — benim
  değişikliğimle ilgisiz teknik borç. `check-quality-gates.mjs` düzeltildi:
  eslint artık sadece değişen dosyaları kontrol ediyor ve engelleyici değil
  uyarı niteliğinde (`tsc`/`mvn test` hâlâ engelleyici).
- **Kullanıcı canlıda pg_hba erişimini tekrar kaldırıp test etti — alert
  yine gelmedi.** Ekran görüntüsü: instance `discovering`/`ready` badge
  gösteriyordu (asla `degraded`), "Son Hata" alanında pg_hba metni vardı
  ama Telegram sessizdi. İkinci bir kök neden bulundu: instance daha önce
  `ready→degraded`'a düşüp otomatik retry ile `pending→discovering`'e
  dönmüş, discovery orada tekrar aynı hatayı almış ama bu hata tamamen
  ayrı bir kod yolunda (`DiscoveryCollector.discover()`'ın catch bloğu)
  yakalanıyordu — sadece `last_error` yazıyor, hiç alert açmıyordu. Ayrıca
  UI'daki `BootstrapBanner` sadece `bootstrap_state==='degraded'` için
  render oluyordu, `discovering` durumunda hiç görünmüyordu.
- İkinci düzeltme: `BootstrapHandler.handleDiscovering` artık discovery
  başarısız olduğunda da `SYSTEM_INSTANCE_UNREACHABLE` alert'ini açıyor
  (`raiseUnreachableAlert()`, `StateRepository.getLastError()` yeni okuma
  metodu ile hata metnini okuyor). `BootstrapBanner`'ın koşulu
  `bootstrap_state !== 'ready'` olarak genişletildi — artık her
  bootstrap-tamamlanmamış state'te (degraded, discovering, vb.) çalışıyor.
  `mvn compile`/`mvn test`/`ui tsc --noEmit` tekrar temiz geçti.
- **Kullanıcı alert geldiğini onayladı ama ekranın karışık olduğunu bildirdi**:
  aynı uzun PostgreSQL hata metni Mesaj/Detay/ham JSON'da 3-4 kez tekrar
  ediyordu. Sadeleştirme: (a) collector mesajları kısa/anlaşılır cümleye
  indirildi, teknik hata metni sadece `details_json.context.last_error`'da
  kaldı; (b) `Alerts.tsx`'teki ham JSON fallback'i, okunur bir panel zaten
  aynı veriyi gösteriyorsa katlanabilir "Ham veriyi göster" bağlantısının
  arkasına alındı (öncesinde ikisi de her zaman birlikte gösteriliyordu);
  (c) `context.reason` değeri (`discovery_failed`/`degraded_after_ready`)
  artık kısa Türkçe etiketle gösteriliyor (`reasonLabel()`).
  `mvn compile`/`mvn test`/`ui tsc --noEmit` tekrar temiz geçti.
- **Kullanıcı pg_hba yetkisini geri vermeden Telegram'dan "Çözüldü" bildirimi
  aldı** — yanlış pozitif. Üçüncü kök neden bulundu:
  `SystemHealthEvaluator.checkInstanceUnreachable()` her 5 dakikada bir,
  `INSTANCE_UNREACHABLE` kodlu TÜM açık alertleri tarayıp
  `consecutive_failures == 0` olan her birini kapatan, benden önce var olan
  bir mekanizma. Benim ikinci düzeltmemdeki `BootstrapHandler` yolu
  `consecutive_failures`'ı hiç artırmıyor (sadece `JobOrchestrator` artırıyor),
  bu yüzden o yoldan açılan alert baştan itibaren `consecutive_failures==0`
  görünüyor ve instance hâlâ gerçekten erişilemezken 5 dakika içinde
  otomatik kapatılıyordu.
- Üçüncü düzeltme: `checkInstanceUnreachable()`'ın auto-resolve döngüsü artık
  `consecutive_failures==0` yetmiyor, ayrıca `bootstrap_state=='ready'`
  olmasını da şart koşuyor (`isBootstrapReady()` yeni yardımcı). Böylece iki
  farklı yoldan açılan alert de doğru şekilde sadece instance gerçekten
  `ready`'ye dönünce kapanıyor. `mvn compile`/`mvn test` tekrar temiz geçti.
- **Kullanıcı üçüncü düzeltme sonrası tam döngüyü tekrar test etti ve "oldu
  tamam" ile onayladı**: alert host adıyla açılıyor, erişim geri verilmeden
  birden fazla 5 dakikalık `SystemHealthEvaluator` döngüsü boyunca erken
  kapanmadan açık kalıyor, erişim geri verilince doğru şekilde kapanıyor.
  PGSTAT-P0-024 kanıtla kapatıldı (AC5 done, customer_acceptance accepted),
  `max_in_progress` 1'e geri çekildi.

## Board hareketleri

- PGSTAT-P0-024: AC3/AC4 done olarak genişletildi (host label + UI banner),
  AC5 (canlı doğrulama) pending. Ayrı P0-025 açılıp sonra P0-024'e gömüldü
  (bağımlılık/max_in_progress çakışmasını önlemek için).
- PGSTAT-P0-024: canlı testte ikinci kök neden bulundu (discovering döngüsü),
  düzeltildi, verification_evidence'a eklendi, AC5 hâlâ pending (yeni deploy
  + tekrar canlı test bekliyor).
- PGSTAT-P0-024: alert mesajı/detay ekranı sadeleştirmesi de
  verification_evidence'a eklendi, AC5 hâlâ pending.
- PGSTAT-P0-024: üçüncü kök neden (erken auto-resolve) bulundu, düzeltildi,
  verification_evidence'a eklendi, AC5 hâlâ pending.
- PGSTAT-P0-024: done — canlı doğrulama tamamlandı, customer_acceptance
  accepted, `max_in_progress` 1'e geri çekildi.

## Kararlar

- Alert mesajlarında host/instance bilgisi merkezi bir noktada
  (`AlertService.upsertSystemAlert`) garanti altına alınacak; her çağıran
  kodun host adını elle eklemesine güvenilmeyecek.

## Yarına kalanlar

- PGSTAT-P0-002: 904 alanın kontrat incelemesi (tek aktif görev,
  max_in_progress=1'e dönüldü).
- PGSTAT-P0-023 (büyüyen control/dim tablolarına retention) artık
  bağımlılık olarak unblocked, sırada.
