# 2026-07-17

## Yapılanlar

- Codex projelendirme çalışması (agentic DBA dokümantasyonu, ~12.5k satır)
  üç koldan incelendi: mimari/plan tutarlılığı, kontrat kalitesi, kod-doküman
  uyumu. Genel hüküm: sağlam; driftler ve boşluklar tespit edildi.
- Tutarlılık temizliği: execution plan board ile senkronlandı (P0-001 done,
  eski sayılar düzeltildi, P0-021 eklendi), problem_type/action_type enum'ları
  mimari ile kontratlar arasında uzlaştırıldı, .kiro spec'leri board'a
  retroaktif done görev olarak bağlandı (P1-002/P1-003) ve git'e alındı.
- SDLC sağ tarafı kuruldu: `docs/test-strategy.md` (dürüst baseline +
  katman kapıları), `docs/release-checklist-template.md` (semver kuralı +
  14 bölüm), ADR pratiği (`docs/adr/` + ADR-0001 kurucu kararlar).
- Lokal CI: GitHub Actions kullanılmayacak kararı ADR-0002 ile kayda geçti;
  `scripts/check-quality-gates.mjs` pre-push'a bağlandı (katman bazlı
  mvn test / tsc / eslint), `make verify` eklendi.
- Retention analizi: 48 retention'sız tablo sınıflandırıldı; ~12 büyüyen
  tablo tespit edildi (P0-023'te kayıtlı).
- **Üretim hatası bulundu ve kapatıldı (P0-022)**: V067'nin 7 snapshot
  tablosu (wal_receiver + 6 progress) PartitionManager/PurgeEvaluator'da
  yoktu; partition'lar 2026-06-16'da tükenmiş, bir aydır insert'ler
  başarısızdı. Canlıda doğrulandı, iki dosyalık düzeltme yazıldı, mvn test
  geçti, main'e merge edilip deploy edildi, canlıda partition'ların
  20260731'e kadar oluştuğu doğrulandı. 2026-06-16..07-17 veri boşluğu
  dictionary'ye işlendi.
- Codex dokümantasyon branch'i main'e merge edildi; prod artık tüm
  governance/gate altyapısıyla main'den çalışıyor.
- **İkinci üretim hatası bulundu (P0-024)**: Kullanıcı raporu — çalışan bir
  instance'ın pg_hba.conf yetkisi kaldırılırsa sistem alerti/Telegram
  bildirimi gelmiyordu. Sistematik debug ile kök neden izlendi:
  `JobOrchestrator.handleSecretOrAuthError` connect/auth hatasında instance'ı
  `bootstrap_state='degraded'`e çekiyor; `findDueInstances` sadece
  `ready` durumundakileri seçtiği için instance bir sonraki döngüde kuyruktan
  düşüyor, `consecutive_failures` donuk kalıyor ve `SystemHealthEvaluator`'ın
  `>=3` eşiğine hiç ulaşmıyor — alert hiç açılmıyor, sadece INFO log yazılıyor.
  Düzeltme: `handleSecretOrAuthError` artık mevcut `SYSTEM_INSTANCE_UNREACHABLE`
  alert kodunu (yeni kod/migration yok) açıyor; instance tekrar `ready`'ye
  dönünce `BootstrapHandler.handleEnriching` aynı alert'i otomatik kapatıyor.
  `mvn compile`/`mvn test` geçti. Canlı doğrulama (gerçek pg_hba testi) ve
  deploy bekliyor.

## Board hareketleri

- PGSTAT-P0-012: planned -> done (release checklist)
- PGSTAT-P0-020: yeni -> done (test stratejisi)
- PGSTAT-P1-002/P1-003: yeni -> done (retroaktif .kiro kayıtları)
- PGSTAT-P1-005: yeni -> done (ADR pratiği)
- PGSTAT-P1-006: yeni -> done (lokal kalite kapıları)
- PGSTAT-P1-007: yeni -> planned (operasyon runbook'u, ertelendi)
- PGSTAT-P0-022: yeni -> done (V067 partition üretim hatası)
- PGSTAT-P0-023: yeni -> planned (büyüyen control/dim tablolarına retention)
- PGSTAT-P0-024: yeni -> in_progress (sessiz instance-degrade alert hatası)

## Kararlar

- GitHub Actions kullanılmayacak; kalite kapıları lokal —
  [ADR-0002](../adr/ADR-0002-local-quality-gates-no-github-actions.md)
- Kurucu ürün sınırı kararları ADR sistemine geçirildi —
  [ADR-0001](../adr/ADR-0001-product-family-baseline.md)
- Runbook (P1-007) gerçek bir deploy döngüsünde doğrulanmak üzere ertelendi.
- Günlük iş özetleri `docs/worklog/` altında tutulacak (bu dosya ilki).

## Yarına kalanlar

- PGSTAT-P0-024: deploy + canlı doğrulama (pg_hba testi) bekliyor, sonra kapanır.
- PGSTAT-P0-002: 904 alanın kontrat incelemesi (aktif görev).
- P0-011 (redaction) ve P0-023 (retention) sırada.
